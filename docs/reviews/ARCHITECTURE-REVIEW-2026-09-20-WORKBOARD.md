# Architecture review work board — one status for every plan, whoever is executing it

**Status:** live · **Tracks:** the implementation plans for
[ARCHITECTURE-REVIEW-2026-09-20.md](ARCHITECTURE-REVIEW-2026-09-20.md)
(revision 3) · **Written:** 2026-09-20

The plans below will be executed by sessions on different vendors — Claude,
GPT (Astra), and models reached through OpenRouter — at the same time. This
file is the **only** shared status. Nothing else (chat, a vendor's memory, a
PR title alone) counts as having claimed or finished work. It lives in the
repository so every vendor reads and writes it the same way: through git and
the Forgejo API, with no vendor-specific tooling.

## How to claim, work and finish a plan

Every executing session follows this exactly. The reason for each rule is the
failure it prevents.

1. **Read before claiming.** Read the plan, its rows in
   [ARCHITECTURE-REVIEW-2026-09-20-ASSESSMENT.md](ARCHITECTURE-REVIEW-2026-09-20-ASSESSMENT.md),
   and this board. A plan whose row says anything but `unclaimed` is someone
   else's; do not start it, do not "help" on it. Two sessions on one plan
   produce two branches that cannot both merge.
2. **Claim = a draft PR that changes this file.** Branch from current `main`
   as `plan/<board-id>` (for example `plan/S-01`), edit your row here to
   `claimed`, fill in **Model**, **Session** and **Branch**, commit, push, and
   open a **draft** PR titled `WIP: <board-id> <plan title>` whose body starts
   with the same three lines. The push is the atomic claim: if it is rejected
   as non-fast-forward, someone claimed something since you read the board —
   fetch, re-read your row, and only proceed if it is still `unclaimed`.
   The draft PR is the claim's public record; a board edit with no PR is not
   a claim and may be reverted.
3. **Identity is mandatory.** **Model** is the exact model identifier your
   runtime reports (`claude-fable-5-1`, `gpt-5`, `anthropic/claude-opus-4`
   via OpenRouter, …), not a product name. **Session** is your session's own
   id or URL (Claude: the `https://claude.ai/code/session_…` URL; GPT: the
   conversation id; OpenRouter: the generation/thread id your client
   exposes). If your runtime gives you no id, write `none:<vendor>:<date>`
   and say so in the PR body. The same two values go in **every commit** on
   the branch as trailers:

   ```text
   Agent-Model: <model identifier>
   Agent-Session: <session id or URL>
   ```

   (Claude sessions keep their existing `Co-Authored-By` and
   `Claude-Session` trailers as well.) This is what lets a later reader tell
   which model wrote which change without guessing from prose style.
4. **One implementation PR owns the whole plan.** Work only inside the
   plan's milestones, but keep every milestone in the draft PR opened by
   rule 2. Milestones are logical commits and rows in the plan's
   **Execution log**, not separate PRs: record date, model, session,
   milestone, commit and outcome, and update this board's **Status** and
   **Last update** cells in that same plan PR. Evidence that can exist only
   after merge (deployment, fleet or device results) is appended later to
   the same execution log and board row through one evidence-only docs PR;
   it never creates milestone PRs or a second status ledger. A plan that
   tells you to stop and flag something means stop and flag it — in the PR
   body and in the board's **Notes** cell — not "make a reasonable choice".
5. **Statuses** (the only words allowed in the Status column):
   `unclaimed` · `claimed` (branch exists, no code yet) · `in-progress`
   (the plan PR contains implementation commits) · `blocked: <reason>`
   (waiting on a fleet measurement, a device test, or a decision that is
   Paul's — say which) ·
   `in-review` (adversarial review requested; name the reviewer session) ·
   `merged: <landed milestones>` (the one implementation PR landed, but
   post-merge evidence is still owed; name the logical milestones without
   implying separate PRs) · `done` (every milestone landed and its acceptance
   evidence is recorded in the plan) · `abandoned: <reason>` (release the row
   back to `unclaimed` in the same edit, keeping the reason in Notes).
6. **Release what you cannot finish.** If your session is ending with a
   milestone unfinished, set `blocked:` or `abandoned:` with a one-line
   handover in Notes (branch, what is done, what is next, what is broken)
   in the same push. A silently stale `in-progress` row costs the next
   session a day of archaeology. Rows untouched for **7 days** in
   `claimed` or `in-progress` may be reclaimed by anyone after they add a
   note saying so.
7. **Never edit another session's row** except to reclaim a stale one under
   rule 6, or to record objective post-merge evidence and mark it `done`
   after its implementation PR lands. Paul's standing rule is that the
   executing session merges its own implementation PR; if you are recording
   evidence or merging for someone else, say why in Notes.
8. **The PR lifecycle is the repository's**: draft until ready · adversarial
   review · findings folded · fast lane green (`make unit` locally for Rust
   changes; the CI fast lane also runs it on ready Rust PRs — see
   `docs/ci/RUST-TEST-EXECUTION-POLICY.md`) · merge it yourself · delete the
   branch. Un-drafting on Forgejo is a title edit that does not re-trigger
   CI; close and reopen the PR to start the lane.
9. **Fleet and device evidence** that a session cannot produce itself goes
   in the plan's Execution log as `needs: <what>` with the plan's GPT prompt;
   before merge the row's Status becomes `blocked: fleet evidence`. If the
   implementation is safe to merge without that observation, merge the one
   plan PR with Status `merged`, then record the result through the
   evidence-only docs PR from rule 4. Do not mark a plan `done` from a device
   observation nobody has made.

A session that cannot follow rule 2 (no push access) has no business
claiming; it can still review.

## The board

Board ids are stable handles for claims and PR titles; the review ids in the
third column are what the plan executes. **Priority** is from the review's
§5: `week` · `month` · `quarter` · `design` (a design document whose output
is a decision or a follow-on plan, not code). Every row now has a
document (the twelve written second landed on 2026-09-20 as well).

| Id | Plan | Executes | Priority | Status | Model | Session | Branch / PR | Last update | Notes |
|---|---|---|---|---|---|---|---|---|---|
| S-01 | [PROCESS-OUTPUT-CAPTURE-AND-SCAN-PROBE-BOUNDS](../streaming/PROCESS-OUTPUT-CAPTURE-AND-SCAN-PROBE-BOUNDS.md) | §2.1, C12 | week | merged: M1-M3 | gpt-5.6-sol | agent:/root/s01_builder | [PR #396](http://192.168.4.7:3000/noirr/plurx/pulls/396) | 2026-09-21 | M1-M3 merged as `f0af512d` after the sole adversarial finding and fast lane were green. M4 still needs the merged image deployed and §5.4 fleet evidence. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-02 | [MEDIA-BODY-BUFFERS](../streaming/MEDIA-BODY-BUFFERS.md) | §2.4, C1 | week | merged: M1, Decision 1 (128 KiB shared read), Decision 6 (TCP_NODELAY); M2 not started | claude-opus-5-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #487](http://192.168.4.7:3000/noirr/plurx/pulls/487) via integration [PR #493](http://192.168.4.7:3000/noirr/plurx/pulls/493); M1: [PR #410](http://192.168.4.7:3000/noirr/plurx/pulls/410) via [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) | 2026-09-24 | M1 merged as `6683eaa4a`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`: one shared 256 KiB reader capacity, with the delivery-proof granularity deliberately decoupled from it. The plan's one adversarial review ([comment 3667](http://192.168.4.7:3000/noirr/plurx/pulls/410#issuecomment-3667)) raised four findings, all fixed and none disputed, disposed in [comment 3715](http://192.168.4.7:3000/noirr/plurx/pulls/410#issuecomment-3715). The load-bearing P1 is that the 256 KiB read had carried the pump's acknowledgement unit with it, so for every media object at or under 256 KiB the delivery proof collapsed to a single body poll: a response the client opened and abandoned still advanced the VOD fetched-segment frontier, renewed the playback lease and credited the full object to the meter. `init.mp4`, subtitle segments and audio-only or low-bitrate renditions are all in that class, which makes it the common case rather than a rare one. Three existing delivery-accounting tests failed at the reviewed head and were the evidence; they are green again because `MEDIA_BODY_ACK_GRANULARITY` now holds the proof at 4 KiB, not because any fixture grew - the review ruled that out explicitly and the plan's section 7 records it as the rejected alternative. The plan's claim that the change was invisible to those tests by construction, and the grep recipe that could not select them, are both gone from section 5.1. Decisions 1 and 6 merged as `f971f178b`, one of the two heads (exact head `76bdd7f78`) carried into main by integration [PR #493](http://192.168.4.7:3000/noirr/plurx/pulls/493) at `b24b00410`. Both were taken by the coordinating session on Paul's behalf, and he can overturn either: **Decision 1** - the shared media read is 128 KiB (`7eea547fe`), with `MEDIA_BODY_ACK_GRANULARITY` pinned separately at 4 KiB; **Decision 6** - `TCP_NODELAY` on accepted HTTP connections (`dbcb1168f`). §5.1 before/after was measured 2026-09-24 (plan §5.1.1, `docs/evidence/media-body-buffers-m1-measurement-2026-09-24.md`, evidence-only PR #483): 128 KiB and 256 KiB matched on throughput and latency, and 128 KiB cost about two thirds of the memory. The HLS p50 Nagle/delayed-ACK hypothesis was tested on nuc3. 128 KiB with `TCP_NODELAY` had p50/p95/p99 of 15–16 / 18 / 19–20 ms; without it, 49–51 / 52–58 / 52–58 ms. Nothing else moved in throughput, memory or threads, so `dbcb1168f` sets the option on accepted connections (plan Decision 6). Its cost is packets on HLS bodies, which the pump flushes in 4 KiB writes: on loopback, 8.7× the data packets per segment at MTU 65536 and no difference at MTU 1500; on a real 1500-MTU link, at most about 6% (not measured, watched in plan §6). The final state against 4 KiB (plan §5.1.2): direct play 373 → 2747 MB/s median; HLS p99 66–76 → 19–20 ms, with p50 also better, at 25 → 15 ms. Group A costs 8–10 MiB for 8 bodies over 4 KiB, or 1.0–1.3 MiB per body. That is above the §2.1 upper bound, which was derived for 256 KiB buffers; the per-body model underestimates measured RSS by about 2×. That still fails the literal "not worse", and it is the cost Decision 1 accepts. Group B's median grew 33.2 → 38.7 MiB at higher concurrency, far from the veto. The single review of #487 ([comment 4265](http://192.168.4.7:3000/noirr/plurx/pulls/487#issuecomment-4265)) raised three P2 findings, all addressed and disposed in [comment 4331](http://192.168.4.7:3000/noirr/plurx/pulls/487#issuecomment-4331). The load-bearing one: `TCP_NODELAY` does turn an HLS body into many small packets, because the pump flushes each 4 KiB piece; the `disable_nagle` doc comment and Decision 6 now state that cost, §6 gains an `nstat` packet-rate check, and coalescing is deferred to M2. **What remains:** M2 (not started; its batch acknowledgement must be reconciled with Decision 5 before it is built, and it needs its one-week media1 prerequisite); Paul's confirmation or reversal of Decisions 1 and 6; and, post-deploy, the §6 telemetry and packet-rate check on media1. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-03 | [ENCODED-VOD-HOLD-AND-RELEASE](../streaming/ENCODED-VOD-HOLD-AND-RELEASE.md) | §2.6 | week | merged: M1-M3 | gpt-5.6-sol | agent:/root/s01_builder | [PR #412](http://192.168.4.7:3000/noirr/plurx/pulls/412) | 2026-09-21 | M1-M3 merged from PR #412: the 90-second ahead-hold low-water latch, the pure contention table with its explicit `YieldToWaiter` termination, and the `vodserve` wiring for the encoded SIGSTOP hold, VOD registration wake, one-second stopped-encoder poll, success-only latch transitions and closed-label generation/termination metrics. Sole adversarial finding #3230 is disposed by real-`spawn_driver` regressions covering the no-kick poll yield, the low-water re-admission fence, and the session-TTL reap with full-permit reacquisition; that evidence is registered in `validation/regressions.d/39472d6e-encoded-vod-driver-lifecycle.toml`. The plan is **not done**. M0 controlled before-counts and M4 post-deploy fleet traces are **post-merge** evidence and are recorded later through the rule-4 evidence-only docs PR; no count was fabricated from an empty log window. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-04 | [FONT-ATTESTATION-AND-BLOCKING-IO](../streaming/FONT-ATTESTATION-AND-BLOCKING-IO.md) | §2.7 | week (M1) / month (M2) | merged: M1 | gpt-5.6-sol | agent:/root/p01_builder | [PR #413](http://192.168.4.7:3000/noirr/plurx/pulls/413) | 2026-09-23 | M1 only merged as `5a0475409`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`: engine attestation now runs off the runtime workers. The plan's one adversarial review ([comment 3669](http://192.168.4.7:3000/noirr/plurx/pulls/413#issuecomment-3669)) raised two findings, both confirmed, fixed and disposed in [comment 3737](http://192.168.4.7:3000/noirr/plurx/pulls/413#issuecomment-3737). The P1 is that the per-segment path M1 claimed to have cleared still made a synchronous `std::fs::metadata` on a runtime worker and was the *first* thing on it, because the boolean or short-circuits left to right ahead of both `spawn_blocking` batches at all five `recipe_engine_is_current` call sites - every producer launch and every `materialize`, for non-burn renditions as well as burn ones. The fix folds the encoder executable into the attestation batch instead of statting it inline ahead of it, and labels the four attestation series by what each batch stats, once per check; the P2 it answers is that a burn rendition's dependency-closure stats were charged to the font series while the media series recorded nothing for it, and that the stat count read three times the number of checks. Two pre-existing gate breaks the branch had carried were repaired in the same head. **M2 is deliberately unimplemented and its blocker is a design question, not missing evidence**: media1 links fontconfig, but the active root and user configs carry live discovery directives, so M2's literal byte-copy layout cannot be frozen; the plan's section 7 records the required correction and the decisions it needs. The plan is **not done** and has **no fleet evidence**: M1's media1 cold and warm observations have not been taken, and `ENGINE_ATTESTATION_BUCKETS_MS` starts at 100 ms so only `_sum / _count` will be readable when they are. That is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** Font attestation counts were zero on all four nodes after deploy; the text-burn cold/warm run is still owed. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-05 | [FFMPEG-SPAWN-UNIFICATION](../streaming/FFMPEG-SPAWN-UNIFICATION.md) | F-stream-12/13, §4.1 | month | merged: M1-M2 | gpt-5.6-sol | agent:/root/c02_builder | [PR #415](http://192.168.4.7:3000/noirr/plurx/pulls/415) | 2026-09-21 | M1 (one `producer_spawn` builder owning program, argv, env, descriptors, pipes, kill-on-drop and Job Object construction for rolling HLS, encoded VOD generation, head regeneration and progressive remux) and M2 (rolling copy and progressive remux sharing one closed FFmpeg progress-key classifier in `plurx_core::transcode::progress`) landed with the sole adversarial review folded in at `fe606774`, alongside executable low-FD, live-child reap and remux stderr-consumer coverage. M3 is **not** implemented: the deployed encoded-text-burn and progressive-remux environment checks, first/second start TTFF, and journal inspection for unexpected progress keys are **post-merge** evidence and are recorded later through the rule-4 evidence-only docs PR, so this plan is **not done**. The Windows cross-check remains an author-host toolchain blocker, not Windows compile or execution evidence. No feature gate or enablement setting. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-06 | [ENCODER-RATE-CONTROL-DEFAULTS](../streaming/ENCODER-RATE-CONTROL-DEFAULTS.md) | Q1 | month | merged: M1, M2, M5 | gpt-5.6-sol | agent:/root/p01_builder | [`plan/S-06` / #414](http://192.168.4.7:3000/noirr/plurx/pulls/414) | 2026-09-23 | M1, M2 and M5 merged as `c4b20d001`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`: per-family rate-control defaults and the balanced representative corpus. The plan's one adversarial review ([comment 3670](http://192.168.4.7:3000/noirr/plurx/pulls/414#issuecomment-3670)) raised three findings, all fixed and disposed in [comment 3748](http://192.168.4.7:3000/noirr/plurx/pulls/414#issuecomment-3748). Two were P1 and both falsified claims the plan and the PR body had made. M1 was **not** behaviour-neutral: it spelled an unset rate-control pair `requested:family_default` where the old code resolved it to `requested:bitrate`, moving a durable speculative-pretranscode dedupe key on every node - cancelling every queued row at deploy and flipping again mid-boot on every restart, permanently. The key spells `bitrate` again and an absolute generation literal is now pinned, shown to be the pre-refactor value by compiling the same assertion against main. And the pinned n2 corpus could not be regenerated at all: the disposition reproduced the finding and found it worse - **six of six** fixtures rather than two - because libx264 partitions frame threads by the host's core count, so `bitexact` now also implies `-threads 1`, `1080p-h264` and `grainy` gained `bitexact`, and the grain filter gained a seed; all six were then generated three times across two core counts, byte-identical, and re-pinned in the n1 and n2 manifests together. The P2 is closed by refusing an HDR10 source until `score_vmaf` tone-maps, because the scorer would otherwise compare SDR output against a PQ reference and pass both modes for the wrong reason rather than failing loudly. QSV and software remain Bitrate, no family default moved, and there is no universal maxrate cap. The plan is **not done** and has **no fleet evidence**: M3 and M4 need read-write n2 captures on reserved deployed nodes and **not one capture has been taken**. The re-pinned hashes are tied to the x264 build and are explicitly not claimed universal; re-pinning is a PR that states the build it used. That capture work is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-07 | [TONE-MAP-CHAIN-CORRECTIONS](../streaming/TONE-MAP-CHAIN-CORRECTIONS.md) | Q3 | month | merged: M0-M2 | gpt-5.6-sol | agent:/root/c02_builder | [PR #416](http://192.168.4.7:3000/noirr/plurx/pulls/416) | 2026-09-21 | M0's media1 measurement, M1's MaxCLL/MaxFALL/mastering-peak retention through probe, SQLite v65, Hiqlite v44 and the read-only DTO, and M2's source corrections (held-facts selection, plan digest v3, explicit peak before gamut conversion, error-diffusion dithering, peak and provenance in web, Apple and Android playback info) landed as the one plan PR; focused Rust, `web-policy`, Android and tvOS checks are green. M2 is not accepted: the lab4 10/50/90 captures, histograms and Harbor Lights real-title stills are post-merge evidence, as are M3's merged-image deployment to media1, before/after `/api/v1/system` pipeline verdicts and Apple TV/Chrome playback observations. All of that is recorded later through the rule-4 evidence-only docs PR, so this plan is not done. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-08 | [INTERLACE-IN-THE-MEDIA-CONTRACT](../streaming/INTERLACE-IN-THE-MEDIA-CONTRACT.md) | Q4, Q9 (bitrate half) | month | merged: M1-M4 | gpt-5.6-sol | agent:/root/s01_builder | [PR #417](http://192.168.4.7:3000/noirr/plurx/pulls/417) | 2026-09-21 | M1-M4 merged; the sole adversarial review's two P1 findings in [comment 3298](http://192.168.4.7:3000/noirr/plurx/pulls/417#issuecomment-3298) are answered in `9789ca4ed` and mapped to `rust-gate` evidence in `validation/regressions.d/9789ca4e-final-interlace-routes.toml`. The plan is **not done**: M5 still needs the plan's media1 QSV/VAAPI idet, signalstats and wall-time qualification, and hardware graphs stay declined. That outstanding qualification evidence is **post-merge** and gets recorded later through the rule-4 evidence-only docs PR, not on this branch. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-09 | [AUDIO-RESOLVED-INDEPENDENTLY](../streaming/AUDIO-RESOLVED-INDEPENDENTLY.md) | Q5 | month | merged: M1, M2 partial | gpt-5.6-sol | agent:/root/p01_builder | [PR #418](http://192.168.4.7:3000/noirr/plurx/pulls/418) | 2026-09-21 | M1 landed whole: bounded fail-closed `audio_sinks` and flat `achannels` claims, one v2/legacy profile translation, a route-aware `resolve_audio` that keeps decoder, sink, passthrough, and sample-rate authority separate, and independently serialized `delivered_audio` re-resolved once the playback method is known. M2 is partial: every lossy rolling, copy-conversion, and progressive remux AAC path pins `-ar 48000` and recipe v4 records `arate`, while copied audio stays untouched. Full M2 still needs normalized source channel layout and M4's measured per-layout matrices before downmix, manifests, offline packages, or prepared handoffs can enable surround output, and M3-M5 need Apple TV/AVR/AirPods, Android, and browser device evidence. All of that remaining evidence is post-merge and is recorded later through the rule-4 evidence-only docs PR, so this plan is not done. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-10 | [HONEST-MASTER-PLAYLIST](../streaming/HONEST-MASTER-PLAYLIST.md) | Q7, A11 | month | merged: M1-M2 | gpt-5.6-sol | agent:/root/p01_builder | [PR #419](http://192.168.4.7:3000/noirr/plurx/pulls/419) | 2026-09-21 | M1 rolling-rung geometry and M2 init-derived fMP4 AVC identity landed, together with the sole adversarial review's two corrections: encoded-VOD geometry now resolves width and height from one `output_size` decision, and AVC identity is read structurally from the selected track's sample descriptions rather than the first `avcC` byte match. This plan is **not done**: M3 per-family 23.976/29.97/59.94/60 SPS evidence, M4 named-device SDR `CODECS` re-qualification, M5 transcode plus copy/remux/index peak evidence and M6 Apple-panel before/after evidence are all **post-merge evidence** that can only be gathered on the fleet and on physical devices, and each is recorded later in this row and the plan's execution log through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-11 | [CODEC-AND-GPU-QUALIFICATION](../streaming/CODEC-AND-GPU-QUALIFICATION.md) | Q12, Q6, Q8 | quarter | blocked: M0 fleet evidence | gpt-5.6-sol | agent:/root/c02_builder | [`plan/S-11` / #422](http://192.168.4.7:3000/noirr/plurx/pulls/422) | 2026-09-21 | M0 counters and pre-instrumentation four-node inventory are implemented. Current fleet probe-refuses NVENC and has no VideoToolbox host, so M7/M8 do not exist for this snapshot. Deploy #422 and collect one reset-aware week on `nynuc`, `m6`, `nuc4`, and `nuc3`; direct final zeros after a restart are not absence evidence. **2026-09-24 evidence-only update:** A reset-aware four-node encoder counter baseline is recorded; the seven-day observation is still owed. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-12 | [VOD-BFRAMES-TIMELINE-DESIGN](../streaming/VOD-BFRAMES-TIMELINE-DESIGN.md) | Q2 | design | merged: D0-D3 | gpt-5.6-sol | agent:/root/p01_builder | [PR #423](http://192.168.4.7:3000/noirr/plurx/pulls/423) | 2026-09-21 | D0-D3 plus the sole adversarial finding merged from the one plan PR after a green fast lane. Option C stays deployed; the v2 oracle binds video-track timescale to the plan, refuses edit lists, and separates runtime-resolved facts from raw B0 box evidence. No Rust, recipe, setting, feature gate or enablement changed. **Not done:** B0-B4 are post-merge evidence - real ffmpeg 6/8 box capture, per-family quality/init/restart measurement, Paul's Option decision and client playback evidence - recorded later through the rule 4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-13 | [DECODE-FACTS-GATE-AND-FALLBACK](../streaming/DECODE-FACTS-GATE-AND-FALLBACK.md) | C13 | month | merged: M0-M2 | gpt-5.6-sol | agent:/root/s01_builder | [PR #424](http://192.168.4.7:3000/noirr/plurx/pulls/424) | 2026-09-23 | M0's five separately named decode-fact lookup phases and M1/M2's fallback identity work merged as `056312c48`: every `DecodeFactError` now maps exhaustively to a bounded reason, severity and counter, and a source that changed during probing is refused with the rescan instruction and counted as `refused_source_changed` rather than silently replanned from the catalogue row. The sole adversarial review ([comment 3333](http://192.168.4.7:3000/noirr/plurx/pulls/424#issuecomment-3333)) raised a P1 hit-only measurement gap and a P2 test-fidelity gap; both are disposed in [comment 3348](http://192.168.4.7:3000/noirr/plurx/pulls/424#issuecomment-3348) and mapped to `rust-gate` evidence in `validation/regressions.d/8432e9bc-decode-plan-fallback-identity.toml` and `0abf5615-decode-fallback-review-disposition.toml`. The plan is **not done**, and two limits are stated plainly because they are true: no latency value is asserted anywhere, only that the right series exist and are attributed to the right lookup outcome, and the `probe_changed` per-reason severity and once-per-process latch are read from the source with no test. M3 and M4 are **not implemented** and cannot open until M0's production-static-binary cold/warm measurement on media1 shows the gate wait exceeding the 10% threshold, and M1 still owes the lab2 deliberate ffprobe-change log and counter observation. No fleet or lab evidence exists for either today; both are **post-merge** evidence recorded later through the rule-4 evidence-only docs PR, and this instrumentation is what makes them measurable. No feature gate or enablement setting. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| S-14 | [TRANSCODE-DECOMPOSITION-PLAN](../streaming/TRANSCODE-DECOMPOSITION-PLAN.md) | §4.1, §4.2, §4.9 | quarter | merged: M0-M1, M2/T2 | gpt-5.6-sol | agent:/root/c02_builder | [PR #425](http://192.168.4.7:3000/noirr/plurx/pulls/425) | 2026-09-23 | M0, M1 and M2/T2 merged as `e444c53d3`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`: the baselines, the inline-test extraction, and the segment-index leaf move, all behaviour-preserving. The plan's one adversarial review ([comment 3350](http://192.168.4.7:3000/noirr/plurx/pulls/425#issuecomment-3350)) raised one P1 and one P2, both answered in [comment 3359](http://192.168.4.7:3000/noirr/plurx/pulls/425#issuecomment-3359). The P1 was that the identity gate could not prove the move it exists to guard: `scripts/split-identity` only concatenated whole piece files, so it placed T2's extracted region at EOF instead of its original location and exited 1, and no `OK` against base had ever been recorded for T2. It now expands a child at an explicit parent marker, reverses only the five declared relative-path relocations and strips exactly the structural `use super::*;` header and the 40 `pub(super)` additions, reporting `OK` byte-for-byte against base for all three parents - which also supplies the executable extraction proof M1 had lacked. The P2 corrected a stale exact-head test count of 2,435 to the reviewer-verified 2,454 byte-identical names on base and head. Integrating the wave together surfaced what nine serial merges would not have: six other plans in this wave had added tests inside the very `mod tests` block M1 deletes, so git presented a ~14.7k-line and a ~21.5k-line conflict, resolved as a real three-way merge of the test bodies and re-split onto this plan's existing chunk boundaries, with the function-name sets identical before and after - 581/581 for `hls` and 1064/1064 for `transcode`. The plan is **not done**: **T3-T7 and M3-M8 are not opened**, registry unification remains a separate evaluation, and no broad unit suite and no second adversarial review were run on the plan branch itself. The decomposition is behaviour-preserving by reassembly identity rather than by measurement, so the plan's own qualification remains **post-merge** work recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| K-01 | [CLUSTER-BACKUP-AND-RESTORE](../cluster/CLUSTER-BACKUP-AND-RESTORE.md) | §2.3, S4 | month | merged: M1-M3 | gpt-5.6-sol | agent:/root/s01_builder | [PR #426](http://192.168.4.7:3000/noirr/plurx/pulls/426) | 2026-09-23 | M1-M3 merged as `65103fa28`: the portable backup format with its fenced restore, the published recovery procedure, and the Developer-settings surface. Sole review #3366's six findings (four P1, two P2) are addressed and disposed in #3380, and the three corrective runtime commits are mapped to `rust-gate` evidence in `validation/regressions.d/`. Promotion also had to close things the review had not reached: `crates/plurxd/src/backup.rs` belonged to no functionality point and is now in `cluster.operations`; the licence notices attributed 14 crates the build no longer resolves after `1cdb1abe` dropped the s3/cryptr edge; `scripts/vendor-audit-lock` crashed on the resulting unused patch entry; `POST /api/v1/cluster/backups` had no route-attribution group, which K-04's table requires; the isolated `spikes/hiqlite-m0` lockfile was stranded by the new plurx-core dependencies; and the same dropped edge had silently been supplying the rustls crypto provider, so every Hiqlite reqwest client panicked until `213b3aea4` named `ring` explicitly. **Not done:** M4's container smoke against a qualified built image and M5's lab1-lab4 and NAS loss drills, physical restore and measured RPO/RTO are all unrun. The procedure is the deliverable and it is here; the drills prove it and cannot run against an unmerged branch. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| K-02 | [RAFT-SNAPSHOT-CADENCE-AND-CONSISTENT-CUT](../cluster/RAFT-SNAPSHOT-CADENCE-AND-CONSISTENT-CUT.md) | S2, S5 | month | merged: M0 | gpt-5.6-sol | agent:/root/p01_builder | [PR #427](http://192.168.4.7:3000/noirr/plurx/pulls/427) | 2026-09-23 | M0 only merged as `669ce22b8`: four fixed-label Raft state-machine size gauges sampled by the existing passive local-Raft tick with no Store or network access, plus the database byte count on direct node status and the Cluster page. No cadence, snapshot policy or consistent-cut behaviour changed on this branch. The plan's one adversarial review ([comment 3369](http://192.168.4.7:3000/noirr/plurx/pulls/427#issuecomment-3369)) returned **no findings** against that M0 boundary. The plan is **not done** and has **no fleet evidence whatsoever**: the mandatory every-voter 24-hour B/E/S/W readout was never collected and neither was A, the applied-index catch-up rate from one prepared follower restart. The attempt failed for access reasons, not effort — the executing host could not resolve the plan's `media1`/`lab1`-`lab3` aliases, SSH to the documented addresses for media1, lab3, lab4 and lab6 timed out, and the configured `billy` jump host could not reach their HTTP listeners; no voter was restarted, and a follower restart is an operational mutation needing explicit authorization. M1-M3 are therefore deliberately unimplemented: there is no evidence-backed `logs_until_snapshot`, no measured storage floor and no safe basis for changing the writer or copy path. That readout is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR; merging the gauges is what puts the sizes on `/metrics` so it becomes collectable at all. No feature gate or enablement setting. **2026-09-24 evidence-only update:** The four Raft size gauges and zero apply lag were sampled on all nodes; the 24-hour voter readout and approved follower restart are still owed. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| K-03 | [REPLICATED-WRITE-RATE-HYGIENE](../cluster/REPLICATED-WRITE-RATE-HYGIENE.md) | S3 | week | unclaimed | | | | 2026-09-20 | Keep the atomic claim |
| K-04 | [BOUNDED-REPLICA-READS-ROLLOUT](../cluster/BOUNDED-REPLICA-READS-ROLLOUT.md) | S1 | month | merged: M0-M1 | gpt-5.6-sol | agent:/root/c02_builder | [PR #428](http://192.168.4.7:3000/noirr/plurx/pulls/428) | 2026-09-23 | M0 and M1 merged as `5b8f3c79e`: store reads attributed to HTTP routes through an exact registered-`MatchedPath` table that fails closed, with `other` reserved for the genuinely unknown, recorded through a Tokio request scope and the production `StoreOperationTimer`/`TimedClient` completion path rather than either counter being written directly; and search named explicitly as a `NodeLocal` catalogue read, with the handler inventory widened to an exact per-function call multiset across photos, images, reading, DVR and library channels while Authority pre-mutation and ownership reads stay individually named. The sole adversarial review ([comment 3344](http://192.168.4.7:3000/noirr/plurx/pulls/428#issuecomment-3344)) raised three findings — two P1 (substring route classification mixing playback into `other`, and an M1 inventory not closed over handlers or calls) and one P2 (`Fenced` and every role-lookup error labelled `voter`) — all fixed at `94093a1fe` and disposed in [comment 3356](http://192.168.4.7:3000/noirr/plurx/pulls/428#issuecomment-3356); the bounded role vocabulary now carries `fenced` and `unknown` explicitly. The plan is **not done**. The §5.1 lab1-lab3 per-route readout has not been taken, so none of this is yet evidence for widening anything. M2 is blocked on more than time: it needs the acknowledged Raft log index from the write response, and the vendored `Client::execute` exposes only rows affected — deriving a fence from a later quorum sample would cost a second round trip and would not be the write-response contract M2 specifies, so this is a real client-library limitation to resolve, not merely uncollected evidence. M3 sits behind M2 and M4 behind all of them. `bounded_replica_reads` stays `false`, the lag budget stays 64, and no enablement gate or ordinary-auth cache was added. The readout is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| K-05 | [SQLITE-READ-PATH-AND-QUERY-PLANS](../cluster/SQLITE-READ-PATH-AND-QUERY-PLANS.md) | S6, S7, S11 | month | merged: M0 partial | gpt-5.6-sol | agent:/root/c02_builder | [PR #429](http://192.168.4.7:3000/noirr/plurx/pulls/429) | 2026-09-23 | Only M0's catalogue fixture and its generator merged as `30d422727`; **no query-path or read-path code landed at all**, and M1-M7 did not start, per §§3.3-3.4. Committed generator `d87861a1a` builds the semantic standalone SQLite fixture — 4 libraries, 20,000 movies, 500 shows × 8 seasons × 12 episodes, Home with 100 folders, 1,000 videos and 1,000 photos, 1,000 recordings, 100,000 semantically owned files with 5–30 KiB probe JSON, 5 users and 35,000 playable-leaf watch rows — measured at 1,870,753,792 bytes generated in 18.34 s from the warm release build, with an executable census proving all four top-level Home rails, the non-empty Recordings exclusion and zero invalid file or watch owners. The sole adversarial review ([comment 3351](http://192.168.4.7:3000/noirr/plurx/pulls/429#issuecomment-3351)) raised two P1 findings, both addressed at `b8b1ff57d` and disposed in [comment 3363](http://192.168.4.7:3000/noirr/plurx/pulls/429#issuecomment-3363). **M0 itself is unfinished, and what remains is work rather than only evidence**: the same population still has to be loaded into a Hiqlite state-machine database, and the verbatim `EXPLAIN QUERY PLAN` output, the cold and warm medians on both backends, and §3.3's 32 × 256 writer-contention matrix are all still uncollected — the 20% threshold and that load shape remain proposals. The database is disposable and uncommitted; `cargo run -p plurx-core --example catalogue_fixture --release -- <out.db>` regenerates it, because the plan's requested `scripts/bench/catalogue-fixture` path cannot exist (`scripts/bench` is the tracked playback benchmark file). |
| K-06 | [CLOCK-SKEW-GUARD-DESIGN](../cluster/CLOCK-SKEW-GUARD-DESIGN.md) | S9 | design | merged: M0-M4 | gpt-5.6-sol | agent:/root/p01_builder | [PR #430](http://192.168.4.7:3000/noirr/plurx/pulls/430) | 2026-09-21 | M0-M4 and the sole-review disposition merged as `02c7760e`: wall/monotonic generation rechecks close local/common-mode step races; admission is distinct from fenced target removal; RTT filtering has a floor and time expiry. **Not done:** runtime and fleet skew measurement is post-merge evidence, recorded later through the rule 4 evidence-only docs PR. **2026-09-24 evidence-only update:** All four hosts reported NTP synchronized; clock-route metrics are absent, so the offset/uncertainty run still needs the measurement release. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| K-07 | [STORE-CONTRACT-COVERAGE-AND-PLACEHOLDER-VALIDATION](../cluster/STORE-CONTRACT-COVERAGE-AND-PLACEHOLDER-VALIDATION.md) | S10 | week | merged: M0-M4 | gpt-5.6-sol | agent:/root/c02_builder | [PR #411](http://192.168.4.7:3000/noirr/plurx/pulls/411) | 2026-09-21 | M0-M4 merged as `58e75260` after the sole adversarial review and green fast lane. The semantic 42-site discard guard and actual SQLite/three-voter no-holder contract are live; first-ten-post-merge lane-cost evidence remains pending. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| K-08 | [HIQLITE-FORK-AND-DEPENDENCY-CLEANUP](../cluster/HIQLITE-FORK-AND-DEPENDENCY-CLEANUP.md) | §4.3 | month | merged: M1-M2 | gpt-5.6-sol | agent:/root/s01_builder | [PR #432](http://192.168.4.7:3000/noirr/plurx/pulls/432) | 2026-09-23 | M1 and the safe M2 boundary merged as `e42efc67b`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`. The plan's one adversarial review ([comment 3365](http://192.168.4.7:3000/noirr/plurx/pulls/432#issuecomment-3365)) raised a single P1 and it was correct: deleting the root `s3-simple` patch left the advertised `backup`/`s3` feature resolving registry `s3-simple 0.8.0`, which is both the `quick-xml 0.39.4` named by RUSTSEC-2026-0194 and RUSTSEC-2026-0195 and the `aws-lc-sys 0.39.1` that `deny.toml` had just banned. It is fixed and disposed in [comment 3697](http://192.168.4.7:3000/noirr/plurx/pulls/432#issuecomment-3697), where reading the graph made the mechanism sharper than the finding: upstream s3-simple 0.9.x already fixes both, but cryptr 0.10.0's `^0.8.0` pin cannot select it, so the ban and the advisory are one edge rather than two. `vendor/s3-simple` is therefore **retained rather than deleted**, carrying two dependency-only patches that mirror what upstream 0.9.0 did to its own manifest - `quick-xml` raised to 0.41 and the four unreferenced `aws-lc-rs`, `aws-lc-sys`, `quinn` and `quinn-proto` edges dropped - and the override lives in `vendor/hiqlite/Cargo.toml`'s own `[patch.crates-io]` as ledger patch 17 rather than at the workspace root, where it would be an unused patch and evidence of nothing; `deny.toml`'s `aws-lc-sys =0.39.1` ban is the tripwire if the root ever enables the feature. `ForkBackupGraphPolicyCase` judges that graph against `deny.toml` itself, which `cargo deny` cannot do because it only ever resolves the workspace graph - the gap that let the regression through. The plan is **not done** and has **no lab or upstream evidence**: M0 timings, M3's provider assertion and three-voter transport proof, M4 corpus equivalence and the conditional M5 are unrun, and M6's public upstream links are still spelled `pending M6` - no issue or PR was filed and no URL was invented. `cargo audit` is not installed on the build host, so no advisory scan was executed and the operations contract is what covers that graph. All of it is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-01 | [HTTP-LISTENER-TIMEOUTS-AND-ASSET-DELIVERY](../server/HTTP-LISTENER-TIMEOUTS-AND-ASSET-DELIVERY.md) | §2.5, W1, W2, W5, W6 | week | merged: M1-M3 | gpt-5.6-sol | agent:/root/c01_builder | [PR #395](http://192.168.4.7:3000/noirr/plurx/pulls/395) | 2026-09-20 | One plan PR merged as `79113254`; §6.2–§6.4 lab soak, HAR/waterfall, and device-reader evidence remains pending, so this row is not done. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-02 | [SCAN-AND-ENRICHMENT-HYGIENE](../server/SCAN-AND-ENRICHMENT-HYGIENE.md) | C3, C5 | week | merged: M1-M3 | gpt-5.6-sol | agent:/root/c02_builder | [PR #400](http://192.168.4.7:3000/noirr/plurx/pulls/400) | 2026-09-21 | M1-M3 merged as `665b8b5c` after the sole adversarial review and unchanged fast-lane retry were green. Fleet NAS timing and provider-drop acceptance remain post-merge. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-03 | [IMAGE-SERVING-AND-DERIVATIVES](../server/IMAGE-SERVING-AND-DERIVATIVES.md) | C6 | month | merged: M1-M2 | gpt-5.6-sol | agent:/root/p01_builder | [PR #431](http://192.168.4.7:3000/noirr/plurx/pulls/431) | 2026-09-23 | M1 and M2 merged as `24a23444b`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`: verified artwork digests and bounded size-parameter derivatives. The plan's one adversarial review ([comment 3668](http://192.168.4.7:3000/noirr/plurx/pulls/431#issuecomment-3668)) raised nine findings, five P1 and four P2, every one accepted and fixed in [comment 3709](http://192.168.4.7:3000/noirr/plurx/pulls/431#issuecomment-3709) with a quoted revert failure for each, none deferred or disputed. They were not cosmetic. The derived-orphan sweep compared a 32-character derivative prefix against a 16-character live-set entry, so **every backdrop derivative was classified an orphan and unlinked every 24 hours, forever**, rebuilding the store from scratch daily. A fallback-to-original was served under the derivative URL as `private, max-age=604800, immutable`, which on a cold grid load is nearly every request, so the derivative was generated on the server and never asked for again. The post-generation source fence read byte-budget exhaustion as a changed source, deleting correct derivatives while double-drawing the budget for a file the caller already held. Every derivative request read and admitted the full original, so a warm hit on a 4 MiB backdrop did more work than serving the original. And one legacy filename disabled the whole sweep process-wide after every restart. **A recorded deviation from the plan's section 3.2:** a derivative is now named `{source}-{bucket}-{digest32}.{ext}` rather than `{digest32}-{bucket}.{ext}`. Recording the source filename is what makes the per-source retention the plan specifies possible at all - with only a digest, one unresolved source makes every unmatched prefix ambiguous, which is why the original code could only abort - and the earlier layout is recognised and reclaimed after the grace so a node that ran a pre-review build does not keep those forever. `BACKDROP_SIZE`/`STILL_SIZE` stay `original`, `crates/plurx-core/src/metadata/mod.rs` is untouched, and no feature gate was added. The plan is **not done** and has **no lab, browser or device evidence**: sections 6.2-6.4's lab1 concurrency, CPU and 503 measurement, the Chrome cold and warm byte comparison, and the Apple TV and Android TV browsing observations are all ungathered, **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-04 | [AUTH-HARDENING](../server/AUTH-HARDENING.md) | C7, C8 | month | merged: M1-M3, M4 decision | gpt-5.6-sol | agent:/root/s01_builder | [PR #433](http://192.168.4.7:3000/noirr/plurx/pulls/433) | 2026-09-23 | M1-M3 and M4's recorded decision merged as `5b694ac54`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`. The plan's one adversarial review ([comment 3378](http://192.168.4.7:3000/noirr/plurx/pulls/433#issuecomment-3378)) raised one P1 and one P2, both fixed and disposed in [comment 3691](http://192.168.4.7:3000/noirr/plurx/pulls/433#issuecomment-3691). The device label was an unconstrained string an authenticated account could use to turn one devices GET into hundreds of MiB while clustered login replicated every oversized value; it is now `MAX_DEVICE_LABEL_BYTES = 256` enforced at login admission, with an 8 KiB `/auth/login` body cap so a megabyte body is refused before Argon2, at both Store writes, and - for pre-bound history - in both projections in SQL, truncated on a character boundary rather than omitted, because a device the user cannot see is one they cannot revoke. The revocation queue was one slot shallower than its stated contract because the active operation kept a waiting permit; the permit is released the moment the mutex is acquired, so eight waiters really queue. Three further defects the branch's focused runs had missed were fixed in the same head: a replicated placeholder introduced out of order, which hiqlite refuses before any I/O and would have made the devices route answer a database error on every cluster; an `unchecked_transaction` registered in no transaction site; and four device routes with no route-attribution group, so every Store read they made was attributed to `other`. **M4 is a recorded decision, not an implementation: device tokens do not expire today**, retained until client recovery is qualified, with the device inventory and revocation as the compensating control. Stated plainly: neither concurrency test distinguishes the off-by-one - only the `extract` test does - and no end-to-end eighth-waiter test was written rather than a flaky one. The plan is **not done** and has **no fleet evidence**: three-node simultaneous logout fence timing, failure injection, the trusted-proxy deployment observation and real-client revocation recovery are all ungathered, **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **Added 2026-09-24 by claude-opus-5-5 (https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK), not this row's owner, to note a superseding change:** on Paul's 2026-09-23 request (*"can't we add an option to make them expire or not expire? Then we can have both and just make it default to expiring."*), draft [PR #486](http://192.168.4.7:3000/noirr/plurx/pulls/486) supersedes M4's non-expiring decision with a server-wide "Sign-ins expire" option, on by default, 90 idle days on a sliding window (plan §3.4.1). Until #486 merges, device tokens still do not expire on `main`; once it merges they expire by default. Its device evidence is the plan's §6.4 GPT prompt, post-merge. **Merged 2026-09-24:** #486 (exact head `52b18c101`) was carried into main by integration [PR #489](http://192.168.4.7:3000/noirr/plurx/pulls/489) at `995b60f3e` (its merge commit `0a7c6f52a`) after its one adversarial review ([comment 4254](http://192.168.4.7:3000/noirr/plurx/pulls/486#issuecomment-4254)) and disposition ([comment 4270](http://192.168.4.7:3000/noirr/plurx/pulls/486#issuecomment-4270)). Device sign-ins on `main` now expire by default after 90 idle days, and the "Sign-ins expire" option in Settings → Users turns that off. The §6.4 device evidence is still owed, post-merge. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-05 | [DETAIL-READS-AND-STORAGE-AVAILABILITY](../server/DETAIL-READS-AND-STORAGE-AVAILABILITY.md) | C14 | month | merged: M1 | gpt-5.6-sol | agent:/root/p01_builder | [PR #435](http://192.168.4.7:3000/noirr/plurx/pulls/435) | 2026-09-23 | M1 only merged as `300f55df9`, carrying the exact deployable boundary `33e66cfd1`: the `validated_revision` publication marker, the bounded node-local backfill, refusal-without-deletion, and the fixed-cardinality convergence metric. The migration was **renumbered from the drafted v64 to v66** when the branch met main — S-08's `FILES_FIELD_ORDER_COLUMN` took v64 and S-07's `FILES_LUMINANCE_COLUMNS_BATCH` took v65 first — so `FRAGMENT_INDEXES_VALIDATION_COLUMN` appends after both, the list stays append-only with no earlier entry moved, `SQLITE_SCHEMA_VERSION` derives 66, and the sidecar is v10. The plan's one adversarial review ([comment 3377](http://192.168.4.7:3000/noirr/plurx/pulls/435#issuecomment-3377)) returned **no findings**. The plan is **not done**: M2 (the batched detail projection) and M3 (the availability cache) are deliberately unimplemented, and M2 is not even eligible for promotion, until this exact M1 boundary is deployed and its sidecar backfill-convergence receipt exists. **No such receipt has been collected** — there is no fleet or device evidence for this row today. That receipt is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR; merging M1 is its precondition, not something it waits on. Cost and backfill stay node-local in the sidecar, never Raft. No feature gate or enablement setting. **2026-09-24 evidence-only update:** All three backfill-result counters were zero on all four nodes; an active-library convergence receipt is still owed. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-06 | [TELEMETRY-BACKPRESSURE](../server/TELEMETRY-BACKPRESSURE.md) | C15 | month | merged: M1-M4 | gpt-5.6-sol | agent:/root/c02_builder | [PR #434](http://192.168.4.7:3000/noirr/plurx/pulls/434) | 2026-09-23 | M1-M4 merged as `a7c6a58d0`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`. The plan's one adversarial review ([comment 3381](http://192.168.4.7:3000/noirr/plurx/pulls/434#issuecomment-3381)) raised three P1 findings, all fixed and disposed in [comment 3682](http://192.168.4.7:3000/noirr/plurx/pulls/434#issuecomment-3682). The opted-in prior writes were not batched - a 64-job batch with priors on cost 65 `NodeLocalTelemetry::with_conn` acquisitions on the sidecar's one mutex, preserving exactly the per-event contention C15 exists to remove - and are now one `record_playback_batch` in one transaction, with the connection counting its own leases so the claim is measured rather than asserted; the two opt-ins stay independent. The terminal reserve was a non-atomic capacity snapshot that could refuse a terminal while 896 samples sat ahead of it, and is now a bounded deque whose admission decision happens inside the queue's own lock and which displaces the oldest non-terminal for an arriving terminal, occupancy never above 1,024. And loss accounting was not fail-closed across writer panic or shutdown timeout; a batch now moves into an in-flight slot taken exactly once, by whoever destroys or drains it. This added a fifth fixed drop reason, `writer_panic`, **deviating from the plan's four** because those four are deliberate discards and this one is not; cardinality stays fixed and the plan's section 3.4 carries the reason. One defect outside the findings was found running the gate and fixed in the same head: `emit` deadlocked against any Store `boot()` had not registered, which hung the whole `plurxd` binary target at the reviewed head. Two limits are recorded because they are true - the concurrent-producer test still passed with the check-then-act shape restored, so it is an invariant guard rather than failing-without-fix evidence, and the `hiqlite-3-voter` arm of the store contract is behind a feature that was not enabled, so the replicated arm of the new batch method is unproven. The plan is **not done** and has **no fleet evidence**: the plan's section 6.3 injected slow-sidecar observation during real playback and the three restart timings have not been taken; they are **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-07 | [PLEX-FACADE-PAGING](../server/PLEX-FACADE-PAGING.md) | C4, C9 | month | merged: M0; M1-M3 not built per §8.3, M5-M6 unstarted | claude-opus-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #462](http://192.168.4.7:3000/noirr/plurx/pulls/462) | 2026-09-24 | M0 merged as `a6490a276`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: the census, run read-only on nuc4, and its measure-only instrument. The census could not be completed as §6.3 specifies because façade traffic is invisible by construction (no access log, no metric separating the façade from the native API, and `plex::part` writes no playback telemetry); what evidence exists shows no Plex-family client in 29 days of `playback_events` and zero user-facing route counts on an idle node. **Recommendation (§8.3): do not build M1-M3, and they were not built.** Built instead: `plurx_plex_requests_total{handler,outcome}`, 14 handlers by 4 outcomes including `unauthorized`, over the façade sub-router only, with no response byte moved. The plan's one adversarial review ([comment 4096](http://192.168.4.7:3000/noirr/plurx/pulls/462#issuecomment-4096)) raised two P2 findings, both fixed and disposed in [comment 4138](http://192.168.4.7:3000/noirr/plurx/pulls/462#issuecomment-4138). The load-bearing one is that the census counter was a process-wide `static`, so the exact-delta census tests raced every other test sending façade traffic; it is now a `PlexCensus` held on `AppState`, pinned by a two-router test that failed 400/400 with the global restored and 0/400 fixed. The other rewrote §8.5's closing rule: one read covers one node since its last restart, so the row closes only on seven gap-free days read from **every** node with its uptime, and a gap makes the week inconclusive rather than zero. The plan is **not done**: those seven days of reads are **post-merge** evidence recorded later through the rule-4 evidence-only docs PR, and **M5/M6, the C9 appendix, remain unstarted** - unblocked and independent of the census, but §6.4's protocol needs four concurrent real playbacks with seeks on the ingress node. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| C-08 | [OBSERVABILITY-BASELINE](../server/OBSERVABILITY-BASELINE.md) | C10, §4.9 | month | merged: M1-M4; M5 unstarted | claude-opus-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #461](http://192.168.4.7:3000/noirr/plurx/pulls/461) | 2026-09-24 | M1-M4 merged as `7218b96ce`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: RED families on the route group from a layer outside `cluster_capacity_gate` (so fenced, learner and maintenance 503s are counted), `x-request-id` adoption, `PLURX_LOG_FORMAT` with ANSI off for non-terminals, and a chaining panic hook that redacts payloads and counts into a fixed subsystem allowlist, with the three deviations from the plan as written recorded in its §7.6. The plan's one adversarial review ([comment 4099](http://192.168.4.7:3000/noirr/plurx/pulls/461#issuecomment-4099)) raised one P1 and one P2, both fixed and disposed in [comment 4153](http://192.168.4.7:3000/noirr/plurx/pulls/461#issuecomment-4153). The load-bearing P1 is that `plurx_http_bodies_total` counted every fully delivered fixed-length body - every direct-play range and HLS segment - as `aborted`, because hyper drops a `Content-Length` body unpolled once it has written the declared bytes; the measured body now records the declared length and counts `complete` at it, pinned over a real connection for chunked, fixed-length and HEAD responses. The P2 is that the panic hook's recursion guard could never fire (std aborts on a panic inside a hook before anything unwinds), so the guard is gone and the reporting path is panic-free by construction, pinned by child-process abort and hostile-payload tests. Integration carried the `/metrics` `format!` to 16 placeholders for 16 arguments. The plan is **not done**: **M5 (the release-evidence set) is unstarted** by design (§7.6), and there is **no fleet evidence whatsoever** - nothing has been deployed or scraped, so §6.3's exposition-size, label-hygiene, journald-ANSI, JSON-mode, request-id and RED-sanity steps are all **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. No feature gate and no enablement setting. **2026-09-24 evidence-only update:** Short scrape, request-ID and ANSI observations are recorded; the one-hour, JSON-mode and active-flow checks remain. The literal label grep matches metric names containing `session`. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| L-01 | [DVR-SCHEDULER-GUIDE-VIEW-AND-SINK-ISOLATION](../features/DVR-SCHEDULER-GUIDE-VIEW-AND-SINK-ISOLATION.md) | L1, L10 | week (L1) / month (L10) | merged: M1-M3 | gpt-5.6-sol | agent:/root/s01_builder | [PR #407](http://192.168.4.7:3000/noirr/plurx/pulls/407) | 2026-09-21 | M1 immutable owner-side full-guide views (`9a4b83af`), M2 bounded per-sink queues with owned writers and attempt-fenced finalization (`46a1aeec`, `f2127e1a`, `6837d56b`), and M3 operations/status/plan contracts are implemented on `plan/L-01`; every corrective runtime commit now maps to its `rust-gate` evidence in `validation/regressions.d/`. The remaining evidence is post-merge and is recorded later through the rule-4 evidence-only docs PR: the media1 336-hour guide versus 2 MiB public clip comparison, the mixed NAS/local sink-interruption run with metric, attempt and reader-gap numbers, and broad unit qualification after P-01. This plan is not done. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| L-02 | [LIVE-TV-SESSION-FENCE-PEER-TRANSPORT-AND-START](../features/LIVE-TV-SESSION-FENCE-PEER-TRANSPORT-AND-START.md) | L2, L3, L6, L9 | week (L3) / month | merged: M1-M2; M3-M4 unstarted | claude-opus-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #455](http://192.168.4.7:3000/noirr/plurx/pulls/455) | 2026-09-24 | M1 (L2) and M2 (L3) merged as `28faf3a6b`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: one process-lifetime `PeerTransport` with a five-second per-owner address memory that every `PeerTransportError` and internal-path 404 invalidates, and one validated settings observation per node that every live session's per-second fence validates against, graced for `FENCE_GRACE_MAX_AGE` = 10 s (Paul's decision of 2026-09-23) and then ending the session with a sentence naming the replicated settings rather than the tuner. The start and pre-publication fences keep their direct, fail-closed read. The plan's one adversarial review ([comment 4100](http://192.168.4.7:3000/noirr/plurx/pulls/455#issuecomment-4100)) raised two P2 findings, both fixed and disposed in [comment 4157](http://192.168.4.7:3000/noirr/plurx/pulls/455#issuecomment-4157). The load-bearing one is that no test had ever made a settings read fail, so the keep-the-last-observation arm of the fence was unpinned and the grace test's assertion was vacuous; a `#[cfg(test)]` failure seam in `settings_snapshot` now drives a real failing observer through grace to `owner_unavailable`, proved against the reviewer's clearing mutant, and `plurx_live_tv_settings_reads_total` gained a `site` label (`observer`, `fence`, `request`) so the fence's read rate is observable. The other corrected two comments that said the opposite of the code, including what `OWNER_PEER_TTL` bounds. `make live-tv-two-node-check` passed 4/4 on nuc3 without root, which removes the harness reason M3 and M4 were left for. One recorded departure stands: the observation comes from the existing 1 Hz Live TV settings loop rather than a second observer that parks, so "zero reads when nothing is live" is **not** implemented. The plan is **not done**: **M3 (L6 warm start) and M4 (L9, the orphan owner that leaves a failed cleanup counted against `MAX_TERMINAL_TOMBSTONES` for ever) are unimplemented**, and none of §6.3's three GPT fleet prompts has run, so the ≈6 s read budget the 10 s grace is sized against is still a budget rather than a measurement - **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. No feature gate or enablement setting. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| L-03 | [LIVE-TV-SHARED-TRANSPORT](../features/LIVE-TV-SHARED-TRANSPORT.md) | L4, Q9 (captions) | quarter | merged: design re-verification, M1, M3; M2 and M4 not started | claude-opus-5-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [`plan/L-03` / PR #482](http://192.168.4.7:3000/noirr/plurx/pulls/482) via integration [PR #493](http://192.168.4.7:3000/noirr/plurx/pulls/493) | 2026-09-24 | Merged as `2cf1e6b27`, one of the two heads (exact head `e036e452c`) carried into main by integration [PR #493](http://192.168.4.7:3000/noirr/plurx/pulls/493) at `b24b00410`: the design re-verification (plan §2.4, `3cb3951a8`), **M3** (caption fixture, decoder, per-graph audit, `ee4a1752c`), **M1** (shared transport, `5cc42346a`) and the review round (`b3c0b14b2`). **§2.4 D7 decided: option (b)** — a viewer joins on channel, device id and address and current serving generation, not configuration generation; taken by the coordinating session on Paul's behalf and recorded in the plan as a decision Paul can overturn. M1 builds the corrected design (D1-D6, D8): the transport now probes (the DVR transport never had, so `DvrTransport.source` was always `None`), admission reserves in one lock hold and never overwrites an entry, the DVR tick and DVR-disabled path no longer close transports viewers hold. The plan's one adversarial review ([comment 4272](http://192.168.4.7:3000/noirr/plurx/pulls/482#issuecomment-4272)) was disposed in [comment 4356](http://192.168.4.7:3000/noirr/plurx/pulls/482#issuecomment-4356) with revert-proven tests. The load-bearing P1: the single probe taken when a recording started decided every viewer of that channel for the whole recording, so one failed or stale probe refused every later joiner even with a tuner free. A transport's facts are now refreshable (a viewer that finds them failed, stale or older than 60 s re-probes the live edge, and a failed re-probe fails only that viewer), and a viewers-only transport whose opening probe fails ends so the next start opens a fresh GET. `make live-tv-two-node-check` 5/5 on nuc3 in `unshare -rnm` on the integration head, including `two_viewers_on_one_channel_open_one_tuner_get` (fixture `opens == 1`). Caption audit on `nuc3`: jellyfin-ffmpeg 8.1.2 preserves CC1 and 708 for x264, QSV, VA-API and copy; the distro FFmpeg 5.1.9 fallback does not (C6). **What remains:** M2 (client UI for the `watchable` rows; its acceptance is a device pass) and M4 (needs prompts A and C and the C6 per-build probe). **Owed, post-merge:** §6.3's shared-transport device prompt and the M3 prompts A-C in the execution log. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). **GPT 2026-09-25:** M2 capacity offers are implemented on draft #506 for web, Apple and Android with one added shared fixture row. The browser offers a direct channel action; native clients name the watchable channel in the refusal. Build and device acceptance remain due. M4 still awaits real broadcast service IDs, client baseline and a per-node/per-build caption probe. |
| W-01 | [WEB-PLAYER-RECOVERY-AND-LOCAL-SEEK](../clients/WEB-PLAYER-RECOVERY-AND-LOCAL-SEEK.md) | Q10, W3, W7 | week (worker, reporter) / month (seek) | merged: M1-M4 | gpt-5.6-sol | agent:/root/c02_builder | [PR #408](http://192.168.4.7:3000/noirr/plurx/pulls/408) | 2026-09-21 | M1-M4 landed on `plan/W-01`: the hls.js worker at both construction sites with a CSP-blocked inline fallback proof (`b50c4816`), one fenced decoder rescue per attach on the shared retry budget (`3d63b5ac`), bounded early error reporting plus the capture-phase missing-resource reports from the sole adversarial P1 (`3362df6a`, `054ac458`), and covered rolling/progressive seeks routed locally with one fenced reopen at the same target (`aadf9c1c`); each is now mapped to its current check in `validation/regressions.d`. The progressive-remux browser/device matrix and the post-deploy fleet event-rate and journal observation are post-merge evidence, recorded later through the rule-4 evidence-only docs PR, so this plan is NOT done. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| W-02 | [WEB-TYPE-CHECKING-AND-PLAYER-DECOMPOSITION](../clients/WEB-TYPE-CHECKING-AND-PLAYER-DECOMPOSITION.md) | W8, W9 | month | in-progress: 5.4-5.5 implementation on draft #506; 5.1-5.3 blocked on §7 Q1 | claude-opus-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #459](http://192.168.4.7:3000/noirr/plurx/pulls/459) | 2026-09-24 | M0 (unplanned), 5.6 and 5.7 merged as `6975b3d99`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`. M0 turned `make web-check` green on `main` (it is not in the required fast lane, which is how it stayed red); 5.6 extends the library grid only where the view owns the order, adds `decoding="async"` and one shared reduced-motion rule; 5.7 bounds rather than suppresses the paused progress repeat, because `trakt.rs`'s `sweep_loop` removes a session quiet for 150 s and a removed session can never scrobble its stop, and adds MediaSession and television back-key handling inside the `player-input-adapter` region. The plan's one adversarial review ([comment 4106](http://192.168.4.7:3000/noirr/plurx/pulls/459#issuecomment-4106)) raised one P1 and four P2 findings, all fixed and disposed in [comment 4145](http://192.168.4.7:3000/noirr/plurx/pulls/459#issuecomment-4145). The load-bearing P1 is that the MediaSession `play`/`pause` handlers judged `video.paused` rather than the viewer's intent, so a headset Play during a reattach or cold open paused a film the viewer wanted playing; they now read intent the way `togglePlay` flips it and route through `watchRouteInput` like the keys, so a blocking prompt ignores them, and Next is offered only while autoplay-next is on and the title is not known to be other than an episode. The plan is **not done**: **5.1-5.3 are blocked on the plan's §7 open question 1** - `tools/web-types/package.json` versus a vendored `tsc`, that is, whether an npm directory may exist in this repository at all - which is Paul's decision; 5.4 and 5.5 (the `attachHls` and `play()` splits) are unstarted because both need the Playwright browser checks and the 5.1-5.3 type baseline. There is no browser, lock-screen, headset or television evidence, and the plan's television prompt for GPT is unrun - **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** Playback-lab readiness fix `2a6fbc855` enabled a Chrome trace; 5.4-5.5 splits, type baseline and physical browser-input checks remain. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). **GPT 23:53 UTC:** `8907b9cf5` splits `attachHls`; `cb7042ea2` and `858c8bc01` split `play` while preserving direct attach timing. JS syntax passed. Type baseline and Playwright browser acceptance remain unrun, so neither milestone is accepted. |
| A-01 | [APPLE-DISPLAY-CRITERIA-AND-AUDIO-SESSION](../clients/APPLE-DISPLAY-CRITERIA-AND-AUDIO-SESSION.md) | §2.8, §2.10 | week | merged: M1-M2, M3 policy code | gpt-5.6-sol | agent:/root/c02_builder | [PR #406](http://192.168.4.7:3000/noirr/plurx/pulls/406) | 2026-09-23 | M1 and M2 whole and M3's policy code merged as `c0b280ecb`, one of the nine plan heads carried into main by integration [PR #454](http://192.168.4.7:3000/noirr/plurx/pulls/454) at `d74ac817a`; there is no second display-criteria owner and no feature gate or readiness-conditioned control in the diff. The plan's one adversarial review ([comment 3154](http://192.168.4.7:3000/noirr/plurx/pulls/406#issuecomment-3154)) raised four findings, three P1 and one P2 - an interruption ending without `.shouldResume` left all three stacks where one Play could not resume, Live TV route loss made an attached session unresumable through its own transport control, Live TV applied display criteria before the item was ready and never retried, and the surface fixture had quietly weakened the retirement-ownership contract - answered in [comment 3162](http://192.168.4.7:3000/noirr/plurx/pulls/406#issuecomment-3162) and confirmed in [comment 3265](http://192.168.4.7:3000/noirr/plurx/pulls/406#issuecomment-3265). Promotion fixed one more defect, this branch's own and present since M2: the published-surface inventory pin failed because M2 had added `systemPaused` to it, and the fix stops publishing the flag rather than widening the pin, since every transition is already announced through the one surface. `c0b280ecb` claims **Apple build 178** because the release-claim lane requires `CURRENT_PROJECT_VERSION` to rise above the merge target and main carried 177; it is a version claim generated by `validation.apple_build`, not a built or tested binary, and no `docs/apple-builds/` note was invented for it. The plan is **not done** and has **no device evidence at all**: the plan's section 6 Apple TV HDMI mode-switching matrix and iPhone interruption/route matrix are ungathered, and the Swift tests named in the client-fix anchors were read rather than executed because no Xcode host was available to the promoting session. Both are **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| A-02 | [APPLE-PLAYER-CONTROLLER-ATTEMPT-AND-OBSERVATION](../clients/APPLE-PLAYER-CONTROLLER-ATTEMPT-AND-OBSERVATION.md) | A3, A4, A6, A7 | month | in-progress | gpt-6 | none:openai:2026-09-24 | [draft PR #506](http://192.168.4.7:3000/noirr/plurx/pulls/506) | 2026-09-24 | After A-01, which merged. Part of milestone 5.1 merged as `ea843583d`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: `Attempt`, `Attempt.Scope`, `snapshotAttempt()`, four multi-counter continuation fences migrated to scoped staleness, and the `ContinuationScopeCensus` as a Python audit under `make operations-check`. The plan's one adversarial review ([comment 4101](http://192.168.4.7:3000/noirr/plurx/pulls/464#issuecomment-4101)) raised two P2 findings, both fixed and disposed in [comment 4144](http://192.168.4.7:3000/noirr/plurx/pulls/464#issuecomment-4144). The load-bearing one is that nothing pinned a migrated fence's scope set: the reviewer swapped all four to unrelated scopes and every Swift test and the census still passed. Each set now lives once as an `AttemptFence` case, the census compares a `[fences]` table with the cases, their scopes and their call sites in both directions, and controller-level Swift tests drive a real Pause and a real title change through every fence; the reviewer's swap now fails 5 of 11 methods. The second wired `staleScopes` and `hasSameItem`, which had no production caller, into the plan's `attempt_stale` client-log event. The branch compiles and runs on mba: all 11 `AttemptScopesTests` pass on iOS and tvOS, which fail only the five cases `main` fails. The plan is **not done**: §5.1's "allow-list at least 12 entries shorter" is **not met** (8 comparisons across 4 fences), and **5.2-5.6** - the item observer, the poll conversions, Now Playing, the `Session` lock and strict concurrency - are **unstarted**. Device evidence is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **GPT takeover, 2026-09-24:** draft #506 commit `da5c6050a` migrates ten more `issueSeek` epoch comparisons to two pinned `AttemptFence` cases; 18 comparisons have been retired across 5.1, so its shrink threshold is reached in code. iOS/tvOS compilation and the tracked commit hook passed. The one adversarial review and unit lane wait for the ready PR; 5.2-5.6 and device evidence remain open. **2026-09-24 evidence-only update:** Commit `f30ef466b` implements the 5.5 `Session` credential lock on a branch; iOS/tvOS `make apple-build` passed, `make apple-test` has not run, and device evidence remains. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). **GPT 23:53 UTC:** `f30ef466b` locks `Session` credentials as one pair and `89d3c3672` moves the finite item status/end/fatal callbacks to one cancellable stream; iOS/tvOS app compilation and iOS test-target compilation passed. Unit execution, one review, prepared successor and 5.3-5.6 remain open. |
| A-03 | [NATIVE-LIBRARY-PAGING](../clients/NATIVE-LIBRARY-PAGING.md) | A5, D6 (library) | month | in-progress: 5.2-5.5 built; review, fast lane and device evidence open | gpt-6 | agent:/root/restore_a03 | [draft PR #506](http://192.168.4.7:3000/noirr/plurx/pulls/506), [PR #465](http://192.168.4.7:3000/noirr/plurx/pulls/465) | 2026-09-25 | Apple + Android. Milestone 5.1 merged as `df541e244`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: one `store::item_sort_order_by` replacing three drifting copies of the library `ORDER BY`, `id ASC` on the four sorts that had no tie-break, `sort_title` on `ItemDto`, and `tests/contracts/library-sort-cases.json` replayed on both backends whole and at page size 7. The plan's one adversarial review ([comment 4103](http://192.168.4.7:3000/noirr/plurx/pulls/465#issuecomment-4103)) raised two P2 findings, both fixed and disposed in [comment 4131](http://192.168.4.7:3000/noirr/plurx/pulls/465#issuecomment-4131). The load-bearing one is that the `resolution` sort ranked every row by its best file height while the DTO carries `resolution` for movies and home videos only, so a Home library's photos were ordered by a key no client can see and a client merging pages by the exposed keys could not reproduce the server's order; the SQL now ranks only the kinds `ItemKind::carries_resolution()` names and every other kind at -1, pinned on the clause, in the two-backend replay and over HTTP. The other corrected the paged-walk rationale: `every_sort_ends_in_a_unique_key` is the guard, not the page-7 walk. The plan is **not done**: the client milestones **5.2-5.5** - `LibraryMerge`, demand paging, drive-to-completion and off-main-thread filtering on Apple and Android - are **unstarted**, each needing a compiler and the device trace the plan requires as acceptance, and §5.1's `curl` acceptance against a deployed server is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). **GPT 2026-09-25:** Branch `codex/a03-library-paging-restore` implements 5.2-5.5 in code: Swift/Kotlin total-order merge cursors, on-demand pages and filtered drive-to-completion, off-main filtering, an older-server fallback, and shared fixture test sources. Apple iOS/tvOS application and iOS test-target builds passed; Android app and JVM test-source compilation passed. Unit execution, one review, Apple TV/Lenovo request-count and profiling evidence remain due. |
| A-04 | [NATIVE-ADAPTIVE-QUALITY-DESIGN](../clients/NATIVE-ADAPTIVE-QUALITY-DESIGN.md) | §3.8 | design | blocked: incomplete D3 matrix | gpt-6 | none:openai:2026-09-24 | [draft PR #506](http://192.168.4.7:3000/noirr/plurx/pulls/506) | 2026-09-24 | D1, D2, D4 and D5 merged as `9a401b75b`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: `tests/playback/auto-quality-policy.json` (30 cases, 12 controller-gate rows pinning all eight tick guards on their own side of the awaited health poll), the design's new §8 auditing every `AutoSample` field across the three codebases, the A-05 build plan, and Paul's D5 decision executed (both Apple `stallReopenIntent` overloads deleted, the wire retained). The plan's one adversarial review ([comment 4105](http://192.168.4.7:3000/noirr/plurx/pulls/458#issuecomment-4105)) raised one P1 and six P2 findings, all taken and disposed in [comment 4148](http://192.168.4.7:3000/noirr/plurx/pulls/458#issuecomment-4148). The load-bearing P1 is that the design's `hold:` evidence row would have frozen Auto: the server returns `hold` on every exchange while a paced producer is ahead and `producer_state` reads `held` whenever it is suspended, so §3.2 now splits it into a verdict answering this client's own `stalled` ask, which suppresses a downswitch, and the routine paced hold, which is not evidence - and the earlier "live web defect" claim is withdrawn. The P2s corrected what counts as an emergency, settled the decode contradiction on §4's one-rung-once answer, and fixed which build a trace runs on (an unmerged, sideloaded build of the milestone branch). `make apple-test` on mba fails only the five iOS cases `main` fails. The plan is **not done** and has **no measurement of any kind**: D3's shaped-network traces have not been taken on any platform, so every §5.3 number is absent and no controller may be enabled anywhere until they exist - **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. No feature gate and no new settings key; `playback_auto_abr` stays the Playback-panel toggle it is, and moving it to Developer with advisory readiness is the build plan's follow-up **F-1**, Paul's call, to be settled before A-05's M3 merges. **GPT evidence attempt, 2026-09-24:** Chrome on exact `f600d2823` failed before first frame with `WATCH_CLOSE_PROMISE is not defined`; Safari WebDriver session timed out and Firefox/geckodriver are unavailable. No cliff or D3 metric was observed. Raw/normalized reports are in the draft #506 status page; the browser runtime failure is being diagnosed before the matrix is repeated. **2026-09-24 evidence-only update:** Chrome 8→1.5 Mb/s D3 trace failed recovery: one restart/downshift and 4.2665 s frame gap. Safari session creation failed; Firefox and all native/two-cliff/HDR traces remain. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). **GPT 23:53 UTC:** playback-lab readiness fix `2a6fbc855` made the Chrome 8-to-1.5 Mb/s cliff observable. The trace failed recovery: one restart/downshift, 4.2665 s maximum frame gap and 21.113 s to downshift; Safari session creation and Firefox availability still block the matrix. Raw and normalized artifacts are named in the dated fleet evidence page. |
| A-05 | [NATIVE-ADAPTIVE-QUALITY-BUILD-PLAN](../clients/NATIVE-ADAPTIVE-QUALITY-BUILD-PLAN.md) | §3.8 (build) | month | unclaimed | | | | 2026-09-23 | Written by A-04 as its D4. **Do not start at M3.** M0 settles four disagreements between the design and the shipped browser that are pinned in `tests/playback/auto-quality-policy.json`, and the whole plan is gated on A-04's D3 shaped-network baseline, which **has not been measured on any platform**. Android is the first adapter; Apple's `link:` class has no evidence until someone shows AVFoundation can yield a per-transfer throughput sample. A platform's trace runs on an unmerged build of its milestone branch, sideloaded, and the PR merges after it. F-1 (moving `playback_auto_abr` to Developer with advisory readiness) is Paul's call. **2026-09-24 evidence-only update:** The Chrome D3 trace is a failed baseline, not a complete D3 matrix; A-05 remains unclaimed and dependent on the remaining traces. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| D-01 | [ANDROID-DISPLAY-MODE-AND-BUFFER-BUDGET](../clients/ANDROID-DISPLAY-MODE-AND-BUFFER-BUDGET.md) | §2.9, D1 | week (measure) / month | merged: M1–M3 | gpt-5.6-sol | agent:/root/s01_builder | [PR #409](http://192.168.4.7:3000/noirr/plurx/pulls/409) | 2026-09-21 | M1–M3 implemented with focused evidence: exact rational source cadence in `/decision`, deterministic same-resolution Android TV display-mode selection wired into the finite and Live TV lifecycles, node-local bounded telemetry, and an advisory-only Developer readiness surface. The sole adversarial review is complete and both findings are addressed: delayed Live TV tunes are fenced by channel serial and window-owner generation and cleaned up by exact session, and the advisory checkbox sends the accepted ordinary-settings payload. The plan is **not done**: M0 three-TV ADB measurement, M4 evidence-derived decoder buffer budgeting, and M5 physical HDMI verification are all still open, and the remaining Android device memory/display measurements are **post-merge** evidence that will be recorded later through the rule-4 evidence-only docs PR, not by this one. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| D-02 | [ANDROID-LIFECYCLE-PLAYER-BUILDER-AND-ERROR-CLASSIFICATION](../clients/ANDROID-LIFECYCLE-PLAYER-BUILDER-AND-ERROR-CLASSIFICATION.md) | D2, D3, D4, D7 | month | in-progress | gpt-6-astra | agent:/root/client_recon | [draft PR #506](http://192.168.4.7:3000/noirr/plurx/pulls/506) | 2026-09-24 | M6 merged as `e855529fb`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`, shipping with D-03 as Android build 120: node failover gated on the HTTP status through the pure `nodeFailoverEligible` predicate, a shared bounded cause-chain walk, a one-line call-site gate and an `http_status` telemetry field. The plan's one adversarial review ([comment 4098](http://192.168.4.7:3000/noirr/plurx/pulls/470#issuecomment-4098)) raised a single P2 - the M6 block in `web-policy.test.js` had split the async-drain comment from the drain it describes - fixed and disposed in [comment 4112](http://192.168.4.7:3000/noirr/plurx/pulls/470#issuecomment-4112). Its load-bearing contribution was evidence rather than a defect: the review compiled and ran the Kotlin the first session could not, and `make android-test` passes all 12 `LadderVerdictTest` cases, with a Kotlin revert proof of the `else -> false` fall-through going red; the only failures are the four `PlaybackSurfaceReducerTest` cases `main` fails, and `lintDebug` is green. The call-site gate is still a source assertion, because no JVM test reaches `onPlayerError`. One flagged deviation stands: `httpResponseCode` is extracted at the call site rather than in `PlaybackPolicy.kt`, whose header promises it stays free of ExoPlayer. The plan is **not done**: **M1-M5 and M7-M9** - the lifecycle contract, Live TV live-edge, AudioTrack classification, `PlurxPlayerBuilder`, offline init and poll backoff - are **unstarted**, and §5.6's device acceptance is **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **GPT takeover, 2026-09-24:** M7 is in draft #506 at `78830ec99`, with all four construction sites migrated and `:app:compileDebugKotlin` green in the pinned Android image. The final unit lane, one adversarial review and physical-device acceptance remain due; M1-M5 and M8-M9 are in progress or unstarted. **2026-09-24 evidence-only update:** The four-node `f600d2823` deploy is recorded; the plan’s named fleet or device acceptance still has no new run. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). **GPT 23:53 UTC:** M5 audio-sink classification is `38ece806e`; production and test Kotlin compilation passed, unit execution remains. M1 prepared-commit lifecycle pause reached its plan stop-and-flag boundary and is paused for a scoped decision. |
| D-03 | [ANDROID-CREDENTIAL-EXPOSURE-AND-RELEASE-BUILD](../clients/ANDROID-CREDENTIAL-EXPOSURE-AND-RELEASE-BUILD.md) | D5, D6 (release) | week (backup rules) / month | merged: M2, M7, M8 repo half; M1, M3, M5-M6, M9-M10 unstarted, M4 Paul's call | claude-opus-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #469](http://192.168.4.7:3000/noirr/plurx/pulls/469) | 2026-09-24 | M2, M7 and M8's repo half merged as `80398856f`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`, shipping with D-02 as Android build 120: both backup rule files exclude `datastore/` under every section, `signingConfigs.release` with no debug fallback, `make android-release`, and `android-publish` switched off `app-debug.apk`. The plan's one adversarial review ([comment 4102](http://192.168.4.7:3000/noirr/plurx/pulls/469#issuecomment-4102)) raised one P1 and two P2 findings, all fixed and disposed in [comment 4130](http://192.168.4.7:3000/noirr/plurx/pulls/469#issuecomment-4130). The load-bearing P1 is that `scripts/ship-physical`, the documented no-Ansible fallback, still built and installed the debug APK, so devices installed through it stayed `debuggable`; it now builds `:app:assembleRelease`, refuses to start without each signing input, rejects a debug signer, and no longer skips a `DEBUGGABLE` install at an equal versionCode. The P2s resolve a relative keystore before it reaches Docker as an empty volume and retain each build's R8 mapping keyed by versionCode. `make android-test` fails only the four `PlaybackSurfaceReducerTest` cases `main` fails, and `lintDebug` is green. The plan is **not done**: **M1 and M3** (the `file_grants` capability replacing the bearer in "Open in…") are **unstarted**, and the exposure stays open until both land; **M4** (Keystore wrap) is Paul's call after M2; **M5/M6** need the Lenovo trace M5 specifies; **M9/M10** need devices. A real `assembleRelease` through `ship-physical`, any device install, and switching the `plurx-agent` `mobile_release` role off the debug variant are **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** Physical device inventory is recorded; the Xiaomi is absent, release signing inputs were unavailable to this session, and the mobile role still points to a debug APK. No client was installed. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| P-01 | [RUST-TEST-EXECUTION-POLICY](../ci/RUST-TEST-EXECUTION-POLICY.md) | §2.2, §4.8 | week | merged: M2-M6 | gpt-5.6-sol | agent:/root/p01_builder | [PR #401](http://192.168.4.7:3000/noirr/plurx/pulls/401) | 2026-09-21 | Option (a), with no schedule, merged as `21eab120` after the sole adversarial P1 and fast lane were green. M1's named runner measurements were blocked by recorded FFmpeg drift; bounded source-only sizing and failed run URLs remain the evidence. |
| P-02 | [SERVICE-LIMITS-CHILD-PRIORITIES-AND-BUILD-HYGIENE](../ci/SERVICE-LIMITS-CHILD-PRIORITIES-AND-BUILD-HYGIENE.md) | §4.6 | month | merged: M1 partial, M2 §3.3, M5 audit, M6 digest half; M3 awaits Paul's seam decision; M4, M7-M8 unstarted | claude-opus-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #457](http://192.168.4.7:3000/noirr/plurx/pulls/457) | 2026-09-24 | M1 partial, M2's §3.3 raise, M5's audit and M6's digest half merged as `f6f519f1c`, one of the ten plan heads carried into main by integration [PR #480](http://192.168.4.7:3000/noirr/plurx/pulls/480) at `886fc8bd4`: idle limits read on nuc3, nuc4 and nynuc (open files **1024 soft / 524288 hard**, no Compose `ulimits`/`pids_limit`/`oom_score_adj`, `plurxd.service` installed on none, answering open question 1), `raise_open_file_limit` at startup, §3.4's ffmpeg-6 lane audit read-only, and both `Dockerfile` bases pinned by digest with a contract test and a weekly non-gating drift report. The plan's one adversarial review ([comment 4104](http://192.168.4.7:3000/noirr/plurx/pulls/457#issuecomment-4104)) raised three P2 findings, all fixed and disposed in [comment 4160](http://192.168.4.7:3000/noirr/plurx/pulls/457#issuecomment-4160). The load-bearing one is that §3.2.1's premise that every child reaches `spawn_job_owned` was false: a re-survey found eleven production spawns that never do (scan thumbnails, cover extraction, the encoder and decoder inventories, `ffprobe -version`, `media_pool.rs`'s `find`, the PGS ride-along self-test and the held decode-fact probes) and five production `pre_exec` registrations, one of which `execveat`s from inside itself so any closure registered after it never runs - so neither M3 seam is complete as written, and each now carries its own migration list. The other two clamp the open-file raise to `kern.maxfilesperproc` on macOS, where the unclamped raise reported a limit the daemon did not have, and keep the drift report running after a failed audit; the fix round also found the reviewed head's `rolling-producer-owners.toml` counts red and re-measured them. The plan is **not done**: **M3 is blocked on Paul's decision on the spawn seam** (per-site wiring versus `spawn_job_owned`, §3.2.1); M4, M7 and M8 are unstarted; §3.1's `LimitNOFILE`/`ulimits` row stays out because its 65536 would lower the observed 524288 hard limit; and the drift step has never run on a CI runner. The busy-evening fleet sample (§5.1's prompt, unrun), post-deploy `/proc/1/limits` and a week without `EMFILE` are **post-merge** evidence recorded later through the rule-4 evidence-only docs PR. **2026-09-24 evidence-only update:** Container open-file soft/hard limits were 524288/524288 on all nodes, with no EMFILE in five minutes; busy-evening and week-long checks remain. See [dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md). |
| P-03 | [LEDGER-TEXT-CONTRACTS-AND-RELEASE-TAGS](../ci/LEDGER-TEXT-CONTRACTS-AND-RELEASE-TAGS.md) | §4.4, §4.5 | month | merged: M1-M4, M6, M7 buildable half; M5 refused by its protocol; phase B and the first tag not started | claude-opus-5-5 | https://claude.ai/code/session_01AZemhL7Y1nXGWxUGRC2tkK | [PR #485](http://192.168.4.7:3000/noirr/plurx/pulls/485) | 2026-09-24 | M1-M4, M6 and M7's buildable half merged as `4424b3fe1`, one of the three heads (exact head `61244e048`) carried into main by integration [PR #489](http://192.168.4.7:3000/noirr/plurx/pulls/489) at `995b60f3e`. Both of the plan's section 7 decisions were answered by Paul on 2026-09-23: the ledger retirement is authorised, and releases are weekly tags cut from a green scheduled run. What landed: M1 (the history audit takes 44.4 s on the general runner, run 2797; 211 landings in 30 days bring in a corrective commit, 49 a week, the plan's section 7.5 line and a question for Paul before phase B); M2 (the pull request `Regression-Test:` field check, judged in a merge candidate the module builds itself with `git merge-tree`, because the fast lane checks out the branch head, not a merge candidate - correction 6); M3 (the landing-commit audit, resolving each line in the landing commit's own tree); M4 (the boundary machinery, proved identical to main's own checker on eight main commits and blocking on a simulated boundary); M6 (STATUS.md 2,890 to 382 lines, 46 sections moved verbatim into six guarded STATUS-HISTORY.md files); and M7's buildable half (`scripts/release-cut` and a Monday release-readiness run). The plan's one adversarial review ([comment 4256](http://192.168.4.7:3000/noirr/plurx/pulls/485#issuecomment-4256)) raised one P1 and three P2 findings, all fixed with revert-proven tests and disposed in [comment 4277](http://192.168.4.7:3000/noirr/plurx/pulls/485#issuecomment-4277). The load-bearing P1 is that the weekly tag would have gone on main's tip rather than the release commit, so a tagged build could carry fixes its changelog files under `[Unreleased]`; `--pending` now names the first-parent commit that brought the dated heading in (on this history it picks exactly the commits `v0.3.0` and `v0.2.8` were tagged on), refuses when the changelog does not describe it, and the run gates and tags that commit. The P2s: the cut rewrites every status line quoting a build counter so its tree passes operations-check; phase B asks a landing line only of corrective commits; and the test-marker rule refuses Rust `test_` helpers and accepts one-line Kotlin tests. The new field-check step ran for the first time on a real runner on #489 itself: it built the merge candidate, printed checkout against head, judged the title prefix, and in phase A reported without blocking that the range carried corrective commits and no field; the fields #489's body then gave resolve. The plan is **not done**. M5 is refused by its own protocol - the pre-authorised replacement passes on the revert (both images built, identical), so the text assertion stays (correction 7). **Nothing is in force:** `enforce_after` is unset (phase A); starting phase B is a follow-up that sets it to a landing commit and removes the lane step's `continue-on-error` in the same diff, and the section 7.5 load question is Paul's first. **Blocked:** the first release tag, because `publish_main` still has no trigger (P-01 / RUST-TEST-EXECUTION-POLICY section 3.1(b)), and a `RELEASE_TAG_TOKEN` secret is owed. No tag was cut. No fleet or device evidence is claimed or owed. |
| P-04 | [ARCHITECTURE-DOC-RECONCILIATION](../ci/ARCHITECTURE-DOC-RECONCILIATION.md) | §4.7 | week | merged: M1-M4 | gpt-5.6-sol | agent:/root/p04_builder | [PR #398](http://192.168.4.7:3000/noirr/plurx/pulls/398) | 2026-09-21 | M1-M4 and both adversarial corrections merged as `d77a1afa`; the maintained documentation audit reports no contradictions, missing headers, or unclear statuses. |

Not on the board by design: C16 (scratch reservations) belongs to the
seek-scratch repair effort and is tracked there.

## Reading the board

`unclaimed` rows with `week` priority and no `Notes` dependency are what a
new session should take first, in id order within the `week` set: S-01,
C-01, C-02, K-03, K-07, P-04, then the client `week` items. A row in
`blocked: fleet evidence` is not free work for a session without device or
fleet access — leave it. A row in `in-review` is waiting on the named
reviewer, not on a builder. `in-progress` always names one plan PR, never a
set of milestone PRs; `merged: <landed milestones>` means its one
implementation PR landed and only the post-merge evidence recorded under
rule 4 remains.

This file is kept honest by `tests/operations/test_docs_index.py` (every
linked plan must exist) and by rule 2 above (a claim without a PR is not a
claim).

# Architecture review GPT build — execution status

**Status:** open — PR #524 merged as exact main `8ae8cab1e136` after its sole adversarial review and green Main promotion gate. Signed Apple build 185 is installed on six physical devices and lineage-signed Android code 128 on five. Pinned `8ae8cab1e136` is running on all four nodes after serial voter then learner deployment; an interim duration collector is running, while active-flow and client interaction acceptance remain open. · **Updated:** 2026-09-25 21:34 UTC · **Main:** `8ae8cab1e136`

The [workboard](ARCHITECTURE-REVIEW-2026-09-20-WORKBOARD.md) remains the
canonical plan ledger. This page shows the assigned build as one operating
queue, with the next action and evidence location visible while implementation
and fleet work run in parallel. A `done` entry requires a merged change and
the acceptance evidence named by its plan.

The live evidence update is [draft PR #536](http://192.168.4.7:3000/noirr/plurx/pulls/536).

**Earlier 19:13 UTC preflight:** Read-only cluster UI showed three fresh voters, quorum two, leader nuc4 and zero apply lag; nuc3 was a healthy learner with a fresh heartbeat on `1d68af6eb`. SSH confirmed nynuc and nuc3 ran that OCI revision, while m6 and nuc4 retained `60f3803d1` despite newer checkouts. Five Android devices passed signer/API preflight, and six paired Apple devices were inventoried. The exact-main device installs have since completed; the pinned four-node rollout is tracked in the live table below. Sanitized preflight receipt SHA-256: `bc5a52e38a377a2a57b5865d3cefc1bcb78ddcf2f78ae8fa443a2d50aa1afc0c`.

| Lane | Current state | Next action | Evidence |
|---|---|---|---|
| D-02 · Android lifecycle | M1-M5 and M7-M9 code merged on PR #506. Exact-main signed, non-debuggable Release code128 installed on five Android devices. Four phones were locked; Google TV authenticated Home and an existing title→Resume showed player controls, but media-session position remained `-1`/speed `0.0` across Home/return, so decoded playback and owner pause/resume are unproved. §5.6 sequences remain open. | Run §5.6 physical sequences when unlocked and verify decoded frames/owner behavior. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), install receipt SHA-256 `64d7ec0a3616105110fa417282ce95e86f8439bfbbfdd111b9eadf4cccf8e88f`, Google TV receipt SHA-256 `9018c904acb52ee6748e9fba0951dc2a3ef9d668981f29e3f011d86b10867c51` |
| D-03 · Android credentials and release | PR #524 merged the audited signing lineage. Five physical devices accepted the same exact-main non-debuggable code128 APK in place with effective release signer, unchanged first-install times and no data clear. The TCL prior canary retained sign-in; playable offline retention is still unproved. The external-reader path remains open after automatic approval rejected handing private content to an unspecified app. An internal-reader follow-up is in progress. Ansible PR #23 remains draft because hosted GitHub checks did not start under account billing limits. | Verify offline playback retention and finish safe internal reader; keep Xiaomi/Lenovo absent-device rows open. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), install receipt SHA-256 `64d7ec0a3616105110fa417282ce95e86f8439bfbbfdd111b9eadf4cccf8e88f` |
| A-02 · Apple controller | 5.1-5.6 code and the physical XCUIRemote harness merged. Exact-main signed iOS/tvOS Release build185 installed on six devices; independent app reads and retained-container checks passed. Direct launches were initially locked or request denied. A physical tvOS Release XCUIRemote paging case then woke Bedroom Apple TV and passed 1/0; controller playback remains unverified. | Collect Release controller playback and interruption behavior when physical input is available. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), Apple receipt SHA-256 `edd908aabc51a5f4ace78ede63731c39f1c3da4147cc6c6787c6740358bc5b6a` |
| A-03 · Native library paging | 5.2-5.5 code merged. Six Apple Release build185 and five Android Release code128 installs are source-exact. Bedroom Apple TV Release remote paging passed 1/0 and loaded beyond first page; its XCUI activity timeline has 58 Down presses and a screenshot, but no retained server request/page or frame trace. Google TV Movies showed 377/459 loaded (331 match); Unwatched then 459/459 (328 match), stable after four seconds. This is partial TV evidence, not named Lenovo/end-scroll/filter restoration or request/frame acceptance. | Capture server request/frame traces, filtering and other-device Release paging after final main; use a preplanned trace window since old access logs are unavailable. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), Apple activity SHA-256 `74ae9263952a07893eaa5df29573779fd2f9a44182b7771223c1f6dd8b310cde`, Google TV SHA-256 `9018c904acb52ee6748e9fba0951dc2a3ef9d668981f29e3f011d86b10867c51` |
| W-02 · Web player decomposition | 5.4 and 5.5 code merged on PR #506. On exact main `8ae8cab1e136`, AST inspection measured `attachHls` at 15 lines and `play` at 28; standalone HLS startup and subtitle-readiness browser checks passed. `make web-check` stopped at current-main settings-sections case 30/31 because the Developer fixture omitted `subtitlePlaybackRangesCard`; the one-line harness repair is going into batched PR #527. The 5.1-5.3 type baseline and `scripts/web-types` are absent under the assigned owner, so full 5.4/5.5 acceptance remains open. | Re-run full web check after #527, then gather type baseline and physical input evidence. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), HLS log SHA-256 `a2cb1fd711694b5f0c082f56db6d15ba83f09ced6f38b024afdfb730ec7c8c4d`, subtitle log SHA-256 `6703149bca4f2a9a8b5b9fce7cb92a7f57fabf06468adf729a5e8777df3798ac` |
| L-03 · Shared Live TV transport | M2 capacity offers and M4 per-build caption proof merged through PR #506; the Live TV caption selector merged through PR #520. A caption-positive 8.1 broadcast source carried CC1 cues, but current-main Chrome exposed only Off and no text track during 106 seconds of playback. Exact-main Apple build185 is installed on six devices; no native Live TV caption selection/rendering is proved. PR #528 is the post-#482 caption follow-up. | Revisit the caption-positive source after #528; compare source, HLS advertisement and web/native selectors, then run the physical client pass. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), 8.1 receipt SHA-256 `e3f93552a5bed5cd9668dc92c972a5706a16c5c176dcd5d80bbbe63bad21abe2` |
| Developer adaptive Auto enablement | Existing `playback_auto_abr` switch moved to Settings → Developer on PR #506 at `4c82aeca0`; controller and dated trace/device evidence are advisory only; code merged at `44cdfccc7`. A signed-in m6 Developer page after the b47 upgrade showed the switch checked; browser controller met, native controller not met, Chrome recovery not met, remaining platforms not measured. The page states these rows never gate the checkbox | Refresh dated trace rows when traces change | F-1 in native adaptive quality build plan and A-05 workboard row |
| A-04 · Adaptive quality traces | The merged two-cliff harness measured both Chrome stages on an A-04 candidate branch based on exact main `8ae8cab1e136`; its binary stamped `v0.3.0-4346-g96dfaa95a`, so merged main itself has no matching binary trace. A loaded-host run failed first-cliff recovery (`commit_timeout`, 12.009 s downshift, two restarts, 5.0665 s maximum gap); its second cliff passed. The same candidate binary on an idle host passed both cliffs: 5.4614 s and 6.8382 s downshifts, 100 ms and 66.8 ms gaps, zero restarts/waits/stalls/hitches. This is load-sensitive candidate evidence, not stable D3 or the platform matrix. Safari session creation timed out; Firefox/geckodriver, native and HDR rows remain unavailable or unrun. | Repeat after final base and finish Safari/Firefox, native, Android and HDR matrix. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), failed raw SHA-256 `703901043f2a227a172f39d4a375f7882ff72fdbb9fa2afcd2c13f3b3385d9d6`, idle-pass raw SHA-256 `201f361345c9b4534859579e8497a87b10ae9cf75331348e0d25e66ec784bc69` |
| Main deployment and fleet evidence | Pinned merged main `8ae8cab1e136` was deployed serially to all three voters, then learner `nuc3`; both Ansible plays finished with zero failed/unreachable hosts. Independent 21:22:28 UTC receipt verifies checkout=running OCI exact SHA, stamped build `v0.3.0-4315-g8ae8cab1e`, Docker healthy/restarts0, readyz/metrics 200, three fresh voters/one learner, quorum/leader known and apply lag0. Read-only interim 30-second four-node collector plus bounded one-hour C-08 scrape started 21:25 UTC; first four exact-build/OCI and health rows passed. This is point and in-progress duration evidence, not active-flow or final-main acceptance. | Keep interim collector bounded; after #528/#534/#527 advance main, redeploy final exact SHA and reset the uniform one-hour, 24-hour and seven-day windows. Collect named active-flow/physical interactions. | [Dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md), four-node receipt SHA-256 `80bcdb418588e520e45b9fec1145ff721c72907aee8b8c97036a8ee20a7b661d` |

**Current history-audit blocker:** immutable #524 merge landing `8ae8cab1e136` omitted required `Regression-Test:` trailers for six corrective commits. The documented append-only erratum was automatically rejected as a validation-policy exception without explicit user authorization. That file remains untouched; evidence PR qualification is held pending the authorization decision.

The sections below retain dated execution history. The table above is the live
queue; a statement marked current at an earlier time is not the present fleet
state.

## Sole adversarial review and fast lane

The one adversarial review of PR #506 examined `e0164d784`. Its caption-master,
Android text-track and account-isolation, web capacity-offer, and Apple paging
findings were addressed in the same PR. D-03's external-reader bearer handoff
remains open under automatic approval rejection; no second review will be
requested for this PR.

Fast-lane #2933 stopped in history preflight. Ledger commits `19ecaa6`,
`2f2ff9d67`, and `fadbe098e` made focused `make history-check` green.
Fast-lane #2939 passed history but stopped on rolling-producer inventory
counts; `719ec563d` records the new owners. Local validation ran 242 tests
with one skip and passed, and all 63 Live TV web cases passed. API/build claim,
Apple attempt census/remote adapter, and moved evidence-workflow contracts were
corrected at `cf5a74a61`, `9301cae1c`, and `39e40d10d`, with their focused
checks green. New main `dafadf043` was merged into the PR at `47338f39d`;
its one conflict retained both client-anchor rows. The current-main merged tree passed pinned Rust check and Clippy, iOS/tvOS
application build, Android app/test-source compilation, and 95 focused
operations and ownership tests. Its final `make history-check` passed before fast-lane #2944. That gate passed all 505 Linux operations checks, then stopped on a web policy source assertion that still looked inside `play()` after the 5.5 split. Commit `9bf0425f9` follows the shipped `play()` → `presentPlayerChrome()` wiring; the focused web policy suite passed locally. Fast-lane #2951 passed policy, Apple, Android, Windows, and web jobs. Its Rust lane passed compilation and Clippy, then reported five file-grant migration/source inventory drifts and two unchanged timing-sensitive tests. `0a774e5ec` updates the schema, all named inventories and rollback fixtures, including the missing v45→v46 migration admission; six focused pinned Rust tests and formatting passed. `dfac3e62e` removes trailing Kotlin whitespace. The two timing-sensitive tests predate this PR and are assigned to the separate unit-failure batch. Fast-lane #2956 passed all policy, Rust, Windows, web, Apple, Android and aggregate promotion jobs on head `264f2a7d1` and base `dafadf043`. Both timing-sensitive Rust tests passed on that run. PR #506 merged as `44cdfccc7`; Apple deployment completed on six devices and all four nodes were verified on that current main commit.

## Postmerge evidence PR

PR #512 records the rollout and baselines and repairs the caption-audit wrapper. Its sole adversarial review found an empty-test success path and stale workboard rows; both were fixed. Fast-lane #2947 stopped in history preflight before unit jobs because the wrapper correction lacked evidence under phase A. A temporary ledger entry made phase A pass; current main then enabled phase B at boundary `448e803da`, which requires PR-level `Regression-Test` evidence for this branch. The PR body names `tests/operations/test_live_tv_caption_audit.py::test_audit_requires_one_completed_test`, the now-forbidden ledger entries were removed, and local `make history-check` passed with one landing commit awaiting its merge trailer. Main `68c29657b` was merged into the PR at `f6df390ec` and that tree passed local `make history-check` with all 2,279 corrective commits accounted for. Fast-lane #2949 then ran 506 operations tests and stopped on this page header: the index says `open` while the header said `merged`. The header now states `open`. Main advanced to `363d48e78` during qualification and was integrated with both parties' workboard changes at `0702cbe84`; focused status audit and current phase-B history check passed locally. Fast-lane #2976 then passed policy, Rust, Windows and the aggregate Main promotion gate on exact head `6b3d944f4`/base `363d48e78`. PR #512 merged as `e2dfc6b77`.

## Batched follow-up PR

[Merged PR #516](http://192.168.4.7:3000/noirr/plurx/pulls/516) batches the current-main fleet/device evidence and the corrected A-04 browser harness. Commits `196997b65` and `781223a21` provide independent two-cliff shaping/scoring and reject fixtures too short for both windows. The exploratory exact-main Chrome trace held 8→1.1 Mb/s for 75 seconds and 1.1→350 kb/s for 75 seconds; both stages applied, but the first failed recovery and the second began from a degraded baseline. Raw SHA-256 `c0e11b12365bcc3276fa98a738ad148f0574797b0b4974ae961262e9e7701509`; normalized SHA-256 `064ec0f75824730666cc29b6cdd8476d2a8a6c421aa044c08bcefef3bb783ba1`. The sole adversarial review of PR #516 found one P2: early first-window stalls could depress the whole-window average used as the second-cliff baseline. Commit `df98c440d` now uses a sampled, sustained 10-second tail immediately before cliff two and adds a regression where early impairment clears before a healthy second recovery. After the review fix, a historical #512 landing-trailer omission required an append-only erratum. Focused A-04 shaping contracts passed 98/98 after aligning the numbered-cliff assertion. Fast lane #3005 passed policy, Rust, Windows and the Main promotion gate on head `a7aa71b4f`/base `b47c5ff88`; #516 merged as `37baf6e0b5` with six checked regression trailers. A separate L-03 web caption selector is committed as `f8d50e594` on [draft PR #520](http://192.168.4.7:3000/noirr/plurx/pulls/520). Its sole adversarial review found one P2: fullscreen idle CSS hid the selector from keyboard Tab focus. Commit `9c421c545` keeps it visible and adds a fullscreen keyboard contract; PR #520 was marked ready at 15:11 UTC; Forgejo did not enqueue a new run from the WIP-title transition, so that status commit provided a ready-head synchronize event. Fast lane #3042 passed history and static-contract steps, then stopped on a Live TV test harness that extracted `liveTvAttachSession` without its new caption-refresh helper. The isolated harness now stubs that helper; `node tests/web/live-tv.test.js` passed all 64 cases (log SHA-256 `a4b101128084ac8fe66c6370627b20488d5bd064ecf7c9ad5792345be4fe602f`). Fast lane #3043 passed web/policy preflight and is running Rust/Windows on the prior base. PR #517 then advanced main to `38f61dfe6`; this branch merges that main at `389547a87`. Its next exact-head run must qualify the new merge tree. Exact-main three-voter rollout and signed Apple installs are complete with receipts in the dated fleet readout. `nuc3` rejoin and feature interaction remain open.

## Current merged main and fleet — 17:08 UTC

[PR #520](http://192.168.4.7:3000/noirr/plurx/pulls/520) passed fast lane #3052 after its sole adversarial review and merged as `415eb047f`; #519 and another merge then advanced protected main to `60f3803d1`. A serial deployment begun at `415eb047f` followed moving `origin/main` per host and therefore landed `415eb047f` on `nynuc` and `m6`, then `3ca348a77` on `nuc4`. Its zero-failure Ansible recap was a mixed-build result, not an exact-main receipt. The pin fix in the agent-owned Ansible branch, `0e5dddb92`, accepts one immutable `target_sha` and asserts each checkout before building.

A separate detached Ansible controller from `nuc3` subsequently deployed `60f3803d1` to the voters. It had no host limit, so it was paused before reaching the quarantined learner and terminated after `nuc4` became healthy. During the `m6` build, two voters remained fresh and quorum stayed available with zero apply lag. The independent post-stop three-voter receipt confirms exact checkout and OCI on all three, healthy containers with zero restarts, `/readyz` and `/metrics` 200, three fresh voters, quorum and leader known, and zero apply lag: SHA-256 `30787e7f26969c099b1e53498777c27dcd171c8ed478fb02b3d363454e20d292`.

The old b47 learner container on `nuc3` was unexpectedly found running from original `/srv/plurx`, starting at 14:12:58 UTC. It was stopped at 17:07:33 UTC without deleting original or forensic data. The reason it started is unknown; this does not replace the authenticated clean learner rejoin. Hold receipt SHA-256 `c0d9da2a16260390097651c7a0d19b043a59ed43d9884279d04abed2889864a1`. [The dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md) carries the timeline and evidence limits.

A bounded 17:10–17:11 UTC passive sample on exact `60f3803d1` recorded K-02/C-05/C-08/P-02/K-06/S-11 point values without restarting or activating workloads; receipt SHA-256 `e79d0a8c213777c6ec4efdd068fc7c4d972b80709dd2238dad217dfa853414d0`. The [dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md) states the values and remaining active/duration requirements.

At 17:12 UTC, another detached SSH-launched Ansible one-shot explicitly targeted `nuc3` with `force=true`. It reset that checkout to `60f3803d1` and entered `docker compose up -d --build` against the still-quarantined original data mount. The exact controller and Compose process tree was paused and terminated before a container start; the old learner stayed exited at its 17:07:33 finish time, and original/forensic data directories remained present. The launcher was a one-shot SSH session from `192.168.4.143`, not a resident cron/systemd unit; its initiating task is unresolved. Sanitized receipt SHA-256 `bf5e4d3b2cc9def1390cf10cbf3eda90e1ee2243ccdcdb8be80ae6f04a1ddea1`. The clean learner rejoin remains owed.

PR #524's sole adversarial review on head `577bed67a` found a release blocker:
the signing helper did not enforce the lineage's installed-data capability.
Commit `0967eb159` now checks both audited certificates, installed-data and
rollback capability before signing; `e3195ad85` pins rejection of a false
installed-data lineage. The reviewed branch's Android Release compiled with
Gradle 9.7.1/JDK 21, and the helper verified the lineage-signed code126 APK
SHA-256 `37455c3f81553069567f64b317bcd8328aa652ad3bb205bce744dee43f5b8e3f`.
The TCL canary retained sign-in, but a playable offline download was not
available to prove that narrower data path. At this earlier snapshot PR #524
remained draft; it has since passed its exact-head gate and merged.

At 17:24:54 UTC, the old b47 `nuc3` container was started again on original `/srv/plurx`, with its checkout already at `60f3803d1`. It was stopped at 17:35:26 UTC; original and forensic directories remained. Three voters were still exact `60f3803d1`, healthy/restart-free and `/readyz` 200. Docker had not restarted since September 20; `unless-stopped` alone does not explain this relaunch, and no live Ansible/Compose process, Plurxd/deploy systemd service or matching user cron entry was found. SSH sessions from `192.168.4.143` overlapped the start, but the caller command was not logged, so the external one-shot explanation remains an inference. No restart policy was changed because it would not block `docker start` or `compose up`. Sanitized receipt SHA-256 `8a0efb33d49e77ba87477ed997445f26980973a1b33c5f71ee43ac177a4c02f9`.


## Earlier impediments — superseded where the live table advances them

- The named handoff `claude/architecture-review-gpt-build-handoff-2026-09-24.md`
  is absent from the supplied checkout, the fresh Forgejo `main` clone and
  `~/code/plurx-agent`. Its location has been requested so its ownership and
  access rules can be applied; assigned work proceeds using the available plans.
- The latest user instruction controls batching: proper commits on one consolidated
  draft PR, exactly one adversarial review at merge readiness, then the fast
  lane. This differs from the repository's per-task focused-regression and
  task-PR convention; compile checks continue before integration.
- Automatic approval review rejected editing either D-03 external-reader
  call site because it would hand a private book capability URL to an
  unspecified app. Explicit authorization was requested; both call sites stay
  unchanged. A new durable release identity and audited debug-to-release
  lineage are held outside all repositories. One TCL API-36 in-place canary
  upgraded from debug125 to non-debuggable release126 with its sign-in intact;
  playable offline retention and the remaining Android fleet are still owed.
  Forgejo has no signing secret or signed artifact, so the agent-owned private
  material must be supplied explicitly for every release build.
- A 13:28 UTC physical revisit found four Apple build-183 installations but sleep/lock or CoreDevice XPC blocked interaction; four Android devices were locked on debuggable versionCode 124. Receipt SHA-256 `d2791c2eef76d9bcc4915e71ccee0bad0dc1e1c30df044785236cdcc06062217`.
- At 14:58 UTC a fresh read-only ADB inventory found four Android devices over five transports, all debuggable build 124 with one signer. TCL 9445X was unlocked with Plurx Home and populated rails; three other devices were locked. No D-02/A-03 interaction or signed release acceptance was obtained. Receipt SHA-256 `d31372ba321ae493c790f4b507b0c0b6e3c91ef6ada1ddb6b0647d6638eb228a`.
- `nuc3` is stopped with its original data intact. A copy at `/srv/plurx.forensic-20260925T131450Z` was inspected read-only with the old image; metadata CRC was valid, but `metadata_ahead_of_wal` requires a clean learner rejoin from the healthy quorum. No admin token file was found in the initial node/local inventory. A signed-in Cluster browser session was subsequently found, but transferring its one-time learner token to nuc3 is awaiting approval after automatic approval review rejected exporting the full authenticated page. The WAL report SHA-256 is `0531738625437b4f925dc0985a94a345c12fccef7768d06d2350d837423c2282`. A delayed old-image Compose container unexpectedly started at 13:32 and answered readiness on the original data; its bounded log was preserved (SHA-256 `aa40368084407b63b17e2841eefdb5e167f8703d26cd4b72ae6eb9fcbcd6be1f`), then that exact learner was stopped again under the runbook. A protected fresh directory is prepared for the authenticated rejoin after voter rollout; no cluster membership change has been made.
- The seven-day read-only fleet collector restarted after a 337 s gap, then the `44cdfccc7` window broke when `nuc3` became unreachable at 07:04:41 UTC. The other three nodes ran exact `60f3803d1` at 17:08 UTC; the four-node 24-hour/seven-day windows must start again after rejoin. The two broken legacy collectors were stopped after preserving their receipts (stop receipt SHA-256 `65bd4403223d3d72d8ea168c826aebdea76110830fc24c98bbe6094d010ca7d0`). A restartable compressed collector is prepared but has not been launched.

## Update rule

Update this page when a lane changes state, with the exact branch, PR, commit,
test or device observation that caused the change. Update its workboard row in
the same implementation or evidence PR. Record an unrun observation as owed;
never infer a pass from a build claim, an empty log window or an unavailable
device.

# Architecture review GPT build — execution status

**Status:** open — PR #524 exact-head gate #3086 found Android versionCode 126 already on new main; candidate advanced to 127 and repeat qualification pending; L-03 caption-positive web acceptance open; three voters on `60f3803d1`, current main awaits rollout; nuc3 forensic hold restored · **Updated:** 2026-09-25 18:39 UTC · **Main:** `1d68af6eb`

The [workboard](ARCHITECTURE-REVIEW-2026-09-20-WORKBOARD.md) remains the
canonical plan ledger. This page shows the assigned build as one operating
queue, with the next action and evidence location visible while implementation
and fleet work run in parallel. A `done` entry requires a merged change and
the acceptance evidence named by its plan.

| Lane | Current state | Next action | Evidence |
|---|---|---|---|
| D-02 · Android lifecycle | M1-M5 and M7-M9 integrated on [PR #506](http://192.168.4.7:3000/noirr/plurx/pulls/506) through `5829810bc`; production and test Kotlin compilation passed. Notification Pause while already owner-paused needs Pixel validation; a 14:58 UTC read-only ADB inventory found four devices still on debuggable build 124 (one unlocked, three locked), so no release-125 acceptance; code merged at `44cdfccc7` TCL 9445X now has debuggable build 125; a bounded Movies paging/filter baseline was observed, but §5.6 playback and notification sequences remain unrun. This is not signed-release acceptance. | §5.6 physical device checks | Plan execution log and workboard row |
| D-03 · Android credentials and release | M1 grant server/revocation and M3 API prep integrated, with a typed grant-store request for pinned Rust Clippy on PR #506; [draft mobile role PR #23](https://github.com/pjunod/ansible/pull/23) uses signed release artifacts. A 14:58 UTC read-only ADB inventory confirmed four physical Android devices still run debuggable version 124 with one shared signer; one showed populated Home rails, but no interaction acceptance. The current repo/org Forgejo Actions secrets have no Android signing inputs, release artifacts or APK upload path; no existing release identity was found. A read-only signing audit matched the installed debug-124 certificate to the local debug keystore private key. TCL (API 36) and both Pixels (API 37) can use an Android signing lineage for a data-preserving update once a durable release key and verified release-125 artifact exist; the Motorola is disconnected and its API level is unverified. Code merged at `44cdfccc7` TCL 9445X has debuggable build 125 with the same audited Android Debug signer; its bounded install/baseline receipt is in the dated fleet readout. This is not signed-release migration or acceptance. A distinct durable RSA-3072 release identity was created outside all repositories in a mode-0700 agent-owned directory; its certificate SHA-256 is `52c046be28f437ca19601b728617c6d4d1c85a13726092e06ac7d66738ff6c37`. A debug→release lineage granting installed-data preservation was created with Android build-tools 36, mode 0600, SHA-256 `7b9030ccfa355beeab3ca4d84236e703a45d976a57a28ee49d74f73a6bf37b7c`. A 17:08 UTC read-only TCL pre-canary census found API 36, debug code125, a nonempty settings datastore, and one file/11 KiB under `files/offline`; current bearer presence was not inspected. Receipt SHA-256 `6a4aaf11cf3d961bf2ba7a60d476c9c64bee7d6813045ed084d068ae4d4db81d`. At 17:24 UTC, one TCL API-36 debug125→release126 `adb install -r` canary succeeded: installed APK SHA-256 `37455c3f81553069567f64b317bcd8328aa652ad3bb205bce744dee43f5b8e3f`, effective release signer `52c046be28f437ca19601b728617c6d4d1c85a13726092e06ac7d66738ff6c37`, no debug flag, unchanged first-install time, and signed-in navigation on launch. Downloads UI said “No downloads”; offline playback retention remains unproved. Sanitized receipt SHA-256 `8c8a05e51a121e0e85d5d47ea01928731b02d25b248efaa52f9e05f3cf45788b`. Current main then took Android code126 via #523; #524 now targets code127 and a new exact-source lineage APK after gate #3086 rejected equal code126. A premerge TCL revisit found it locked, so no D-02/A-03 interaction pass was claimed (receipt SHA-256 `817966025b459ac3afa4897666dce3779d638106433bcb99af85b46a94f2b412`). | Roll out the verified release artifact serially to other compatible Android devices; finish external chooser if automatic approval permits; collect device acceptance and actual offline-download retention evidence | Plan execution log, workboard row and dated fleet evidence |
| A-02 · Apple controller | 5.1-5.6 code integrated on PR #506 through `fe204f789`; combined iOS/tvOS compilation and promotion gate passed; code merged at `44cdfccc7`. A dedicated physical tvOS XCUIRemote input harness is committed on this follow-up branch at `f548fb918`; build-for-testing compiled, but no device run or acceptance pass yet. Its fixture launch is DEBUG-only, so a future device test cannot by itself qualify the shipped Release archive. Signed iOS/tvOS Release from exact main `415eb047f` installed and independently verified on five physical devices; four iOS/iPadOS devices required passcodes and Apple TV launched without a remote input path. Install receipt SHA-256 `a128784fffaaa988cbd6410fa6c69a1a1ecef2913b66f8739c07bbdbcb1e28e9`; interaction receipt SHA-256 `6de3351bb209a9bfb0a8b506245bc2d1a747de32da088d39af14c2fe17d727ef`. Draft PR #524 advances the signed Apple build number to 184 for its changed app sources; the fallback ship script now reinstalls the exact archive even if a device reports the same build number. This new build is not yet installed. Bedroom Apple TV signed Debug XCUIRemote playback passed 1/0 after wake fix `09e8d7d69`; caption selection failed after bounded attempts. Private receipt SHA-256 `347ab4f96d4fce146bdd06f3fa586fb1d38c4e71231ddbf44b89e2c0b5789342`. This is not shipped Release acceptance. | Collect Release interaction after exact-main install; resolve tvOS caption menu harness | Plan execution log and workboard row |
| A-03 · Native library paging | 5.2-5.5 code integrated into PR #506 at `34e4f4364`; Apple iOS/tvOS and Android app/test-source compilation and promotion gate passed. Code merged at `44cdfccc7`; a build-183 iPad Pro screenshot shows Home and loaded content but no paging/filter interaction; a read-only Android pass saw one unlocked debug-124 Home screen with populated rails, without paging/filter interaction. The follow-up tvOS XCUIRemote harness adds a >40-item paging interaction case at `f548fb918`; it has compiled but not run on device TCL debuggable build 125 opened Movies: first dump showed 377/458 loaded and 331 matching, then Unwatched showed 458/458 loaded and 328 matching. UI dump times are coarse bounds; request and frame traces remain owed. Receipt SHA-256 `a3337559b4eac75752b11e51c89788a1321f67afe9c7adb6d7055645669c3134`. Bedroom Apple TV signed Debug `category:movie` paging case passed 1/0: remote navigation increased loaded count beyond its first page; private receipt SHA-256 `347ab4f96d4fce146bdd06f3fa586fb1d38c4e71231ddbf44b89e2c0b5789342`. Request/frame trace and Release acceptance remain owed. | Collect Apple TV request/frame trace and Release interaction; finish Lenovo checks | Plan execution log, workboard row and dated fleet evidence |
| W-02 · Web player decomposition | 5.4 `8907b9cf5` and 5.5 `cb7042ea2`/`858c8bc01` splits on PR #506; playback-lab startup fix `2a6fbc855`; JS syntax and promotion gate green; code merged at `44cdfccc7` | Type baseline and browser acceptance remain open under their assigned owners | Plan execution log and workboard row |
| L-03 · Shared Live TV transport | M2 capacity offers and M4 per-build caption proof integrated on [PR #506](http://192.168.4.7:3000/noirr/plurx/pulls/506) at `6710d15f4`; prompt A found CC1/SERVICE1 dialogue on three broadcasts and prompt C Chrome found an English hidden track without drawn text. On merged main `44cdfccc7`, a fresh 6.1 source capture again carried CC1/SERVICE1 dialogue; PR #512 repairs both stale audit-wrapper selectors at `8d128e143` and pins its nonempty result contract with a focused operations test Exact-main signed Apple Release is installed on five devices, but four required passcodes and Apple TV had no remote input path. Earlier 6.1/6.2/12.2 source captures carried no A/53 frames. A direct 16:40–16:41 UTC current-main source revisit decoded 338/336/328 frames on 6.1/6.2/12.2, with zero A/53 frames; no browser run was attempted. Source receipt SHA-256 `afc174bd8f4a40c54f5516cbf7c93fa092bc699b50940aaa5056f7d18b2d739c`. The 2026-09-25 physical tvOS Debug attempt used a VOD movie subtitle fixture, not Live TV; no caption choice was selected. It supplies no L-03 M4 pass (private receipt SHA-256 `347ab4f96d4fce146bdd06f3fa586fb1d38c4e71231ddbf44b89e2c0b5789342`). | The Live TV caption selector merged on PR #520 as `415eb047f` after its sole review and exact-head fast lane #3052 passed. Collect caption-positive web/native visual checks. On isolated PR source, Chrome showed the selector but three current airings had no CC payload, so that 60-second visual pass was source blocked (receipt SHA-256 `339db095e1aff2015cd7b45833c54edb9d4d9ecbd654a9a7dfc13dde680874eb`). A 17:45–17:51 UTC caption-positive 8.1 source follow-up exposed only Off in Chrome after 106 seconds and no textTracks; M4 remains open, with missing HLS/client layer to diagnose (receipt SHA-256 `e3f93552a5bed5cd9668dc92c972a5706a16c5c176dcd5d80bbbe63bad21abe2`) | Plan execution log, workboard and dated fleet evidence |
| Developer adaptive Auto enablement | Existing `playback_auto_abr` switch moved to Settings → Developer on PR #506 at `4c82aeca0`; controller and dated trace/device evidence are advisory only; code merged at `44cdfccc7`. A signed-in m6 Developer page after the b47 upgrade showed the switch checked; browser controller met, native controller not met, Chrome recovery not met, remaining platforms not measured. The page states these rows never gate the checkbox | Refresh dated trace rows when traces change | F-1 in native adaptive quality build plan and A-05 workboard row |
| A-04 · Adaptive quality traces | Exact-main Chrome 8→1.5 Mb/s trace failed recovery: 24.974 s, one restart/downshift and 1.883 s maximum video gap. Earlier two-cliff Chrome attempts failed before first frame. An earlier run reached only the first 8→1.1 Mb/s cliff and failed recovery at 20.9 s; the browser runner did not apply a second stage. An exact-main 8→350 kb/s Chrome run held 349.3 kb/s for 74.8 s and failed recovery, with a 16.433 s maximum gap. The harness merged through PR #516 now applies and scores both 75-second stages independently. Its exact-main exploratory Chrome 8→1.1→0.35 Mb/s run shaped both stages at measured 1099.2/349.2 kb/s but failed the first recovery: no sustained 10 s at 0.9×, one restart, 21.318 s downshift and 8.6 s maximum gap. The second stage started from a 0.747× baseline, so it cannot independently prove recovery. On exact merged main `37baf6e0b5`, the fixed harness measured both stages at 1099.2/349.2 kb/s, but first-cliff recovery took 61.748 s against 10 s and the second began from a 0.883× sampled tail; source-exact isolated-server receipt SHA-256 `e948519f7d40d6506b5e9273e33f8d5fce9a51f9803e7a503dbf1bca86034c89`. Firefox/geckodriver were unavailable; Safari session creation timed out. Apple TV asleep and iPhone/iPad locked refused native trace launch On exact current main `415eb047f`, the source-exact Chrome trace shaped 8→1.1→0.35 Mb/s at measured 1100/350 kb/s, but first-cliff recovery never held 10 seconds at 0.9×, with one restart, 16.617 s downshift and 3.003 s maximum transition video gap. The second 10-second baseline was 0.867×, so second-cliff recovery cannot be scored. Receipt SHA-256 `6399fc4afb34ea195c2d09724c5a097f7874ec3a2f84ec1f55d2efe50c0203bc`. The lowest advertised web rung is 360p at 1,360 kb/s (1,200 video + 160 audio), above the 1,100 kb/s link, and the 350 kb/s stage has no sustainable advertised rung. This is a real ladder/controller limit. | Complete platform matrix and physical interaction when available; retain failed traces as baseline evidence | Raw and normalized reports in `/Users/pjunod/code/plurx-agent/codex-a04-evidence-20260925/`; design §5.3 and dated fleet evidence |
| Main deployment and fleet evidence | At 05:14:47–49 UTC all four nodes ran then-current main `44cdfccc7`. On then-current main `e2dfc6b77`, a serial Ansible run completed on `nynuc`, `m6` and `nuc4` with zero failed hosts; independent checkout, OCI label, health, restart count, `/readyz` and build metric checks passed on all three. `nuc3` is stopped after a read-only WAL inspection of a preserved forensic copy returned `metadata_ahead_of_wal`; the documented path requires an authenticated clean learner rejoin. Main advanced to `b47c5ff88` through #510. At 13:49 UTC, `nynuc`, `m6` and `nuc4` independently verified checkout and running OCI revision `b47c5ff88`, healthy with zero restarts and `/readyz` 200; three fresh voters had quorum, leader nuc4 and zero apply lag. A synchronized 13:49:52 UTC passive sample captured K-02/C-05/C-08/P-02/K-06/S-11 point readings, receipt SHA-256 `5a863aa7226ceeac8f7d21bd426604f7fbad18e477e0994545c98c3c69248239`. The initial playbook exited 2 on an m6 Compose name race despite its subsequent healthy b47 container. An unforced nuc4 run initially advanced only its checkout; a forced run exited 0 and corrected its image. Nuc3 remains stopped pending the authenticated clean rejoin. A synchronized 13:49:52 UTC b47 passive sample captured K-02/C-05/C-08/P-02/K-06/S-11 point readings, private receipt SHA-256 `5a863aa7226ceeac8f7d21bd426604f7fbad18e477e0994545c98c3c69248239`. The three-voter quorum remains healthy. At 15:03 UTC, serial rollout and independent synchronized verification put all three voters on exact merged main `37baf6e0b5`, healthy/restart-free, readyz 200, quorum available and zero apply lag; private summary SHA-256 `f545b4b2b492ae5cdbde5ba1e78ff104dceb131e6a13f05b9186136008728668`. This was exact main when sampled; #517 subsequently advanced main to `38f61dfe6`, so a new post-promotion deployment is owed. It is a point sample, not four-node continuity. The selected collector’s first uniform HTTP-200 tick was 05:14:11 UTC. Earlier build changes, a 337 s collector gap and deployment refusals remain in its raw series. Signed Apple Release build 183 was rebuilt from exact newer main `b47c5ff88` and forcibly reinstalled on 17air, 17promax, Bedroom Apple TV, iPad Pro and iPhone 18 Pro; 16pro and iPad Mini were unavailable. Codesign and post-install bundle/version/build checks passed. The unchanged build number cannot identify the source by device metadata alone; the verified source-to-install receipt supplies that link. Signed iOS/tvOS Release from merged main `37baf6e0b5` was then built, codesign-verified and forcibly installed with post-query on the same five reachable devices; receipt SHA-256 `4d4c9ee5518241b39ab042b7035797cdda6854107b0056489ab30777430040e1`. A bounded physical revisit found no usable feature controls; interaction receipt SHA-256 `9ebf1669e6c940ad23dade96a8d7ce76b6ca16b2e0e4cacb2735b0c926fe8df8`. Interactive acceptance remains open. Earlier build 183 from `44cdfccc7` was installed on six devices; these are separate source builds with the same bundle number. A read-only iPad screenshot shows Home with loaded content. At 17:08 UTC, after an overlapping serial rollout was identified and its unbounded controller stopped before nuc3, `nynuc`, `m6` and `nuc4` independently verified exact checkout and running OCI `60f3803d1`, Docker healthy/restarts0, `/readyz` and `/metrics` 200, three fresh voters, quorum/leader known and zero apply lag; post-stop receipt SHA-256 `30787e7f26969c099b1e53498777c27dcd171c8ed478fb02b3d363454e20d292`. An old b47 nuc3 learner on original `/srv/plurx` was discovered running since 14:12 UTC and stopped at 17:07:33 UTC; original and forensic directories remain, hold receipt SHA-256 `c0d9da2a16260390097651c7a0d19b043a59ed43d9884279d04abed2889864a1`. Four-node duration and active-flow evidence remain owed. | Cleanly rejoin `nuc3` from the healthy quorum with a valid one-time learner token, pin the exact current main SHA, then begin fresh four-node one-hour, 24-hour and seven-day windows; collect active-flow and device interactions; verify the signed Android release canary after PR review | [Dated fleet evidence](ARCHITECTURE-REVIEW-FLEET-EVIDENCE-2026-09-24.md), [fleet/client baselines](ARCHITECTURE-REVIEW-FLEET-CLIENT-BASELINES-2026-09-25.md), [fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md) and deployment logs |

## Sole adversarial review and fast lane

The one adversarial review of PR #506 examined `e0164d784`. Its caption-master,
Android text-track and account-isolation, web capacity-offer, and Apple paging
findings were addressed in the same PR. D-03's external-reader bearer handoff
remains open under automatic approval rejection; no second review will be
requested for this PR.

Fast-lane #2933 stopped in history preflight. Ledger commits `19ecaa6`,
`2f2ff9d67`, and `fadbe098e` made focused `make history-check` green.
Fast-lane #2939 passed history but stopped on rolling-producer inventory
counts; `719ec563d` records the new owners. Local validation ran 242 tests
with one skip and passed, and all 63 Live TV web cases passed. API/build claim,
Apple attempt census/remote adapter, and moved evidence-workflow contracts were
corrected at `cf5a74a61`, `9301cae1c`, and `39e40d10d`, with their focused
checks green. New main `dafadf043` was merged into the PR at `47338f39d`;
its one conflict retained both client-anchor rows. The current-main merged tree passed pinned Rust check and Clippy, iOS/tvOS
application build, Android app/test-source compilation, and 95 focused
operations and ownership tests. Its final `make history-check` passed before fast-lane #2944. That gate passed all 505 Linux operations checks, then stopped on a web policy source assertion that still looked inside `play()` after the 5.5 split. Commit `9bf0425f9` follows the shipped `play()` → `presentPlayerChrome()` wiring; the focused web policy suite passed locally. Fast-lane #2951 passed policy, Apple, Android, Windows, and web jobs. Its Rust lane passed compilation and Clippy, then reported five file-grant migration/source inventory drifts and two unchanged timing-sensitive tests. `0a774e5ec` updates the schema, all named inventories and rollback fixtures, including the missing v45→v46 migration admission; six focused pinned Rust tests and formatting passed. `dfac3e62e` removes trailing Kotlin whitespace. The two timing-sensitive tests predate this PR and are assigned to the separate unit-failure batch. Fast-lane #2956 passed all policy, Rust, Windows, web, Apple, Android and aggregate promotion jobs on head `264f2a7d1` and base `dafadf043`. Both timing-sensitive Rust tests passed on that run. PR #506 merged as `44cdfccc7`; Apple deployment completed on six devices and all four nodes were verified on that current main commit.

## Postmerge evidence PR

PR #512 records the rollout and baselines and repairs the caption-audit wrapper. Its sole adversarial review found an empty-test success path and stale workboard rows; both were fixed. Fast-lane #2947 stopped in history preflight before unit jobs because the wrapper correction lacked evidence under phase A. A temporary ledger entry made phase A pass; current main then enabled phase B at boundary `448e803da`, which requires PR-level `Regression-Test` evidence for this branch. The PR body names `tests/operations/test_live_tv_caption_audit.py::test_audit_requires_one_completed_test`, the now-forbidden ledger entries were removed, and local `make history-check` passed with one landing commit awaiting its merge trailer. Main `68c29657b` was merged into the PR at `f6df390ec` and that tree passed local `make history-check` with all 2,279 corrective commits accounted for. Fast-lane #2949 then ran 506 operations tests and stopped on this page header: the index says `open` while the header said `merged`. The header now states `open`. Main advanced to `363d48e78` during qualification and was integrated with both parties' workboard changes at `0702cbe84`; focused status audit and current phase-B history check passed locally. Fast-lane #2976 then passed policy, Rust, Windows and the aggregate Main promotion gate on exact head `6b3d944f4`/base `363d48e78`. PR #512 merged as `e2dfc6b77`.

## Batched follow-up PR

[Merged PR #516](http://192.168.4.7:3000/noirr/plurx/pulls/516) batches the current-main fleet/device evidence and the corrected A-04 browser harness. Commits `196997b65` and `781223a21` provide independent two-cliff shaping/scoring and reject fixtures too short for both windows. The exploratory exact-main Chrome trace held 8→1.1 Mb/s for 75 seconds and 1.1→350 kb/s for 75 seconds; both stages applied, but the first failed recovery and the second began from a degraded baseline. Raw SHA-256 `c0e11b12365bcc3276fa98a738ad148f0574797b0b4974ae961262e9e7701509`; normalized SHA-256 `064ec0f75824730666cc29b6cdd8476d2a8a6c421aa044c08bcefef3bb783ba1`. The sole adversarial review of PR #516 found one P2: early first-window stalls could depress the whole-window average used as the second-cliff baseline. Commit `df98c440d` now uses a sampled, sustained 10-second tail immediately before cliff two and adds a regression where early impairment clears before a healthy second recovery. After the review fix, a historical #512 landing-trailer omission required an append-only erratum. Focused A-04 shaping contracts passed 98/98 after aligning the numbered-cliff assertion. Fast lane #3005 passed policy, Rust, Windows and the Main promotion gate on head `a7aa71b4f`/base `b47c5ff88`; #516 merged as `37baf6e0b5` with six checked regression trailers. A separate L-03 web caption selector is committed as `f8d50e594` on [draft PR #520](http://192.168.4.7:3000/noirr/plurx/pulls/520). Its sole adversarial review found one P2: fullscreen idle CSS hid the selector from keyboard Tab focus. Commit `9c421c545` keeps it visible and adds a fullscreen keyboard contract; PR #520 was marked ready at 15:11 UTC; Forgejo did not enqueue a new run from the WIP-title transition, so that status commit provided a ready-head synchronize event. Fast lane #3042 passed history and static-contract steps, then stopped on a Live TV test harness that extracted `liveTvAttachSession` without its new caption-refresh helper. The isolated harness now stubs that helper; `node tests/web/live-tv.test.js` passed all 64 cases (log SHA-256 `a4b101128084ac8fe66c6370627b20488d5bd064ecf7c9ad5792345be4fe602f`). Fast lane #3043 passed web/policy preflight and is running Rust/Windows on the prior base. PR #517 then advanced main to `38f61dfe6`; this branch merges that main at `389547a87`. Its next exact-head run must qualify the new merge tree. Exact-main three-voter rollout and signed Apple installs are complete with receipts in the dated fleet readout. `nuc3` rejoin and feature interaction remain open.

## Current merged main and fleet — 17:08 UTC

[PR #520](http://192.168.4.7:3000/noirr/plurx/pulls/520) passed fast lane #3052 after its sole adversarial review and merged as `415eb047f`; #519 and another merge then advanced protected main to `60f3803d1`. A serial deployment begun at `415eb047f` followed moving `origin/main` per host and therefore landed `415eb047f` on `nynuc` and `m6`, then `3ca348a77` on `nuc4`. Its zero-failure Ansible recap was a mixed-build result, not an exact-main receipt. The pin fix in the agent-owned Ansible branch, `0e5dddb92`, accepts one immutable `target_sha` and asserts each checkout before building.

A separate detached Ansible controller from `nuc3` subsequently deployed `60f3803d1` to the voters. It had no host limit, so it was paused before reaching the quarantined learner and terminated after `nuc4` became healthy. During the `m6` build, two voters remained fresh and quorum stayed available with zero apply lag. The independent post-stop three-voter receipt confirms exact checkout and OCI on all three, healthy containers with zero restarts, `/readyz` and `/metrics` 200, three fresh voters, quorum and leader known, and zero apply lag: SHA-256 `30787e7f26969c099b1e53498777c27dcd171c8ed478fb02b3d363454e20d292`.

The old b47 learner container on `nuc3` was unexpectedly found running from original `/srv/plurx`, starting at 14:12:58 UTC. It was stopped at 17:07:33 UTC without deleting original or forensic data. The reason it started is unknown; this does not replace the authenticated clean learner rejoin. Hold receipt SHA-256 `c0d9da2a16260390097651c7a0d19b043a59ed43d9884279d04abed2889864a1`. [The dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md) carries the timeline and evidence limits.

A bounded 17:10–17:11 UTC passive sample on exact `60f3803d1` recorded K-02/C-05/C-08/P-02/K-06/S-11 point values without restarting or activating workloads; receipt SHA-256 `e79d0a8c213777c6ec4efdd068fc7c4d972b80709dd2238dad217dfa853414d0`. The [dated fleet readout](ARCHITECTURE-REVIEW-FLEET-READOUT-2026-09-25.md) states the values and remaining active/duration requirements.

At 17:12 UTC, another detached SSH-launched Ansible one-shot explicitly targeted `nuc3` with `force=true`. It reset that checkout to `60f3803d1` and entered `docker compose up -d --build` against the still-quarantined original data mount. The exact controller and Compose process tree was paused and terminated before a container start; the old learner stayed exited at its 17:07:33 finish time, and original/forensic data directories remained present. The launcher was a one-shot SSH session from `192.168.4.143`, not a resident cron/systemd unit; its initiating task is unresolved. Sanitized receipt SHA-256 `bf5e4d3b2cc9def1390cf10cbf3eda90e1ee2243ccdcdb8be80ae6f04a1ddea1`. The clean learner rejoin remains owed.

PR #524's sole adversarial review on head `577bed67a` found a release blocker:
the signing helper did not enforce the lineage's installed-data capability.
Commit `0967eb159` now checks both audited certificates, installed-data and
rollback capability before signing; `e3195ad85` pins rejection of a false
installed-data lineage. The reviewed branch's Android Release compiled with
Gradle 9.7.1/JDK 21, and the helper verified the lineage-signed code126 APK
SHA-256 `37455c3f81553069567f64b317bcd8328aa652ad3bb205bce744dee43f5b8e3f`.
The TCL canary retained sign-in, but a playable offline download was not
available to prove that narrower data path. PR #524 remains draft pending its
exact-head fast lane and merge.

At 17:24:54 UTC, the old b47 `nuc3` container was started again on original `/srv/plurx`, with its checkout already at `60f3803d1`. It was stopped at 17:35:26 UTC; original and forensic directories remained. Three voters were still exact `60f3803d1`, healthy/restart-free and `/readyz` 200. Docker had not restarted since September 20; `unless-stopped` alone does not explain this relaunch, and no live Ansible/Compose process, Plurxd/deploy systemd service or matching user cron entry was found. SSH sessions from `192.168.4.143` overlapped the start, but the caller command was not logged, so the external one-shot explanation remains an inference. No restart policy was changed because it would not block `docker start` or `compose up`. Sanitized receipt SHA-256 `8a0efb33d49e77ba87477ed997445f26980973a1b33c5f71ee43ac177a4c02f9`.


## Current impediments

- The named handoff `claude/architecture-review-gpt-build-handoff-2026-09-24.md`
  is absent from the supplied checkout, the fresh Forgejo `main` clone and
  `~/code/plurx-agent`. Its location has been requested so its ownership and
  access rules can be applied; assigned work proceeds using the available plans.
- The latest user instruction controls batching: proper commits on one consolidated
  draft PR, exactly one adversarial review at merge readiness, then the fast
  lane. This differs from the repository's per-task focused-regression and
  task-PR convention; compile checks continue before integration.
- Automatic approval review rejected editing either D-03 external-reader
  call site because it would hand a private book capability URL to an
  unspecified app. Explicit authorization was requested; both call sites stay
  unchanged. A new durable release identity and audited debug-to-release
  lineage are held outside all repositories. One TCL API-36 in-place canary
  upgraded from debug125 to non-debuggable release126 with its sign-in intact;
  playable offline retention and the remaining Android fleet are still owed.
  Forgejo has no signing secret or signed artifact, so the agent-owned private
  material must be supplied explicitly for every release build.
- A 13:28 UTC physical revisit found four Apple build-183 installations but sleep/lock or CoreDevice XPC blocked interaction; four Android devices were locked on debuggable versionCode 124. Receipt SHA-256 `d2791c2eef76d9bcc4915e71ccee0bad0dc1e1c30df044785236cdcc06062217`.
- At 14:58 UTC a fresh read-only ADB inventory found four Android devices over five transports, all debuggable build 124 with one signer. TCL 9445X was unlocked with Plurx Home and populated rails; three other devices were locked. No D-02/A-03 interaction or signed release acceptance was obtained. Receipt SHA-256 `d31372ba321ae493c790f4b507b0c0b6e3c91ef6ada1ddb6b0647d6638eb228a`.
- `nuc3` is stopped with its original data intact. A copy at `/srv/plurx.forensic-20260925T131450Z` was inspected read-only with the old image; metadata CRC was valid, but `metadata_ahead_of_wal` requires a clean learner rejoin from the healthy quorum. No admin token file was found in the initial node/local inventory. A signed-in Cluster browser session was subsequently found, but transferring its one-time learner token to nuc3 is awaiting approval after automatic approval review rejected exporting the full authenticated page. The WAL report SHA-256 is `0531738625437b4f925dc0985a94a345c12fccef7768d06d2350d837423c2282`. A delayed old-image Compose container unexpectedly started at 13:32 and answered readiness on the original data; its bounded log was preserved (SHA-256 `aa40368084407b63b17e2841eefdb5e167f8703d26cd4b72ae6eb9fcbcd6be1f`), then that exact learner was stopped again under the runbook. A protected fresh directory is prepared for the authenticated rejoin after voter rollout; no cluster membership change has been made.
- The seven-day read-only fleet collector restarted after a 337 s gap, then the `44cdfccc7` window broke when `nuc3` became unreachable at 07:04:41 UTC. The other three nodes ran exact `60f3803d1` at 17:08 UTC; the four-node 24-hour/seven-day windows must start again after rejoin. The two broken legacy collectors were stopped after preserving their receipts (stop receipt SHA-256 `65bd4403223d3d72d8ea168c826aebdea76110830fc24c98bbe6094d010ca7d0`). A restartable compressed collector is prepared but has not been launched.

## Update rule

Update this page when a lane changes state, with the exact branch, PR, commit,
test or device observation that caused the change. Update its workboard row in
the same implementation or evidence PR. Record an unrun observation as owed;
never infer a pass from a build claim, an empty log window or an unavailable
device.

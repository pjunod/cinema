# Anchor playback information as a top-right ledger

Build: 81
Issue: #539

Mini, Standard, and Debug playback information uses a stable top-right,
two-column ledger. Fields keep structural placement as their values change,
and long explanations move to bounded notes rather than reshaping the panel.

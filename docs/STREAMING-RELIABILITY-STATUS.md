# Streaming reliability — review, repair, and promotion status

**Status:** effort merged to `main` once; §1 and §3 complete; §4 steps 1 and 2
(server half) landed; **§4's remaining server work is blocked on the open P2/D6
device measurement** · **Updated:** 2026-09-08 · **Effort:**
`effort/streaming-reliability` (merged) · **Started:** 2026-09-04 ·
**Effort fork:** `48615baf` · **Merged to main:** `bbe0bae9` via
[#53](http://192.168.4.7:3000/noirr/plurx/pulls/53) on 2026-09-06 ·
**Open:** [#65](http://192.168.4.7:3000/noirr/plurx/pulls/65) (§3 capacity
visibility, the durable outcome ledger, and the deploy status probe).
[#54](http://192.168.4.7:3000/noirr/plurx/pulls/54),
[#55](http://192.168.4.7:3000/noirr/plurx/pulls/55) and
[#57](http://192.168.4.7:3000/noirr/plurx/pulls/57) are closed as landed —
their commits reached `main` through the stack rather than through their own
PRs, and `git rev-list --count main..<head>` is 0 for each

Companion to [PLAYBACK.md](PLAYBACK.md) (the shipped playback contract),
[PLAYBACK-CONTROL-STATUS.md](PLAYBACK-CONTROL-STATUS.md) (the control-protocol
chronicle), and [VOD-CUTOVER.md](VOD-CUTOVER.md) (the immutable-HLS cutover) —
this page answers *what the current reliability effort is doing, what has been
proved, and what remains before it may reach `main`*.

The detailed, ranked result is
[STREAMING-RELIABILITY-REVIEW.md](STREAMING-RELIABILITY-REVIEW.md). The review
and evidence tasks were merged before the Forgejo cutover; all current task
branches and reviews use `noirr/plurx` on Forgejo.

The resumable remaining-work queue, exact worktrees and verification commands
are in [STREAMING-RELIABILITY-HANDOFF.md](STREAMING-RELIABILITY-HANDOFF.md).
Completed tasks are removed from that queue and retained here as evidence.

The working copy is an isolated clone under `/private/tmp`; the existing
developer checkouts are not used for implementation or validation. Task
branches merge into the effort branch through the compile-only development
gate. The effort reaches `main` only after one frozen-candidate full
qualification run, an exact-head adversarial approval, and green unit tests.

## Outcome — playback changes must preserve the player

The effort has four product outcomes:

- ordinary playback does not freeze or stutter because a producer, playlist,
  session owner, or client recovery loop disagreed about progress;
- a seek stays on the same immutable film timeline and does not recreate the
  stream;
- a quality, resolution, track, dynamic-range, or placement change uses one
  make-before-break transaction when the destination is compatible;
- an unsupported transition fails explicitly and leaves the working stream
  intact.

The review may change any component or contract needed to reach those
outcomes. It will not preserve the retained live-HLS engine as an unannounced
production fallback, hide an unfinished capability behind a compile-time gate,
or call a green unit suite physical playback evidence.

## Workstreams — the review decides the build order

| Workstream | State | Current evidence | Exit condition |
|---|---|---|---|
| Last-seven-days change review | **Complete** | 1,193 commits · 217 first-parent changes · 512 files · 220,101 insertions · 50,655 deletions since 2026-08-28; method and disposition are in the findings document | Exact-head review verifies scope and evidence |
| End-to-end architecture review | **Complete — adversarially approved** | Independent server, client, and operations passes converge on the missing prepare/commit transaction, incomplete VOD recipe coverage, and unbounded client freezes; exact-head re-review approved every correction at `b3179001` | Keep the approved contract authoritative while implementation evidence accumulates |
| Retained live-HLS engine, unhidden | **Merged — [#97](http://192.168.4.7:3000/noirr/plurx/pulls/97)** | The `live-hls-recovery` default Cargo feature is gone; the setting means the same thing in tests and production; sessions the engine serves are counted by reason, exported, and reported in Settings → Developer | The engine's existence is not a compile-time question and no session it serves is unattributable |
| §4 predecessor drain (step 1) | **Built — [#128](http://192.168.4.7:3000/noirr/plurx/pulls/128), stacked on [#111](http://192.168.4.7:3000/noirr/plurx/pulls/111)** | Durable `media_sessions.drain_deadline_ms` (SQLite 50, replicated AUTH 30). The commit gives the predecessor a deadline and touches nothing else, so it keeps renewing and keeps serving; the owner ends it on its existing three-second tick, the sweep is the cross-node backstop, and takeover refuses a draining row through both predicates *and* a trigger, because predicates fence only this binary. Store contracts green on SQLite and three voters; 1,908 daemon tests | Step 2 (`Switched`) releases the window early; without it every switch holds the second admission permit for the full ten seconds |
| §4 review findings folded in | **In [#128](http://192.168.4.7:3000/noirr/plurx/pulls/128)** | Two adversarial passes. The first, on the ratified design, found two structural holes before the column landed: a draining predecessor answering `demand: end` lost its receipt to the commit's and 503-retried for the whole window, and an old binary mid-rolling-upgrade could adopt a draining row and renew it forever. The second, on the implementation, found that the first fix for the receipt problem was worse than the problem — it made the retained reply mutable while every reader still requires it byte-exact, so a replayed commit reads as *refused*. A terminal exchange on a draining predecessor now ends the row instead of receipting it, which needs no schema and delivers most of step 2 early. Three more from that pass, all in the owner's tick: the ends are bounded by the tick's shared deadline rather than serially, a failed end no longer reaps the worker, and the reap carries the remembered VOD flag | Complete for step 1 |
| §4 predecessor drain (step 2, server half) | **Built — [#148](http://192.168.4.7:3000/noirr/plurx/pulls/148)** | Every node accepts `switched` and acts on it: an accepted exchange carrying that acknowledgement on a draining route ends the predecessor with cause `superseded` instead of letting it run the full window. The release lives in `control_local`, the one function both public ingress and the internal relay reach, runs after the exchange so the reporting packet is answered, and runs only on success so it sits behind the client-instance and monotonic-sequence fences. A `switched` naming a still-staged slot aborts that preparation. The vocabulary advertisement was withdrawn before merge — `ControlResponseV1` carries `deny_unknown_fields` and the relaying node re-parses it, so a new field on it is a rolling-upgrade break, not an addition | Clients send `switched` in a later release, once every node runs a binary from this one; the negotiation it needs is a separate release before that |
| Web prepared-switch adapter | **Implemented locally — draft PR next** | The passive web reporter declares `prepare_replacement`, retains the complete offer, emits the metadata/buffer/commit ladder, echoes the offered media origin, abandons a changed selection, detects a silently discarded commit from the repeated offer, and releases staged resources on every terminal path. The shipped-page integration and focused reporter contract pass. The measured capability remains `dual_player_preparation: false`; no successor player, presentation swap, `switched`, storage state, server behavior, or Rust source was added | Adversarial review, complete web gate, embedded-server format/lint proof, green effort development gate, then merge into the effort |
| Hidden gates — the settings with no control | **Built — [#153](http://192.168.4.7:3000/noirr/plurx/pulls/153)** | The last three entries in the §5 inventory, and none was an environment variable: `transcode.max_hw_sessions`, `transcode.software_pool_threads` and `offline.enabled` were settings the server already read and no page rendered. The two capacity keys are selects in Playback → Streaming; the offline switch and the three budgets beside it are a card in Maintenance. One `stored_switch` now owns the offline string, which three files parsed their own way. The adversarial pass found the copy quoting a live number off a response that does not carry it, a zero bound written from a guess about `try_admit_software` that the function's own comment contradicts, a `0` hardware option that is not the software-only mode its label promised, and a card that would have pinned one node's core count on the cluster from an edit about something else | No feature decides what the product does from a value an operator cannot see |
| Developer Enable prerequisites | **Merged — [#91](http://192.168.4.7:3000/noirr/plurx/pulls/91)**; corrections in [#127](http://192.168.4.7:3000/noirr/plurx/pulls/127) | `GET /api/v1/developer/readiness` answers each listed prerequisite with met / not met / not observable and the sentence of evidence it read. An adversarial pass then found two rows reporting a status from a reading the code never took: the passive-reporter row measured the four-action counter, so it answered *not met* on every fleet whose clients all carry a complete passive reporter, and the cache-revocation row blamed a peer's heartbeat for a closure whose live cause here is an unactivated credential guard. Both narrowed to what they read; both gates-nothing tests widened | Every §5 enable card carries a live reading, no reading gates a control, and no row asserts a reading it did not take |
| Hidden feature gates retired | **Merged — [#141](http://192.168.4.7:3000/noirr/plurx/pulls/141)** | `PLURX_PGS_OVERLAY` and `PLURX_DV_CONVERT` were the last two switches that decided what the server does from a compose file. Both are settings now, read per request, with cards in Settings → Developer. The overlay one is the user-visible one: default-off, it decided whether a viewer with a PGS-only subtitle track was offered that track at all, and selecting one forced a burn-in that re-encodes to SDR. An adversarial pass then found the move incomplete — the fragment indexer still read the boot value, one stored string was parsed three ways, a failed store read answered with the expensive default, and the test proving no prerequisite gates a switch was writing nothing at all. All four closed | No feature decides what the product does from an environment variable |
| Review and status PR | **Merged — [#903](https://github.com/pjunod/plurx/pull/903)** | Exact-head adversarial approval at `2fd62daf`; Effort development gate green; merged as `be088173` | Complete |
| VOD demand and deadline isolation | **Merged — [Forgejo #10](http://192.168.4.7:3000/noirr/plurx/pulls/10)** | Exact-approved `da291af5`, 103 focused Rust tests, 115 policy tests, and green Effort development gate; merged into the effort as `ebe71eb4` | Complete for this slice; not a prepared-handoff or release qualification |
| Client recovery and intent | **Merged — [Forgejo #6](http://192.168.4.7:3000/noirr/plurx/pulls/6)** | Exact-approved `263c69f0`, green development gate, and verified merge `ad79ae81`; includes current main `9c934391`. Integrated proof: 148 focused Rust tests, 121 validation tests, 123 focused Android tests, complete web static checks, and iOS/tvOS builds | Complete for this recovery/ownership slice; canonical captures and cold inputs remain separate tasks |
| Direct-file ranges and pacing | **Merged — [Forgejo #14](http://192.168.4.7:3000/noirr/plurx/pulls/14)** | Exact-approved `9239999a`, 45 focused stream tests, 121 validation tests, and green development gate; merged as `56e4f6eb`. Includes the reviewed merge-aware history-audit correction, with no gate bypass | Complete for this slice |
| Atomic client captures | **Merged — [Forgejo #20](http://192.168.4.7:3000/noirr/plurx/pulls/20)** | Exact-approved `8a7829b5` passed 103 Apple tests, 102 Android tests, tvOS compilation, complete web checks and every required development job; merged as `6efec8d9`, ancestry verified. Corrections cover source order, attachment binding, readiness consumption, reset while capture is absent, and terminal-response stop racing a newer intent | Complete for capture/reporter ownership; not a whole-pipeline canonical-intent or handoff claim |
| Apple cold input ownership | **Merged — [Forgejo #16](http://192.168.4.7:3000/noirr/plurx/pulls/16)** | Exact-approved `8d5d7693`, 50 focused Apple tests (including 19 ownership regressions), iOS/tvOS compilation, and green development run 93; merged as `816203bb` | Complete for this slice; no claim of a whole-pipeline deadline or prepared handoff |
| Durable-settlement foundation integration | **Merged — [Forgejo #19](http://192.168.4.7:3000/noirr/plurx/pulls/19)** | Main `15f88e53` integrated into the effort as `12be7b5b` after exact-head adversarial approval and a green development gate. The integrated task retained atomic accepted-demand publication and both ownership inventories; pinned check, Clippy, 535 focused daemon tests, 27 Store contracts, 165 operations contracts, 121 validation tests, and complete web checks passed | Complete for this foundation slice; durable settlement alone does not run or prime a successor |
| Serving-proof continuity | **Approval required — other tasks continue** | Independent design review approved retaining only an already-valid proof until its original expiry; automated safety review refused the implementation as a cluster authority-boundary change. No source changes applied | Explicit user approval for this safety-boundary change, then implementation, adversarial review, and deterministic/cluster qualification; no bypass |
| Immutable VOD transcode and burn | **Implementation and review corrections active** | Seven real-media tests pass production create → GET → decode, NTSC restarts, VFR normalization, text/PGS burn spanning a seek, ±250 ms audio correction, and independently decoded adjacent AAC joins. A warm local two-hour fixture takes 12.273 seconds near its end: bounded, but not responsive or seamless. Strong source/cache identity, current capacity policy, executable identity, and typed recipe refusals are being corrected | Final frozen recipe proof plus seekable exact-sample audio work and long-film/NAS performance evidence; the 12-second result is not product acceptance |
| Final qualification | **Complete for the effort** | Qualified and merged as `bbe0bae9`. The gate that mattered was not the unit suite: `main`'s own corrective-history audit was failing before this branch existed, so nothing based on `main` could pass until [#59](http://192.168.4.7:3000/noirr/plurx/pulls/59) landed first | Complete for the effort; each follow-on §-slice qualifies on its own |
| Promotion and cleanup | **Merged — [#53](http://192.168.4.7:3000/noirr/plurx/pulls/53)** | Merged into `main` as `bbe0bae9` on 2026-09-06 after resolving the Live TV promotion overlap, fixing a browser gate that had never once executed the reporter it exists to test, and a fixture-publication race found and repaired by a concurrent session | Complete |
| §1 — canonical desired intent | **Merged — [#61](http://192.168.4.7:3000/noirr/plurx/pulls/61)** | One shared `MediaIntentEnvelope` on ordinary create *and* control; the ask compared atomically at preparation admission, prepared commit **and ordinary activation**; database-level fencing of writers that predate the column; all eleven required races proved. Verified: 1,827 fast-gate tests, 139 replicated Store contracts against three voter processes, 0 failures | Complete |

## Known facts — claims already verified against the baseline

1. **The documented VOD-only cutover is not the production default.** Omitted
   presentation means VOD and an explicit live request is refused, but the
   default Cargo feature retains growing live HLS and a missing runtime setting
   enables fallback. VOD refuses transcode and subtitle burn, so those common
   classes silently enter the old mutable engine. Tests invert the runtime
   default and therefore miss the shipped configuration.
2. **The control plane stages but does not yet complete transparent change.**
   The 2026-09-04 M6 slice can write a prepared successor from an accepted
   in-session selection change. Its own handoff records the commit trigger as
   the next slice, and the current clients still perform several changes as a
   replacement open.
3. **A passing source test is not the playback claim.** The VOD cutover records
   automated Chrome coverage but still carries named-device and two-hour 4K
   physical evidence debt. This effort will preserve that distinction.
4. **The required compiler is available.** Rust 1.97.1 is installed and is
   invoked explicitly because Homebrew Rust 1.95.0 precedes it on `PATH`.
5. **The strongest freeze gates are not running.** Pull-request CI explicitly
   omits VOD steady, seek-storm, and stall-recovery cases. All eight inspected
   nightly runs from 2026-08-28 through 2026-09-04 failed before playback
   because the installed Playwright Chromium executable was not passed to the
   playback lab.
6. **The fleet reproduces an unbounded-hold class.** Seven-day node-local
   telemetry includes repeated Apple `server_hold` outcomes at one frozen
   position for many minutes. The current Apple and web paths allow an advisory
   producer hold to defer a decoder recovery without spending a finite budget.

## Decisions made without waiting

1. **Use the existing VOD and control foundations.** Their core direction is
   sound: immutable film-time resources remove entire freeze categories, and
   prepare/commit/abort is the right shape for compatible stream changes. The
   effort will repair or replace faulty details instead of introducing a third
   playback architecture.
2. **Expose unsafe enablement in Developer settings.** A capability that needs
   prerequisites will name them beside its control. No new compile-time or
   hidden configuration feature gate will be added.
3. **Prefer explicit refusal over destructive fallback.** If a destination
   cannot be prepared, the current stream remains authoritative. Recreating a
   session or silently changing presentation is not a transparent handoff.
4. **Use focused tests during development and qualify once.** Each task PR gets
   the smallest regression that proves its behavior plus the effort gate. The
   final fixed candidate gets the unit suite and one complete main-promotion
   qualification, in that order.
5. **Use Forgejo as the write authority.** GitHub is retained as a read-only
   historical remote. Current branches, task reviews, merges, and build status
   are published through `noirr/plurx` on Forgejo; final reporting records the
   exact branch and head so no GitHub status is mistaken for current evidence.

## Update log — newest entry first

| At (America/New_York) | State change |
|---|---|
| 2026-09-08 — web prepared-switch message plumbing built locally | The isolated Forgejo clone now carries the web adapter through the complete message-only transaction: declaration, exact offer adoption, readiness acknowledgements, commit with the offered origin, selection-change abandonment, silently discarded commit detection, refusal handling, expiry, withdrawal, and cleanup. A shipped-HTML harness proves the page sends the module's acknowledgements rather than merely testing an unused state machine. The focused `node tests/playback/web-control.test.js` contract passes. The capability remains false on its measured evidence, and this slice intentionally creates no second player and sends no `switched`. Draft review, adversarial findings, the one final full web suite, pinned Rust format/lint evidence, and the effort gate remain outstanding. |
| 2026-09-08 — the client brief, and the finding that a device stands in front of the rest of §4 | The prepared-switch contract is written as one document each adapter works from ([#160](http://192.168.4.7:3000/noirr/plurx/pulls/160), corrected by [#161](http://192.168.4.7:3000/noirr/plurx/pulls/161)), because three sessions inferring it separately would each infer it differently and the divergence would not surface until a real device. **Its adversarial pass returned 26 findings and the first one invalidated the plan the document existed to enable:** staging starts no worker — `Prepare`'s own doc says so — so a client that is offered a successor cannot fetch it, and three of the seven acceptance criteria described something the current server cannot do. Four statements in the first draft were outright wrong, each of which would have become the same defect in three codebases: the origin fence discards silently with a `200` rather than refusing with a `400`, so a client detects it by the offer's persistence and not by a status code; `session_gone` is `404`; a new `client_instance_id` is refused for the life of the owner epoch rather than resetting the sequence space, so "mint a fresh instance and start over" is a recovery path that can never succeed; and `committed` may not ride `demand: end` either, which is exactly where a teardown would put it. The review was itself wrong about one thing worth more than the rest — it said all three clients hardcode the capability `false`, having found Apple's *test fixtures*; Apple ships `true`. And Android's and web's `false` are **correct answers to a measurement** (the Google TV failed 3/3 admission attempts, Safari reached 13/20), so the first correction's advice to flip them would have turned a refusal an engineer can see into a stall a viewer sees. **Then the survey of the remaining server work found a device measurement standing in front of it** ([#162](http://192.168.4.7:3000/noirr/plurx/pulls/162)): `rendition_key` hashes neither height nor bitrate, so a Copy successor shares its predecessor's producer and bytes and is not a transition at all, and the one crossing that changes the media — Copy→Transcode, the pair the hardware run measured — is refused `501 vod_transcode_unavailable` pending P2/D6. The worker can be built; for every case VOD may legally serve it would produce the stream the viewer already has. |
| 2026-09-08 — the settings with no control, and a review that was right about all four things | The last three hidden gates are closed ([#153](http://192.168.4.7:3000/noirr/plurx/pulls/153)). None was an environment variable — `transcode.max_hw_sessions`, `transcode.software_pool_threads` and `offline.enabled` were settings the server already read and no page rendered, which is the same problem from the other end. The offline master switch, which cancels every preparation in flight, was reachable only by an operator willing to write JSON. **The adversarial pass found four things, and three of them were this change believing its own rationale instead of reading the code it was about.** The new copy quoted a live slot count off `SERVER`, which is the `/server` response and has no such field, so every operator read "the pair on System reads *slots in use*" — and System does not render that pair at all, so the sentence pointed at a page that does not show it. The API refused a zero software pool on the theory that `try_admit_software` admits nothing against a budget of zero; it does not, and `SwPool::try_take` says so in its own comment, because on a two-core box every session is over budget and refusing all of them would turn a budget into a ban. That false premise had a real cost: the DTO reports a stored value verbatim and the page sends the card whole, so any node already holding a zero could not save *any* streaming setting until an operator noticed a control they had never touched — **a control must be able to write back every value its own page can display.** A `0` hardware option labelled "Off — software only" is not one: `admit_live` still takes the hardware branch whenever the node has an encoder, so every start queues for a slot that never frees, waits out the whole five-second window, and 4K HEVC HDR is refused outright with "all 0 hardware transcode slots are in use". And the software default is derived per node while the setting is replicated, so the card now sends either capacity field only when an operator moved it, rather than giving a four-core node a sixteen-core machine's budget from an edit about the buffer limit. Also closed today, and closed as *settled* rather than left open: peer takeover keeps adopting live-HLS sessions regardless of the fallback switch. The note that left it open assumed a node could be configured differently from its cluster; settings are replicated, so it cannot. The real case is a stream that started while the switch was on and outlived its owner after it was turned off — gating it would end a film someone is watching rather than prevent anything. |
| 2026-09-08 — §4 step 2's server half, and a review finding that was invisible from inside the change | Every node learns the word `switched`: a client that reports its successor on screen releases the predecessor's drain immediately instead of paying the ten-second window, which matters most for the client that switches and keeps the tab open, because that one tears nothing down. Draft [#148](http://192.168.4.7:3000/noirr/plurx/pulls/148). No client sends it yet and none may until the whole fleet runs a binary that accepts it — `AcknowledgementState` has no unknown-value fallback, so a `switched` sent to an older owner is a 400 and then a 503 the client retries forever. **The adversarial pass returned a P0 that the diff could not show.** The change advertised the accepted vocabulary as a new field on `ControlResponseV1` so a client could ask before it spoke — but that struct carries `#[serde(deny_unknown_fields)]` and is re-parsed by the *relaying* node, so the "additive" field breaks every cross-node control exchange during a rolling upgrade: an older ingress relaying to a newer owner cannot parse the reply and answers `503 control_unavailable`. The advertisement is withdrawn; the handoff records that any negotiation field needs `deny_unknown_fields` relaxed and rolled out on its own first, and names two cheaper routes. Three P1s in the same pass, all about *where* the release ran: it was in `control_inner`, which a relayed request never reaches; it ran before the exchange, so the packet reporting the switch was answered `410 session_ended`; and it ran ahead of the client-instance and sequence fences, so a second instance could pull the drain out from under the one that had actually committed. It now runs after the exchange, in `control_local`, only on success. A `switched` naming a slot that was never committed also aborts that preparation instead of leaking an encoder permit until its deadline lapses. The qualification run then found two failures on the effort branch itself, from #128's column and not from this change: both hand-wound schema fixtures were a migration behind v50, which the fast gate's own assertion named exactly. |
| 2026-09-08 — the last environment gates, and two reviews that earned their keep | `PLURX_PGS_OVERLAY` and `PLURX_DV_CONVERT` are settings with Developer cards ([#141](http://192.168.4.7:3000/noirr/plurx/pulls/141)). Nothing in plurx now decides what the product does from a variable read at boot. Both PRs today were reviewed adversarially before merge and both reviews found real defects in work that was already green: on the drain, that the receipt fix I had written was worse than the problem it closed, and that a rolling upgrade could let an old binary adopt a draining session; on the gates, that the fragment indexer never saw the switch at all, so turning the Profile 7 conversion on gave you a decision path naming an index nothing would build. The pattern worth keeping: in both cases the expensive defect was not in the new code but in a *reader that was left behind*, and in both cases a test I had written to prove the change safe was passing without exercising anything. |
| 2026-09-08 — §4 step 1 built, and an adversarial pass on the design paid for itself | The predecessor drain is implemented on the durable-deadline design: SQLite 50 / replicated AUTH 30 add `media_sessions.drain_deadline_ms`, the commit gives the predecessor a deadline instead of retiring it, and the owner ends it on the three-second tick it already runs. Draft [#128](http://192.168.4.7:3000/noirr/plurx/pulls/128), stacked on [#111](http://192.168.4.7:3000/noirr/plurx/pulls/111). The review of the *design* found two holes worth more than any code finding, because they were cheaper to close before the column shipped: a draining predecessor answering the `demand: end` every reporter sends when the viewer closes the player would lose its receipt to the commit's and 503-retry for the whole window, and an old binary mid-rolling-upgrade could adopt a draining row and renew it forever — predicates fence only the binary that emits them, so takeover exclusion needed a trigger the way the v49 pointer fence did. Separately, draft [#127](http://192.168.4.7:3000/noirr/plurx/pulls/127) corrects two Developer readiness rows that reported a status from a reading the code never took; the passive-reporter row was answering *not met* on every fleet whose clients all carry a complete passive reporter. Three sentences in the §4 record were also wrong and are corrected in place, including the instruction to land steps 1 and 2 together — that was a property of the withdrawn lease-bounded drain, not of this one. Evidence: Store contracts green on SQLite and three voters, 1,908 daemon tests on each branch independently, Clippy clean, 18/18 web, validation catalog / history audit / operations contracts green. |
| 2026-09-07 — §4 step 1 built, reviewed, and withdrawn | The prepared commit was changed to stop retiring the predecessor and write it a lease ceiling instead, bounded by a rule about which incarnation the playback pointer names. It reached 145 green Store contracts on three voters and 1,908 green daemon tests. **Three independent adversarial reviews then found three different P0s in it, and the third ended the approach.** First: refusing the renewal does not bound anything, it kills the worker in one three-second tick — `owned_media_sessions` has no pointer predicate either, so the owner keeps offering the predecessor and reads the refusal as `cluster lease lost`. The old commit had hidden it from all of these at once with the blocked-publication sentinel; that sentinel, not `state = 'ended'`, was doing the work. Second: clamping instead of refusing only delays that by about eight seconds, because the lease loop drops any route with under four seconds left *before* it calls the store and fails it down the same path — so every healthy quality change would emit the split-brain log line, and the reap then leaves a zombie the VOD arm writes no terminal for, for up to six minutes. Third, and fatal: the rule needs "absence of a pointer row renews normally", because a session mid-creation has no pointer — but `end_media_session` **deletes** the pointer row, which is what happens when the viewer closes the tab. Two seconds after a switch the predecessor loses its pointer, the rule falls through to renew, and it renews every three seconds forever: an encoder, an admission permit and a shared-cache generation held for a stream nobody is watching, with node removal refused the whole time. **The conclusion is not another fix.** The owner's lease loop already owns what a lease means, and a drain is not a lease. The corrected design — the node that performed the commit schedules `end_media_session_if_owner` with cause `superseded`, and the lease machinery is not touched at all — is written into the handoff with all three failures beside it, so the next pass does not spend another day arriving here. Step 1 and step 2 must land together: step 1 alone replaces a clean terminal with a retry for a client that has no `Switched` to send. Landed from the work because it is correct standalone: `preparation_ack_replay` now gates on the durable receipt rather than on the predecessor being `ended`. Those were the same fact only while the commit retires it, and the receipt is the stricter fence — so lost-commit replay cannot silently break when step 1 finally lands. |
| 2026-09-07 — the live-HLS engine stops hiding | `live-hls-recovery` was a **default** Cargo feature that nothing in the build system ever turned off, so it decided nothing while hiding roughly a hundred code sites and an entire streaming engine behind a compile flag. Removed; the engine is simply compiled, as every shipped build already had it. **The find underneath it was worse.** `live_hls_recovery_enabled` read `== Some("1")` under `cfg(test)` and `!= Some("0")` otherwise — so every VOD-refusal regression in the repository asserted a contract production does not honour. One of them carries the comment *"The old fourth method is not a fallback. A transcode request is an explicit typed refusal"*, about a request the fleet answers with a live session. Four tests changed answer when the two rules were unified; each now names the policy it is asserting, and the shipped default finally has a test. And because that engine transcodes — real encode time on the node, announced in a log line and nowhere else — sessions it serves are now counted by the reason it was chosen, exported as `plurx_live_hls_recovery_sessions_total{reason}`, and reported on a Developer card that names the switch which stops it. The switch stays in Playback → Streaming; two copies of one control drift. One path is deliberately reported as *not* stoppable: a request arriving already named `Presentation::Live` — the peer takeover path — never consults the setting, so a node can serve live-HLS sessions with the fallback off. **Settled on 2026-09-08 by Paul: takeover refuses, and the refusal names the switch.** This entry originally left it open on the premise that a node might be configured differently from its cluster; it cannot be, because settings are replicated. The real case is a session that started while the fallback was on, kept running when it was turned off, and then outlived its owner. The argument for adopting anyway was that refusing ends a film rather than preventing a stream. The rule beat the trade: the cluster does not do what a setting says it may not do, and a case this rare makes the rule cheap rather than skippable. |
| 2026-09-07 — the Developer section can answer its own questions | The enable cards listed what must be true first and then left the operator with no way to find out. `GET /api/v1/developer/readiness` now answers each row from this process. **Two independent adversarial reviews found the first draft dishonest in five places and the "gates nothing" proof blind in two, and both are worth recording because both are the same mistake — answering a question the process cannot actually answer.** `cache_admin_revocation_ready()` returns `Ok(true)` on an unreplicated node (correctly: its real caller asks "may cache-only recovery proceed", and there is nothing to revoke), so every single-node install got a green tick under a sentence describing a heartbeat read that never happened — one payload contradicting its own roster row two lines above. `recovery_budgets` asserted "budgets fit the deployment" and disclaimed it in the same sentence. `clients_report` went green on one exchange for a requirement that says *every* client, from a counter that structurally cannot see a client which never reported. `client_two_player_handoff` said "no client declared `prepare_replacement`" from a counter that only counts clients declaring all four actions. `trusted_network` was printed as evidence for a boundary nothing in the daemon enforces. All five are now `unobservable` or read from a counter that answers the actual question — `prepare_replacement` got its own — and `Met` is unreachable for the rows that cannot earn it, which is the honest shape of those questions rather than a gap to close. The two blind proofs: the Rust advisory test drove a prerequisite that was `unobservable`, not `unmet`, so a gate spelled `if status == Unmet { refuse }` would have sailed past the test written to forbid it; and the web `togRow` stub dropped the arguments a `disabled` flag would travel in, so a reviewer disabled the switch and the test still passed. Both are fixed and both mutations now fail. The web assertion compares the whole panel with only the reading spans removed, so a `<fieldset disabled>`, an added title, or a card that vanishes is caught too. `server_preparation_is_real` is a claim about the build and would have gone stale silently when §4 lands, so it is pinned from the other end by `a_prepared_successor_still_publishes_a_staged_encoder`. Also: a store read failure no longer renders as "the switch is off", and the `process-capable-launch-method` ledger is +1 for a `membership.status()` the sentinel counts as a process launch and which launches nothing. |
| 2026-09-06 — §3 closed out | Everything in §3 that did not need a decision from the user is done and on `main` or in [#65](http://192.168.4.7:3000/noirr/plurx/pulls/65). `Hold` carries `revisit_after_ms`, so `no_room` is no longer an instruction to wait forever — taken as a bound on the existing action rather than a new one or a capacity variant of `ProducerDecisionReason`, because node capacity is not a producer decision and that category error is why the item sat open. Runway counts only a buffer that reaches the playhead; the web client already picked the region containing `currentTime` and the server was discarding the answer. Cache-hit GETs are counted for the first time — a gauge, not a cap, because refusing a viewer a segment already on disk is a decision about what that viewer loses. Control outcomes are written to the node-local `playback_events` table, so a terminal outlives the process that sent it; four kinds are kept and ordinary holds are not, since an exchange happens every few seconds per viewer. `scripts/ship --status` asks each node what build it is actually serving rather than what Ansible last did. What remains in §3 is three decisions that are the user's: fair service at the global limit, whether cache-hit GETs get a ceiling at all, and peer hydration versus local rebuild for VOD owner loss. |
| 2026-09-06 — the CI fleet is out of disk | Three runners hit `No space left on device` during one afternoon, and the failure does not say so: a runner that fills mid-link reports `ld terminated with signal 7 [Bus error]`, which is what a miscompile looks like, with the real cause hundreds of lines further down. Jobs were re-run that could never have passed. `scripts/ci-require-disk` now fails such a job on its first line, names the runner, and lists the largest directories for whoever prunes; it deletes nothing, because the work root holds other jobs' checkouts and a cleanup that guesses which are finished is how a running job loses its tree. The hosts still need pruning and that is not in this repository: nuc1 and nuc2 refuse the deploy key, rogg16 does not resolve, and the nuc4/m6 runners are inside Incus containers whose quotas are the thing that is full. |
| 2026-09-06 — §1 in review | [#61](http://192.168.4.7:3000/noirr/plurx/pulls/61) carries §1 whole. One `MediaIntentEnvelope` on ordinary create and control, with three separately monotone revisions because a recipe change invalidates work in flight, a destination change does not, and a pause must change neither. The ask is now compared inside the committing statement at all three doors — admission, prepared commit, and **ordinary activation**, which had no compare at all and made the whole guard bypassable by the plainest path there is. Old writers are fenced at the database, because schema compatibility is checked when a database is opened and never again. All eleven required races are proved; four were added after an audit found them missing rather than assumed present, and one test was deleted for proving nothing. Two defects found and fixed on the way: an import plan naming a version from the wrong number line (a restore-only failure), and a browser gate that had never once run the reporter it exists to test. |
| 2026-09-06 — three-voter divergence | The replicated Store contract found two faults in the §1 fence that the single-writer backend structurally cannot reach. Every migration in the chain died on `duplicate column name`, because a fresh install declares the new column while an upgrade adds it and both shapes are reachable from one marker. More seriously, a *refused* activation still ended the predecessor: the compare gated the pointer write, and a replicated transaction cannot branch, so the supersede applied on its own predicates — the viewer's session stopped and nothing started, strictly worse than the race the compare prevents. Both fixed and re-verified at 139 passed, 0 failed. |
| 2026-09-06 — effort merged to main | [#53](http://192.168.4.7:3000/noirr/plurx/pulls/53) merged as `bbe0bae9`. Four things had to happen first: the mobile release counters raised above main's, the Live TV promotion overlap resolved across six conflicts without taking a side, [#59](http://192.168.4.7:3000/noirr/plurx/pulls/59) merged because main's own history gate was broken and nothing based on main could pass until it landed, and a fixture-publication race repaired. That race is worth recording: publishing by `rename` unlinked the inode a reader was holding, which moves `ctime` and so trips a source fence for a source that never changed. A first repair replaced the rename with a hard link, which removed the replacement and introduced a narrower window in its place — the destination becomes visible before the temporary is unlinked, and that unlink moves the `ctime` of the inode now published. The repair that held is a concurrent session's advisory lock spanning the absence check through the rename. |
| 2026-09-06 — CI was never running | [#61](http://192.168.4.7:3000/noirr/plurx/pulls/61) targeted a task branch, and `ci.yml` fires on pull requests into `main` only. Every run reported "skipped" and the branch looked green for days without a single job executing. Retargeted at `main`; the real fan-out then found four failures, all genuine. Recorded here because a green-looking branch with no jobs is indistinguishable from a green one at a glance. |
| 2026-09-04 — 21:25 EDT | Capture [PR #20](http://192.168.4.7:3000/noirr/plurx/pulls/20) merged after exact-head adversarial approval and a fully green effort gate; current effort is `6efec8d9`. Added a resumable [handoff](STREAMING-RELIABILITY-HANDOFF.md) and removed capture from its outstanding queue. Dirty VOD work has its own on-disk checkpoint; its final review found a bounded-diagnostics/reap correction still to implement. Serial warm two-hour near-end preparation is 7.629 seconds (12.189 seconds child CPU), versus an earlier loaded 12.273-second run: neither is seamless-latency acceptance. No runtime edits have begun for the next desired-intent/film-time slice, and no final qualification has started. |
| 2026-09-04 — 21:02 EDT | Foundation PR #19 merged green as `12be7b5b`, integrating main `15f88e53`. Atomic capture code `fc544fad` and its separate regression bridge `7d5f959a` passed the tracked effort hooks. The capture task integrated this new effort cleanly and repeated 103 focused Apple tests, 102 Android tests, tvOS compilation, and complete web checks; its exact integrated-head review and development gate remain required. Prepared-media handoff, remaining cold-input axes, and long-film/NAS performance are still unfinished. No final qualification or production promotion has started. |
| 2026-09-04 — 20:47 EDT | Four repair PRs are integrated: server #10, HTTP #14, clients #6, and Apple cold-start #16. Published effort is `816203bb`; main remains separately at `15f88e53`. The main-integration task has 535 focused daemon and 27 Store tests green, plus pinned compile/lint and policy/web proof; its final Apple-inclusive tree is under review. Atomic captures have scoped source approval and have passed 103 integrated Apple tests, 102 Android tests, and complete web checks; tvOS compilation remains in progress. Real-media VOD proof now includes independent AAC joins, but a warm two-hour near-end seek still takes 12.273 seconds and remains performance work. No final qualification or production promotion has started. |
| 2026-09-04 — 20:26 EDT | Apple cold-input fix `3155bfe9` is committed after adversarial corrections and the tracked effort hook; 50 focused simulator tests and both Apple builds pass. Atomic capture review found three additional cross-queue/attachment/readiness races being corrected. VOD burn/VFR/offset production tests pass, but strict adjacent AAC waveform comparison found an eight-sample drift that remains under repair; identical init and timestamps alone were insufficient proof. Main separately advanced to `15f88e53` with PR #5 durable settlement and CI fixes; it is not yet integrated into this effort. |
| 2026-09-04 — client task merged | Client PR #6 merged as `ad79ae81` after exact-head approval and all development checks. Atomic capture envelopes and Apple cold input ownership started in separate worktrees from that base. The VOD task proved actual generation, GET, and FFmpeg decode at 0 → 90.09 → 3 seconds with unchanged initialization data; further burn/audio/VFR proof is active. Existing FFmpeg Full 9.0.1 has libass and is being used explicitly for burn tests, without installing or changing host software. Quorum proof-retention implementation is deferred for explicit approval after automated safety rejection; no authority code changed. |
| 2026-09-04 — 19:49 EDT | HTTP PR #14 merged into the effort as `56e4f6eb`; exact-approved head ancestry verified. Web correction `0a70d6fb` is exact-commit approved. Client integration now includes that effort and main `9c934391` without conflicts; the exact integrated web suite, iOS/tvOS compilation, and focused Android run are green. Rust Clippy passed; fresh Rust test compilation and focused regressions are running before the client merge commit and PR update. No full qualification has started. |
| 2026-09-04 — 19:40 EDT | HTTP [PR #14](http://192.168.4.7:3000/noirr/plurx/pulls/14) opened at exact-approved `9239999a`; its development gate is running. Independent review repeated 45 HTTP, 66 VOD, 21 history, and 7 ownership-inventory tests. The final web preparation/retained-observer patch passed `make web-check` and independent source review; its scoped commit is running. Main advanced separately to `9c934391` through layout PR #4; client integration will retain that work and repeat exact-tree checks. |
| 2026-09-04 — server task merged | VOD [PR #10](http://192.168.4.7:3000/noirr/plurx/pulls/10) passed its corrected Effort development gate and merged as `ebe71eb4`. Remote ancestry verified that the effort contains exact-approved `da291af5`; main remained `4ce33f95`. No main promotion occurred. The live next tasks are client web completion, HTTP range/pacing integration, and real immutable VOD recipe production. |
| 2026-09-04 — 19:00 EDT | Android follow-up `310a226b` committed with its tracked effort gate green. Final local source passed 123 focused JVM tests and all 115 validation-contract tests; its four-file source manifest was independently approved. Apple `856d9a0a` is also exact-commit approved. The detailed report now retains the executable-handoff implementation sequence and the remaining atomic snapshot/intent-envelope audit lead. Web preparation ownership remains active, so client #6 is still not ready to merge. |
| 2026-09-04 — 18:55 EDT | Apple continuation fix `856d9a0a` is committed and independently approved at its exact three-file tree: 43 focused simulator tests and iOS/tvOS compilation passed. Android follow-up addresses Pause revoking a create, orphaned stale responses, predecessor failures stealing a newer recipe, and a fired stall latch surviving an awaited control exchange; final focused verification is running. Web is adding an absolute decision/create/body deadline and preservation of the working predecessor on preparation failure. Client #6 remains WIP. |
| 2026-09-04 — 18:50 EDT | Server [PR #10](http://192.168.4.7:3000/noirr/plurx/pulls/10) is open into the effort at `b904b92c`, with exact-commit adversarial approval and 103 focused tests. Its [development run 56](http://192.168.4.7:3000/noirr/plurx/actions/runs/56/jobs/1) found eight ownership-inventory count changes introduced by the new regression code. The added task/timer/process lifetimes are being explicitly audited and documented before rerunning the gate; the failing gate has not been bypassed. |
| 2026-09-04 — executable handoff audit | A fresh source audit of main `4ce33f95` and concurrent PR #5 head `20951a9` confirms that durable acknowledgement settlement still creates an `encoder: staged` placeholder, not a running successor. The remaining contract, real-media producer, priming authority, client-ready/server-commit, switch, and predecessor-drain work is now itemized in the findings document. Existing ownership corrections are not a claim that transparent handoff is complete. |
| 2026-09-04 — focused ownership fixes | Android full-recipe correction committed as `c5504641`; its compile, 105 focused tests, and effort commit gate passed. Server demand correction committed as `abb872ba`; integration with main is being checked before publishing a task PR. Independent web review found partial-open recipe loss, a native-seeking watchdog latch, stale media-event counters, and unfenced diagnostic callbacks; corrections remain active. Apple lifecycle/offline/subtitle follow-ups are also active. No final qualification or main promotion has started. |
| 2026-09-04 — integration | Web correction `ca0913c1` passed the effort commit gate and complete static web checks. Current Forgejo main `4ce33f95` merged cleanly into client head `737952e8`, whose pinned Rust effort gate passed. Generic iOS/tvOS compilation passed with Xcode service access. The server reviewer independently repeated 103 focused tests (66 VOD including FFmpeg, 11 wait-pool, 26 scheduler); Clippy and formatting also passed on the reviewed server patch. That slice is being committed and integrated with main before exact-tree re-verification. New Apple review blockers cover unfenced subtitle awaits, missing fallback recipe ownership, stale stop/start demand and decisions, and offline seek routing. They remain active, alongside Android's full-recipe follow-up. Related [Forgejo #5](http://192.168.4.7:3000/noirr/plurx/pulls/5) implements durable handoff acknowledgements and Developer enablement guidance but explicitly does not start a real successor encoder; this effort will consume it after integration and complete the remaining media/client transaction. |
| 2026-09-04 — Astra follow-up | Apple commits `c50b6025` and `96065b89`, and Android commit `0248fa68`, passed their effort commit gates. Latest focused evidence is 31 Apple simulator tests and 98 Android unit tests. Subsequent review found Android audio/burn/offset composition and retry-budget ownership gaps, so further corrections are active. Web's static fast lane passed, then review found delayed HLS/direct metadata callbacks could suppress startup or overwrite a newer seek; exact-attachment fixes and regressions are being verified. Server review is closing cancellation, admission-clock, mixed-client fairness, publication, and zero-progress eviction races. Forgejo PR #6 has a fresh [progress comment](http://192.168.4.7:3000/noirr/plurx/pulls/6#issuecomment-29). No intermediate result is final approval. |
| 2026-09-04 — Astra implementation | Apple correction commit `c50b6025` passed its effort commit gate and 29 focused simulator regressions. Android's final focused compile and 98 tests passed; its effort commit is running. Cross-platform review then found additional Apple recipe-composition, pause-during-open, readiness, and target-deferral gaps; follow-up implementation separates desired recipe from the attached revision and is being compiled/tested locally. Web's new command-race tests pass; its complete static fast-lane gate is running. A second, independent Astra server reviewer is examining the VOD task. No current task has final approval or merge authority from these intermediate results. |
| 2026-09-04 — CI diagnosis | Forgejo run 26's Rust job failed before compilation because a relative action URL resolved to a missing repository; latest main fixes the URL. The web job found an obsolete three-argument `seekTo` source assertion after the corrected call added explicit intent preservation; that contract is being updated while retaining the behavioral regression. The source-only focused loop remains the build authority before pushing. |
| 2026-09-04 — Astra pass | The current published client head is **not approved**. Forgejo PR #6 is marked WIP with a progress comment. Additional review found web `/decision` responses overwriting newer commands, paused playback resuming during internal replacements, missing execution marking on copy-HLS fallback, leaked abandoned sessions, audio-offset routing loss, and an event-only `playing` recovery cancellation. These are active fixes, not closed findings. Android is also correcting quality-then-seek composition and an independent presentation deadline. Apple’s latest focused 27-test simulator run passed, Android’s intermediate focused 90-test run passed, and web’s cadence/epoch focused suite passed; later edits require fresh proof. The final full suite has not started. |
| 2026-09-04 — Astra pass | VOD correction now gives each admitted segment/init request a bounded deadline and file pin, isolates timeout failure to its entry, preserves retry deadlines, and schedules each viewer’s nearest request against accepted control demand. Pinned local compilation passed; focused regressions and lint are still running. The old PR #6 CI run has failed compile/static jobs and is not merge evidence; those failures will be inspected against the Forgejo pipeline. Latest notified main is `a013458f` after Forgejo PR #8. |
| 2026-09-04 | Exact-head client re-review found two additional Apple presentation gaps: subtitle replacements had no destination generation, and delayed healthy audio could run beyond the landing tolerance and remain pending forever. Subtitle commands now publish an exact destination on every route, in-place selection settles only after its exact mutation, reopen selection waits for destination presentation, and audio applies ±250 ms only to its first landing sample before requiring monotonic advance. Focused simulator proof is green; the follow-up commit and another exact-head pass are in progress. |
| 2026-09-04 | The second adversarial client pass rejected eleven P1 and two P2 race/evidence gaps. All are closed at `78223cef`: actual-output callbacks carry intent generations; Android relative seeks accumulate; failover gets fresh ownership without resuming a pause; Apple/web honor the absolute deferral deadline; presentation waits are bounded; replacements publish exact destinations; overlay mutation follows publication; native seeks are item-fenced; late terminal replies cannot re-arm; Original copy explicitly refuses Auto; audio requires advancing presentation; leases are monotonic; and tests map production seams. Focused Android, Apple, web, and Rust proof plus the effort gate are green. Exact-head re-review is next. |
| 2026-09-04 | Committed the client corrections as `3b5387e5` and their exact regression mapping as `fb3424ac`. Both effort-mode commit gates passed. The first attempt was correctly refused because a historical Android seek regression still named a production helper the new coalescer had bypassed; the seek now routes through one production-linked viewer-seek invalidation seam, its focused regression passes, and the history audit is green. Exact-head adversarial re-review is next. |
| 2026-09-04 | Moved current work to Forgejo as the sole write authority. The first exact-head adversarial client review rejected nine gaps: Android could still freeze before establishment; deadlines counted control latency incorrectly; quality state went stale; Apple did not publish the exact seek target before mutation; request sequence could be lost; seek completion was not presentation; stale actions crossed awaits; terminal verdict lifetime was too broad; and Original collapsed into Auto. All nine are corrected locally. Android units, the web control contract, Rust 1.97.1 all-target compilation and focused units, 359 Apple simulator tests, and generic iOS/tvOS compilation are green. The first Apple test run exposed one stale fixture that asserted an implicit seek target; the fixture now supplies the explicit target and the entire rerun passes. |
| 2026-09-04 | Opened historical GitHub client-recovery PR #911 against the effort before the Forgejo cutover. It is no longer an authority or merge path. The reviewed branch is now published only to Forgejo; its Forgejo task PR must name the new exact head before merge. |
| 2026-09-04 | Client recovery is implemented locally without a feature flag. Android now detects a still-open established buffering freeze at 8 seconds, gives control at most one deferral inside a 20-second absolute deadline, and turns exhausted recovery into a visible failure. Apple and web use the same finite-authority rule. Owner changes settle old asks immediately; web terminal diagnoses now survive same-title replacement only for their lease; Android carries stable playback identity, exact quality, and explicit seek intent across controller replacement. Focused proof: web control passed, Android JVM units passed, and Apple ran 274 selected tests with zero failures. |
| 2026-09-04 | Playback evidence PR #908 passed exact-head adversarial review at `73d9bab7`, passed the Effort development gate, and merged into the effort as `7db47b35`. PR #905 was superseded and auto-closed when its old head branch was removed. |
| 2026-09-04 | PR #905 third-pass review also found that one 200 ms Auto hitch could pass the unimplemented p95 target, and that Auto never checked its first target frame against the film-position boundary. The singleton nightly transition now must stay within 100 ms, the 250 ms absolute ceiling remains explicit, and target-tagged frame evidence rejects both forward and backward jumps over 250 ms. |
| 2026-09-04 | PR #905 third-pass review found API presence could be mistaken for an operative frame oracle when Chromium never invoked its callback. Auto now requires a finite post-cliff callback before suppressing sampled-clock evidence; zero-callback and stopped-callback regressions both fail closed. |
| 2026-09-04 | PR #905 second-pass adversarial review rejected four P1 evidence races and one P2 seam despite a green gate: outgoing frames could satisfy target readiness, hold-window gaps could be reset, Auto moves between polls could collapse, non-finite Auto runway could pass, and final pre-replacement faults could escape sampled rollup. The local correction tags frames, closes each transition after its hold, uses a monotonic timestamped switch ledger, fails runway closed, and increments lifetime faults at their source. |
| 2026-09-04 | Addressed every first-pass PR #905 finding: the Auto cliff is a strict nightly point; rendition completion needs the expected method, exact height, and a new frame; the boundary snapshot and page-lifetime frame/counter probes cover seams and silent freezes; 60-second post-downshift stability is mandatory; identity, runway, position, per-transition p95/max gaps, and partial-browser scope are explicit. |
| 2026-09-04 | Opened corrective PR #905. Its first adversarial pass rejected unscheduled Auto evidence, stale-rendition acceptance, silent-freeze and first-sample seams, bad hold-time math, incomplete identity/dwell checks, and overclaimed browser SLO coverage. All findings are being implemented; audio remains explicitly device evidence. |
| 2026-09-04 | PR #903 merged into the effort as `be088173`. Started the first corrective task in the isolated clone: repair the playback evidence path before changing the behavior it judges. |
| 2026-09-04 | PR #903 exact-head adversarial re-review approved the architecture and findings at `b3179001` with no remaining merge-blocking factual or design errors. The required Effort development gate passed; this status-only accuracy update is the final pre-merge change. |
| 2026-09-04 | PR #903 re-review found three remaining acceptance/evidence holes: supported transitions could all refuse, Auto could still visibly stutter, and retained fleet/receipt claims lacked executable detail. The contract now requires 20/20 commits for qualified tuples, applies continuity SLOs to Auto, and retains the commands and named receipts. |
| 2026-09-04 | PR #903 exact-head adversarial review found eight documentation/architecture defects: adaptation authority, seek intent, owner-loss acceptance, gated-feature inventory, numerical SLOs, retained evidence, completion scope, and inconsistent status. All eight were corrected for re-review. |
| 2026-09-04 | Opened draft review PR [#903](https://github.com/pjunod/plurx/pull/903); completed independent client, server, and operations passes; recorded four P0 blockers and the target immutable-rendition/transaction architecture. |
| 2026-09-04 | Created isolated clone and `effort/streaming-reliability`; confirmed authenticated GitHub access and the pinned Rust 1.97.1 toolchain; began independent server, client, and operations/architecture reviews. |

# Optical M4 ownership advertisement evidence — 2026-09-21

**Status:** partial; owner placement and relay built, live acceptance remains open ·
**Branch:** `codex/optical-m0-foundations` · **Toolchain:** Rust 1.97.1

## What this slice proves

Media-pool protocol 7 adds an explicit `optical_v1` capability and a bounded
list of path-free drive snapshots. Each drive names its owner, public ID,
label, lifecycle state, insertion generation and disc identity. Device and
mount paths cannot enter the advertisement type.

The snapshot receiver requires the exact protocol, fresh expiry, the claimed
peer identity, unique bounded drive IDs, bounded lifecycle fields and
`optical_v1` whenever any drive is advertised. A stale peer snapshot expires
the drive ownership with the rest of that node's media capabilities.

Authenticated `GET /api/v1/optical/drives` now combines local drives with
fresh remote advertisements. Replicated disc metadata supplies the display
summary at any ingress. Commit `a59d1429` adds bounded, exact-request-authenticated
owner forwarding with signed responses for disc/title discovery, playback
decisions and generation-checked eject. The ingress accepts only currently
advertised owners from committed reachable membership; transport timeouts,
oversized or unsigned replies fail as stable `optical_owner_unavailable`.
Eject additionally requires voter authentication at the owner.

Commit `59f52278` extends that exact-auth owner exchange to session start and
session-bound progress. The physical drive owner is the only node that claims
the reader and creates the managed VOD producer. It then publishes an active
route in the existing durable media-session ledger with the separately
versioned, path-free optical payload. Any ingress can consequently reuse the
existing bounded relay for the HLS playlist, initialization and media
segments, status, and terminal DELETE without adopting the physical source.
The optical payload is not accepted by the legacy file recipe decoder, so an
expired owner cannot trigger file failover or a second physical reader on a
node that merely has access to replicated metadata.

The pinned all-target `plurxd` compiler loop passed on `59f52278` after owner
placement and durable routing were added:

```bash
/Users/pjunod/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin/cargo \
  check --locked -p plurxd --all-targets
# pass in 8.44s with the task target warm
```

Focused ownership and relay tests join the single final fast-lane run rather
than being run after every construction step.

## Still open

- A live multi-node advertisement expiry and owner-loss observation.
- Multi-node acceptance for remote start, progress, playlist, segment, status
  and terminal relay on the exact packaged candidate.

The remaining qualification gap is evidence, not an alternate placement path:
the implementation keeps source execution on the advertised owner and refuses
physical-source adoption by design.

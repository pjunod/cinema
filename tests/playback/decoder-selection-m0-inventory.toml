version = 1

# Shipping command builders. A new production hls_args call must be added here.
[[surfaces]]
id = "builder.prepublication_retry"
kind = "hls_builder"
source = "crates/plurxd/src/transcode.rs"
anchor = "impl PrepublicationTranscodeRetry"
obligation = "freeze the validated alternate plan and shared recovery budget"

[[surfaces]]
id = "builder.resumable_part"
kind = "hls_builder"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn produce_into("
obligation = "carry plan identity and health evidence through every retained part"

[[surfaces]]
id = "builder.live_hls"
kind = "hls_builder"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn start_with_audio_offset("
obligation = "resolve once from the held source for command, identity, and admission"

[[surfaces]]
id = "builder.live_tv_hls"
kind = "hls_builder"
source = "crates/plurxd/src/live_tv.rs"
anchor = "fn live_ffmpeg_command("
obligation = "freeze direct Live TV HLS arguments and migrate its decoder choice to the same explicit plan"

# Process owners and consumers. Support-only subprocesses remain explicit so
# their success cannot be mistaken for decoded-video producer health.
[[surfaces]]
id = "process.settings_ffmpeg_version"
kind = "support_process"
source = "crates/plurxd/src/main.rs"
anchor = "async fn ffmpeg_version("
obligation = "show the probed binary version as readiness evidence without treating it as qualification"

[[surfaces]]
id = "process.observed_ffmpeg"
kind = "process_owner"
source = "crates/plurxd/src/transcode.rs"
anchor = "fn spawn_ffmpeg("
obligation = "return one owned child and joined diagnostic observation"

[[surfaces]]
id = "process.fragmented_ffmpeg"
kind = "process_owner"
source = "crates/plurxd/src/transcode.rs"
anchor = "fn spawn_ffmpeg_pipe("
obligation = "preserve the media pipe while joining progress and diagnostics"

[[surfaces]]
id = "process.live_tv_producer"
kind = "process_owner"
source = "crates/plurxd/src/live_tv.rs"
anchor = "fn spawn_live_ffmpeg("
obligation = "own the tuner producer under the same plan identity, observation result, and recovery budget"

[[surfaces]]
id = "process.live_tv_stderr"
kind = "process_observer"
source = "crates/plurxd/src/live_tv.rs"
anchor = "async fn capture_live_stderr("
obligation = "replace substring latching with bounded selected-stream diagnostics and joined completion evidence"

[[surfaces]]
id = "process.live_tv_graph_probe"
kind = "support_process"
source = "crates/plurxd/src/live_tv.rs"
anchor = "async fn run_graph_probe(\n    ffmpeg: &str,"
obligation = "bind graph readiness to exact build, device, encoder, renderer, and output surface without qualifying arbitrary media"

[[surfaces]]
id = "process.vod_generation"
kind = "process_owner"
source = "crates/plurxd/src/vodserve.rs"
anchor = "async fn spawn_generation("
obligation = "classify copied VOD output separately and own stderr completion"

[[surfaces]]
id = "process.vod_head_regeneration"
kind = "process_owner"
source = "crates/plurxd/src/vodserve.rs"
anchor = "async fn regenerate_init_head("
obligation = "remain probe/head-only and never attest a complete producer"

[[surfaces]]
id = "process.vod_pipe_consumer"
kind = "process_consumer"
source = "crates/plurxd/src/vodgen.rs"
anchor = "pub async fn run<R, S>("
obligation = "consume the producer pipe without becoming a second child owner"

[[surfaces]]
id = "process.pgs_demux"
kind = "support_process"
source = "crates/plurxd/src/pgs_overlay.rs"
anchor = "async fn prepare_stage("
obligation = "remain subtitle demux support and never attest video decode health"

[[surfaces]]
id = "process.fragment_index"
kind = "copy_process"
source = "crates/plurxd/src/fragindex.rs"
anchor = "async fn build_with_args("
obligation = "own the FFmpeg copy/index child without claiming decoded-video health"

[[surfaces]]
id = "process.progressive_remux"
kind = "copy_process"
source = "crates/plurxd/src/http/stream.rs"
anchor = "async fn remux("
obligation = "own the progressive copy/remux child and classify audio-only transcode separately"

[[surfaces]]
id = "process.subtitle_extract"
kind = "support_process"
source = "crates/plurxd/src/subtitles.rs"
anchor = "async fn extract_vtt("
obligation = "remain whole-track subtitle extraction, not video qualification"

[[surfaces]]
id = "process.subtitle_window"
kind = "support_process"
source = "crates/plurxd/src/subtitles.rs"
anchor = "async fn extract_vtt_window("
obligation = "remain bounded subtitle extraction, not video qualification"

[[surfaces]]
id = "process.pipe_probe"
kind = "support_process"
source = "crates/plurxd/src/pipeprobe.rs"
anchor = "impl Tools for Spawn"
obligation = "keep capability/test probing distinct from producer health"

[[surfaces]]
id = "process.ffmpeg_engine_identity"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "async fn fragment_index_engine_inner("
obligation = "identify the binary and dependency graph without qualifying media output"

[[surfaces]]
id = "process.ffmpeg_capability_query"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "async fn probe_ffmpeg("
obligation = "treat help/list output as advertised capability only"

[[surfaces]]
id = "process.dovi_reshape_graph_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "async fn probe_dovi_reshape_graph("
obligation = "retain graph-probe evidence without attesting an arbitrary source"

[[surfaces]]
id = "process.dovi_passthrough_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "pub async fn has_dovi_passthrough("
obligation = "keep synthetic filter capability separate from per-file RPU evidence"

[[surfaces]]
id = "process.hdr10_passthrough_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "pub async fn has_hdr10_passthrough("
obligation = "record synthetic Main10 capability without claiming producer completion"

[[surfaces]]
id = "process.hdr10_qsv_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "pub async fn has_hdr10_passthrough_qsv("
obligation = "record the QSV upload/encode probe as node capability only"

[[surfaces]]
id = "process.dovi_qsv_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "pub async fn has_dovi_passthrough_with("
obligation = "record QSV pairing evidence without replacing real Dolby media evidence"

[[surfaces]]
id = "process.dovi_pixel_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "async fn dovi_probe_output("
obligation = "bind the per-file pixel probe to source identity and never to cache completion"

[[surfaces]]
id = "process.pacing_probe"
kind = "support_process"
source = "crates/plurxd/src/ffmpeg.rs"
anchor = "async fn probe_burst("
obligation = "keep startup pacing measurement outside decoder qualification"

[[surfaces]]
id = "process.media_origin_probe"
kind = "support_process"
source = "crates/plurxd/src/transcode.rs"
anchor = "pub(crate) async fn probe_media_origin("
obligation = "keep FFprobe timeline evidence distinct from decoded-video health"

[[surfaces]]
id = "process.chapter_probe"
kind = "support_process"
source = "crates/plurxd/src/http/stream.rs"
anchor = "async fn run_chapter_probe("
obligation = "keep bounded FFprobe chapter discovery outside producer qualification"

[[surfaces]]
id = "process.dv_disk_capability"
kind = "offline_process"
source = "crates/plurxd/src/dv_disk.rs"
anchor = "async fn bounded_tool_probe("
obligation = "keep dovi_tool and mkvmerge version probes as capability evidence only"

[[surfaces]]
id = "process.dv_disk_media_probe"
kind = "offline_process"
source = "crates/plurxd/src/dv_disk.rs"
anchor = "async fn probe_bound_with("
obligation = "bind FFprobe input to the held source descriptor and independent source fence"

[[surfaces]]
id = "process.dv_disk_unbound_conversion"
kind = "offline_process"
source = "crates/plurxd/src/dv_disk.rs"
anchor = "async fn run_tool_with_timeout("
obligation = "inventory the unbound conversion child without confusing its output with qualified transcode health"

[[surfaces]]
id = "process.dv_disk_bound_conversion"
kind = "offline_process"
source = "crates/plurxd/src/dv_disk.rs"
anchor = "#[cfg(unix)]\nasync fn run_tool_with_bound_media("
obligation = "retain descriptor-bound conversion identity, timeout, and post-run source verification"

# Renderer constraints that the future candidate validator must consume.
[[surfaces]]
id = "renderer.candidates"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub const CANDIDATES: &[Pipeline]"
obligation = "preserve deterministic candidate order in explicit plan selection"

[[surfaces]]
id = "renderer.live_tv_filter"
kind = "renderer_contract"
source = "crates/plurxd/src/live_tv.rs"
anchor = "fn live_video_filter("
obligation = "validate Live TV deinterlace, scale, pixel format, and upload transitions against the resolved plan"

[[surfaces]]
id = "renderer.pairing"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn pairs_with("
obligation = "validate encoder and surface-family pairing"

[[surfaces]]
id = "renderer.residency"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn on_gpu("
obligation = "describe frame residency and required transfers"

[[surfaces]]
id = "renderer.dynamic_range"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn handles("
obligation = "preserve HDR and Dolby Vision correctness restrictions"

[[surfaces]]
id = "renderer.decode_arguments"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn decode_args("
obligation = "fold required surface flags into the resolved decoder"

[[surfaces]]
id = "renderer.device_initialization"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn init_args("
obligation = "freeze Vulkan/OpenCL device initialization in the plan"

[[surfaces]]
id = "renderer.filters"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn filters("
obligation = "validate exact graph, pixel format, and transfer composition"

[[surfaces]]
id = "renderer.software_requirement"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn requires_software_decode("
obligation = "retain frame side data and system-memory requirements"

[[surfaces]]
id = "renderer.session_selection"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn for_session("
obligation = "replace legacy session inference with candidate validation"

[[surfaces]]
id = "renderer.output_grade"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn output_grade("
obligation = "bind SDR or HDR10 output identity and encoder arguments"

[[surfaces]]
id = "renderer.fallback"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn fallback("
obligation = "map only presentation-compatible alternatives"

[[surfaces]]
id = "renderer.declined"
kind = "renderer_contract"
source = "crates/plurx-core/src/transcode/pipeline.rs"
anchor = "pub fn declined("
obligation = "retain bounded refusal reasons when candidate constraints reject a graph"

# Local and shared transcode cache boundaries.
[[surfaces]]
id = "cache.shared_mount_io"
kind = "cache_lookup"
source = "crates/plurxd/src/shared_cache.rs"
anchor = "pub(crate) async fn run_mount_io"
obligation = "keep blocking shared-cache access inside its bounded mount operation"

[[surfaces]]
id = "cache.shared_root_identity"
kind = "cache_lookup"
source = "crates/plurxd/src/shared_cache.rs"
anchor = "async fn open_verified_root"
obligation = "bind reads to the verified shared filesystem root before receipt validation"

[[surfaces]]
id = "cache.shared_root_admission"
kind = "cache_write"
source = "crates/plurxd/src/shared_cache.rs"
anchor = "async fn publish_verified_root"
obligation = "admit publication only through the verified shared root and preserve receipt identity"

[[surfaces]]
id = "cache.local_publish"
kind = "cache_write"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn publish_from("
obligation = "publish a receipt with the immutable generation"

[[surfaces]]
id = "cache.manifest_publish"
kind = "cache_write"
source = "crates/plurx-core/src/transcode/manifest.rs"
anchor = "pub async fn publish_controlled<F>("
obligation = "bind receipt identity to pathname-backed publication"

[[surfaces]]
id = "cache.manifest_capability_publish"
kind = "cache_write"
source = "crates/plurx-core/src/transcode/manifest.rs"
anchor = "pub async fn publish_controlled_directory<F>("
obligation = "bind receipt identity to descriptor-backed publication"

[[surfaces]]
id = "cache.shared_publish"
kind = "cache_write"
source = "crates/plurxd/src/shared_cache.rs"
anchor = "pub(crate) async fn publish_generation("
obligation = "copy and reverify the same qualified manifest and receipt"

[[surfaces]]
id = "cache.location_read"
kind = "cache_lookup"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn cache_read_location("
obligation = "require a compatible receipt from either storage class"

[[surfaces]]
id = "cache.local_location_read"
kind = "cache_lookup"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn local_cache_read_location("
obligation = "apply the same receipt rule to node-local fallback"

[[surfaces]]
id = "cache.speculative_hit"
kind = "cache_lookup"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn verified_cache_hit("
obligation = "never report a hit from bytes without qualified completion"

[[surfaces]]
id = "cache.offer_verification"
kind = "cache_lookup"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn verify_cache_offer_location("
obligation = "authenticate the offered generation and receipt"

[[surfaces]]
id = "cache.shared_read_prepare"
kind = "cache_lookup"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn prepare_shared_cached_read("
obligation = "pin the exact shared generation before receipt validation"

[[surfaces]]
id = "cache.session_serve"
kind = "cache_serve"
source = "crates/plurxd/src/transcode.rs"
anchor = "async fn serve_cached("
obligation = "install only a receipt-compatible cached session"

[[surfaces]]
id = "cache.playlist_read"
kind = "cache_serve"
source = "crates/plurx-core/src/transcode/manifest.rs"
anchor = "pub async fn read_verified_playlist("
obligation = "serve the authenticated playlist snapshot"

[[surfaces]]
id = "cache.object_read"
kind = "cache_serve"
source = "crates/plurx-core/src/transcode/manifest.rs"
anchor = "pub async fn open_verified_object("
obligation = "serve the same object handle that was authenticated"

[[surfaces]]
id = "cache.decoded_manifest"
kind = "cache_lookup"
source = "crates/plurxd/src/manifest_cache.rs"
anchor = "pub async fn load("
obligation = "cache decoded manifests only under their fenced digest and receipt identity"

# Offline lookup and delivery are separate call paths over the transcode cache.
[[surfaces]]
id = "offline.prepare"
kind = "offline_owner"
source = "crates/plurxd/src/offline.rs"
anchor = "async fn prepare("
obligation = "persist one job-scoped recovery budget across claims and restarts"

[[surfaces]]
id = "offline.publish_ready"
kind = "offline_owner"
source = "crates/plurxd/src/offline.rs"
anchor = "async fn publish_ready("
obligation = "refuse ready state without complete qualified producer evidence"

[[surfaces]]
id = "offline.generation_cache"
kind = "offline_lookup"
source = "crates/plurxd/src/http/offline.rs"
anchor = "fn cached_offline_generation("
obligation = "cache decoded manifests without weakening receipt validation"

[[surfaces]]
id = "offline.location_validation"
kind = "offline_lookup"
source = "crates/plurxd/src/http/offline.rs"
anchor = "async fn validate_package_candidate("
obligation = "validate local or shared location and its qualified receipt"

[[surfaces]]
id = "offline.playlist"
kind = "offline_serve"
source = "crates/plurxd/src/http/offline.rs"
anchor = "pub async fn playlist("
obligation = "serve only a verified playlist from the qualified generation"

[[surfaces]]
id = "offline.segment"
kind = "offline_serve"
source = "crates/plurxd/src/http/offline.rs"
anchor = "pub async fn segment("
obligation = "serve only the authenticated object handle"

# Client declarations are capabilities, not two-player handoff evidence.
[[surfaces]]
id = "client.web_actions"
kind = "client_capability"
source = "crates/plurxd/src/web/playback-control.js"
anchor = 'Object.freeze(["hold", "retry_resource", "terminal"])'
obligation = "add and physically qualify prepare_replacement before full recovery"

[[surfaces]]
id = "client.apple_actions"
kind = "client_capability"
source = "clients/apple/Sources/PlaybackControlReporter.swift"
anchor = 'static let supportedActions = ["hold", "retry_resource", "terminal"]'
obligation = "implement and physically qualify two-player replacement"

[[surfaces]]
id = "client.android_actions"
kind = "client_capability"
source = "clients/android/app/src/main/java/tv/plurx/app/player/PlaybackControlReporter.kt"
anchor = 'val SUPPORTED_ACTIONS = listOf("hold", "retry_resource", "terminal")'
obligation = "implement and physically qualify two-player replacement"

version = 1

[[coverage]]
commits = ["19d9911b"]
points = ["playback.pipeline"]
checks = ["rust-gate"]
reason = "rejected_rolling_snapshots_obey_the_request_loop_deadline exercises the actual inner request service against persistent header-only and missing-retained-boundary playlists, requiring its typed deadline without the outer HTTP timeout and retaining terminal failure precedence. Six adjacent focused deadline, producer-rebinding, startup-cushion and terminal-cause tests passed on the pinned compiled daemon binary; the normal workspace hook passed. The mechanical ownership inventory records removal of the duplicate production poll plus the test-only timeout and terminal injection; its focused contract suite passed. This is the sole final adversarial review finding's correction."

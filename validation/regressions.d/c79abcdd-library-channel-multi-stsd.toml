version = 1

[[coverage]]
commits = ["c79abcdd", "b718bf58"]
points = ["playback.pipeline", "library.channels"]
checks = ["rust-gate", "rust-gate-ci"]
reason = "Production Library-channel session s-99a265 on nynuc reproduced a legal seeked ffmpeg init with two hvc1 entries in one stsd. `complete_multi_entry_hevc_is_a_typed_validated_structural_refusal` and `incomplete_multi_entry_hevc_configuration_remains_terminal` require independent validation of every description before fallback. `validated_multi_entry_hevc_feed_requests_fallback_before_publication` drives the parsed bytes through the copy reader and proves it publishes no init or playlist. `copy_retry_is_unsupported_only_and_successor_classifier_is_recipe_owned` proves the actor admits one legacy retry and cannot recurse."

version = 1

[[coverage]]
commits = ["62aa054e", "7d00f646", "3caf7ea6", "7286221b", "5b8a6235", "3b1cb76e", "7bcd22ab", "b8d46627", "3c602880", "6b306185"]
points = [
  "cluster.auth",
  "cluster.page-reads",
  "core.media",
  "persistence.upgrades",
  "server.api",
  "validation.framework",
]
checks = [
  "api-wire",
  "catalog-contract",
  "cluster-auth",
  "database-upgrades",
  "history-regressions",
  "operations-contract",
  "resource-bounds",
  "rust-gate",
  "user-journey",
]
reason = "The retained vendor tests exercise real SQLite snapshot build/install success and rejected-candidate exits, cancellation classification, concurrent immutable metrics publications, exact-pointer reads before and after forced auto-heal, cleanup when process memory lags a deliberately older durable pointer, read-only generation inspection, live-metadata fallback after a failed legacy build, pointer-less recovery by greatest applied Raft index rather than UUID order, and durable pending-generation completion after a cancelled caller plus path-scoped post-restore pointer failure; that handoff refuses a replacement receiver while unresolved and preserves its new bytes after recovery. The one-voter compaction growth gate proves real build deltas. The three-voter target compiles the integration surface, while API/rendering tests prove the local-only wait-free scrape boundary, exact fixed Prometheus labels derived from the exported bounds, and the absence of storage calls or dynamic identifiers."

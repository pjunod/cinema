version = 1

[[coverage]]
commits = ["62aa054e", "7d00f646", "3caf7ea6", "7286221b", "5b8a6235", "3b1cb76e", "7bcd22ab", "b8d46627"]
points = [
  "cluster.auth",
  "cluster.page-reads",
  "core.media",
  "persistence.upgrades",
  "server.api",
  "validation.framework",
]
checks = [
  "api-wire",
  "catalog-contract",
  "cluster-auth",
  "database-upgrades",
  "history-regressions",
  "operations-contract",
  "resource-bounds",
  "rust-gate",
  "user-journey",
]
reason = "The retained vendor tests exercise real SQLite snapshot build/install success and rejected-candidate exits, cancellation classification, concurrent immutable metrics publications, exact-pointer reads before and after forced auto-heal, cleanup when process memory lags a deliberately older durable pointer, and pointer-less legacy recovery by authoritative live metadata or greatest applied Raft index rather than UUID order; the one-voter compaction growth gate proves real build deltas. The three-voter target compiles the integration surface, while API/rendering tests prove the local-only wait-free scrape boundary, exact fixed Prometheus labels derived from the exported bounds, and the absence of storage calls or dynamic identifiers."

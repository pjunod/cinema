version = 1

[[coverage]]
commits = ["c29b0ac2"]
points = ["playback.pipeline", "web.experience"]
checks = ["web-static"]
reason = "The shipped-player fixture gives an Auto remux 18.310 seconds of loaded media, no media error, and no frame progress, then requires one same-recipe seek repair with an unknown cause, no transcode, and an exact frozen recipe covering method, copy mode, quality, dimensions, dynamic range, audio, subtitle, offset, session, attachment, and control generations. Its companion cases require a typed decoder error and typed network error to remain independently attributable, a bare play event to leave recovery unresolved, frame progress to settle it once, film-clock progress to settle only when frame counters are unavailable, and a newer viewer intent to cancel the episode. The policy fixture separately rejects a rung-changing transcode and legacy stall reopen reason without decoder/network evidence while retaining those adaptation paths when the evidence is present; the settings fixture rejects the retired typeless-HLS toggle and outgoing setting. `web-static` runs all three retained source-linked suites and the embedded-JavaScript checker."

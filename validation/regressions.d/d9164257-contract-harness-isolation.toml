version = 1

[[coverage]]
commits = ["d9164257", "f3040512", "2394cc8b", "0cc6902d", "246fa799", "dde5978d", "19f01dc7", "eba47254"]
points = ["cluster.auth", "cluster.page-reads", "persistence.upgrades", "validation.framework"]
checks = ["rust-gate", "cluster-auth", "history-regressions"]
reason = "The retained Rust and replicated-contract gates create an isolated three-voter ContractCluster for every migrated test and pass that cluster explicitly to the remote Store opener. The harness selects each cluster's complete six-port set while all probe listeners coexist, preventing duplicate numbers within the set. Vendored Hiqlite synchronously binds each exact child's Raft and API sockets before either detached server can run and before start_node may return; either collision is therefore the intended child's address-specific startup verdict, while bounded raw connects subsequently prove both owned servers are accepting. The parent reallocates the entire set under a bounded retry without stderr timing heuristics, shared-secret identity ambiguity, or one-shot remote-client reconnect tasks. The history audit separately pins the evidence-only sender and listener-ownership corrections to this exact retained mechanism. Reverting this merge follow-through restores calls to the removed process-global harness, omits the now-required cluster argument, revives nondeterministic listener collisions, or drops required corrective-history evidence."

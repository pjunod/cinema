version = 1

[[coverage]]
commits = ["93039753", "dcb76dec"]
points = ["cluster.auth", "cluster.membership", "operations.packaging"]
checks = ["rust-gate", "cluster-auth", "operations-contract", "history-regressions"]
reason = "The vendored Hiqlite regressions fill the bounded Raft request and writer queues, require the handler coordinator to consume a retained reset, discard a canceled queued RPC before a replacement socket can transmit it, and prove forced reset cleanup cannot block behind the writer. The standard cluster-auth lane runs those exact regressions, both snapshot mismatch boundaries, OpenRaft's locked offset-zero retry test, the three-voter learner snapshot lifecycle, topology and leader/follower failure drills, and real-daemon restart contracts. Operations contracts parse the effective Compose and final-image health grace, derive the budget from source timeout bounds and all three named application phases, and require the preflight to precede every standard Compose mutation."

version = 1

[[coverage]]
commits = ["732d3442"]
points = ["playback.pipeline", "validation.framework"]
checks = ["catalog-contract", "rust-gate", "rust-gate-ci"]
reason = "Round-1 repairs wake the passive executor after timer- or producer-ingress-only terminal transitions, monitor unexpected executor receiver loss inside the actor without recreating mailbox self-ownership, retain the actor's committed End/authority/expiry cause across later mailbox loss, and distinguish observation from DecisionApplied. Deterministic regressions cover cancelled polling, retained Notify delivery, capacity-one wake coalescing, timer-only lease expiry, executor abort, actor abort, terminal-cause preservation, and weak transport teardown; compile/lint repairs preserve warnings-as-errors."

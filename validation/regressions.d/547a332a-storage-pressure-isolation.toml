version = 1

[[coverage]]
commits = ["547a332a"]
points = ["core.media", "persistence.upgrades", "server.api", "operations.packaging"]
checks = ["rust-gate", "database-upgrades", "cluster-auth", "operations-contract", "resource-bounds"]
reason = "The retained configuration and startup regressions prove omitted roots preserve every legacy path, explicit cache and transcode roots cannot alias durable data, shared cache, or credentials through canonical paths, inodes, stacked Linux mounts, or mount-source ancestry, and only an owner-claimed scratch tree may be cleared after the daemon lock is held. Descriptor-relative cleanup is bounded by device, depth, entry count, ownership, and mode; crash hooks prove an incomplete scratch claim never authorizes deletion. Migration keeps storage.data_dir authoritative and the replicated startup gate exercises the bounded read pool without changing the WAL, snapshot, sync, or election defaults. Operations and resource checks retain the explicit environment, example, mount, capacity, restart, rollback, and private-image evidence contract."

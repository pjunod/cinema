version = 1

[[coverage]]
commits = ["404c8d55", "4c93b342", "d7c55089"]
points = ["apple.client", "operations.packaging"]
checks = ["apple-simulators", "operations-contract", "container-runtime"]
reason = "Concrete simulators, writable state, and ephemeral ports are checked statically and then exercised by both platform and container jobs. The Apple lane removes only workflow-owned stale devices left behind when a whole-job deadline prevents final cleanup, then explicitly boots each newly isolated destination through runtime readiness before XCTest begins; the operations contract pins cleanup-before-create and boot-before-test ordering."

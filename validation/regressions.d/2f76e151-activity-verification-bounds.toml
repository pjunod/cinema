version = 1

[[coverage]]
commits = ["2f76e151"]
points = ["cluster.auth", "cluster.membership", "server.api"]
checks = ["cluster-auth", "rust-gate", "security-boundaries"]
reason = "The retained cluster lifecycle independently constructs the removed shared-HMAC activity exploit and both directions of the established v4 artwork proof, requiring impersonation to fail while old and new artwork peers remain compatible. Unit regressions fill a 64-peer verifier cache, require one uncached peer to replace exactly one entry, and exhaust then reopen the global cache-miss admission window. Paused-time daemon coverage requires queued peer requests to expire at the original common deadline. Reverting this correction rejects an uncached voter, breaks rolling artwork compatibility, or reintroduces scheduler-dependent deadline coverage."

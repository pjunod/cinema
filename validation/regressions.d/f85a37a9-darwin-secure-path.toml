version = 1

[[coverage]]
commits = ["f85a37a9"]
points = ["core.media", "persistence.upgrades", "server.api"]
checks = ["rust-gate", "database-upgrades", "cluster-auth", "api-wire", "resource-bounds"]
reason = "The merged Darwin secure-path correction pins the production descriptor boundary through current Rust tests: directory faults rewind before the cross-platform errno assertion, canonical temporary roots preserve no-follow path semantics on macOS, child media producers inherit only the held output-directory authority, directory compaction resumes without skipping siblings, and daemon process-state fixtures use the platform's real process interface. The Rust gate directly exercises the secure filesystem, metadata, producer, HTTP, and activation paths; upgrade, cluster, wire, and resource gates retain the affected daemon startup and native API boundaries."

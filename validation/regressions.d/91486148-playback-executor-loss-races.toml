version = 1

[[coverage]]
commits = ["91486148"]
points = ["playback.pipeline", "validation.framework"]
checks = ["catalog-contract", "rust-gate", "rust-gate-ci"]
reason = "Round-2 repairs distinguish uncommitted actor loss from a committed terminal cause, retain terminal truth independently of mailbox lifetime, settle terminal executor state without erasing prior lost evidence, and monitor receiver loss under the transition fence. Actor-start and executor-poll phase barriers plus standalone retained-Notify and capacity-one inbox probes make the liveness and actor-loss regressions independent of task scheduling."

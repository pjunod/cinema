version = 1

[[coverage]]
commits = ["f3612451"]
points = ["playback.pipeline", "web.experience"]
checks = ["web-static", "rust-gate"]
reason = "The passive browser controller is pinned by `tests/playback/web-control.test.js`: it proves strict bootstrap/response fences, one in-flight exchange, newest-state coalescing, typed retry/owner transition, predecessor cancellation isolation, terminal end, adapter state mapping, trigger wiring, the bounded transport deadline, and dynamic capability refresh. The Rust regression proves accepted and current-trigger client contexts are bounded, separately labeled, and generation-redacted before persistence. Reverting the reporter, its shipped asset route, or that correlation breaks the mapped web/Rust gates."

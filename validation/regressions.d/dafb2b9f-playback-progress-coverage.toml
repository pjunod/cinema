version = 1

[[coverage]]
commits = ["dafb2b9f", "12ed2ff3", "205187e9", "03ee8ff1"]
points = ["playback.pipeline", "validation.framework"]
checks = ["catalog-contract", "rust-gate", "rust-gate-ci"]
reason = "The validation catalog pins the known legacy rolling producer recovery, replacement, transition, failure, process-action, and response-admission owners; scans the entire compatibility source; normalizes comments, literals, turbofish payloads, and owner-checked innermost balanced invoked-callable groups out of structural evidence; and applies module-wide legacy/deadline plus task, timer, command/process-start, lifecycle, and import/namespace/type/local-alias sentinels. Thirty-three positive and three negative contract snippets distinguish real wrapped starts—including argument-nested, `for ... in`, and labeled-break forms—from ordinary callable-owned argument groups and a raw-keyword identifier owner, while namespaced clone/clone3 remains restricted to process APIs, so comment or ordinary data-clone churn cannot offset a process start before the final M4 deletion check drives legacy counts to zero. The Rust producer-ingress regressions retain constant-space first/covered/gap/latest deadline evidence, use a persistent exact-attempt watermark so a repeated timestamp across drains cannot manufacture a deadline link, reset that watermark for a successor's lower restarted timeline while rejecting late predecessor progress, prove a timely delayed A-to-B-to-C chain, prevent later telemetry from bridging the first missed horizon while preserving latest visible progress, seal progress into the exact exit barrier, preserve nonblocking mailbox behavior, and keep post-exit progress terminal."

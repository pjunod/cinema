version = 1

[[coverage]]
commits = ["dafb2b9f", "12ed2ff3", "205187e9", "03ee8ff1"]
points = ["playback.pipeline", "validation.framework"]
checks = ["catalog-contract", "rust-gate", "rust-gate-ci"]
reason = "The validation catalog pins the known legacy rolling producer recovery, replacement, transition, failure, process-action, and response-admission owners; scans the entire compatibility source; and applies module-wide legacy/deadline plus namespaced/method/bare task, timer, command construction and method/bare-or-namespaced-UFCS launch, low-level fork/clone/spawn, method/free/UFCS signal-wait-kill-exec, and import/namespace/type/local-callable alias sentinels so unreviewed structural drift fails before the final M4 deletion check drives legacy counts to zero. Namespaced clone/clone3 is restricted to process APIs so ordinary data cloning cannot offset a low-level process start. The Rust producer-ingress regressions retain constant-space first/covered/gap/latest deadline evidence, use a persistent exact-attempt watermark so a repeated timestamp across drains cannot manufacture a deadline link, reset that watermark for a successor's lower restarted timeline while rejecting late predecessor progress, prove a timely delayed A-to-B-to-C chain, prevent later telemetry from bridging the first missed horizon while preserving latest visible progress, seal progress into the exact exit barrier, preserve nonblocking mailbox behavior, and keep post-exit progress terminal."

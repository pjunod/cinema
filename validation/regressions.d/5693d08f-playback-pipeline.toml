version = 1

[[coverage]]
commits = ["5693d08f"]
points = ["playback.pipeline"]
checks = ["rust-gate", "rust-gate-ci"]
reason = "The retained transcode regressions recognize only HEVC sources whose primary video stream has the legal 23-byte minimal hvcC shape, keep VPS/SPS/PPS long enough to populate init data, and change the copy-pipeline fingerprint for repaired VOD indexes. Core fMP4 regressions require a complete VPS/SPS/PPS set, prove refused promotion is atomic, and prove hvc1 segment serialization removes those NALs while preserving the encoded picture and Dolby Vision RPU. Daemon regressions refuse incomplete configurations in both VOD analysis and live HLS, while progressive-remux argv tests require hev1 for ordinary in-band HEVC and dvhe plus unofficial MOV strictness for preserved Dolby Vision. The existing ordinary-HEVC filter and Dolby Vision strip behavior remain pinned."

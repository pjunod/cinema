version = 1

[[coverage]]
commits = ["5693d08f"]
points = ["playback.pipeline"]
checks = ["rust-gate", "rust-gate-ci"]
reason = "The retained transcode regressions recognize only HEVC sources whose primary video stream has the legal 23-byte minimal hvcC shape, keep VPS/SPS/PPS in-band so first-fragment promotion can populate init data, and change the copy-pipeline fingerprint for repaired VOD indexes. The same matrix preserves parameter-set filtering for ordinary HEVC and the existing Dolby Vision strip policy, including retaining VPS/SPS/PPS when minimal configuration and stripped Dolby Vision coincide."

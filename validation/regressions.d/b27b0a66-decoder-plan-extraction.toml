version = 1

[[coverage]]
commits = ["b27b0a66", "32aeb759", "935b060f", "3f0f75e6", "4c093b65", "19827c87", "c03a98b3"]
points = ["playback.pipeline"]
checks = ["rust-gate"]
reason = "The M1 selector matrix freezes the extracted decoder plan without changing broad routing: it retains the MPEG-4/VideoToolbox exclusion and legacy preferences; rejects unknown or contradictory pixel, HDR/Dolby, geometry, surface, build, device, driver, and capability evidence; binds selected-stream and source identity into the facts digest; and records effective presentation and ordered renderer transitions. Daemon tests prove FFprobe runs a descriptor-bound or immutable startup snapshot, duplicates source and executable descriptors before fixed-fd assignment, reads the exclusively leased held source, charges lease waits to the same deadline, resolves legacy ordinals to absolute streams, applies catalog facts only to the catalog's first playable stream, rechecks source and build before caching, preserves hash singleflight after waiter cancellation, signals the full probe process group before restoring the source offset, bounds output, and preserves the legacy producer's original startup budget after neutral observation. These regressions fail if any review repair or the original extraction is reverted."

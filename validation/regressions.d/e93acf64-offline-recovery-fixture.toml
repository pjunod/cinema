version = 1

[[coverage]]
commits = ["e93acf64"]
points = ["playback.pipeline"]
checks = ["rust-gate", "rust-gate-ci", "history-regressions"]
reason = "The retained offline manager regression scripts a refused primary, a yielded alternate, and a terminal refused alternate across a durable requeue. Its recovery-specific fixture enables the same runtime authorization required by production, so the first refusal must install and persist the alternate before the manager requeues the package; omitting that fixture guard fails the test by settling the package as decode_unhealthy instead."

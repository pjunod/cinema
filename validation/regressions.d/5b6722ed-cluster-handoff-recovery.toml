version = 1

[[coverage]]
commits = ["5b6722ed"]
points = ["cluster.auth"]
checks = ["cluster-auth", "rust-gate"]
reason = "The retained repair-handoff regressions require a mature successor to reprove the same leader term, fresh quorum watermark, complete monotonic lease, and unchanged durable fence/index after any read-only reroute; the generation CAS remains single-dispatch. The separate-process membership lifecycle repeats that evidence after typed forward-to-leader responses, while the full cluster harness proves the handoff cannot advance stale recovery state."

version = 1

[[coverage]]
commits = ["a76860d2"]
points = [
  "cluster.auth",
  "cluster.membership",
  "cluster.operations",
  "core.media",
  "identity.access",
  "server.api",
]
checks = [
  "rust-gate",
  "rust-gate-ci",
  "hiqlite-vendor-clippy",
  "cluster-auth",
  "security-boundaries",
  "operations-contract",
]
reason = "The focused cluster/WAL lane runs the exact SQLite and three-voter Store interleaving that rejects token insertion after the authenticated password hash changes. It also runs the replicated rollback regression proving a legacy heartbeat may remain live but legacy password, role, user, and token mutations fail immediately unless the same transaction carries the current Store intent."

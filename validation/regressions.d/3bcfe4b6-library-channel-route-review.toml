version = 1

[[coverage]]
commits = ["3bcfe4b6"]
points = ["library.channels", "web.experience"]
checks = ["rust-gate-ci", "operations-contract", "web-static"]
reason = "The retained router regression constructs the complete application and drives both Library-channel collection spellings through unauthenticated and authenticated GET, invalid POST, and oversized POST requests while asserting authentication, the 64 KiB ceiling, and private no-store headers. The operations contract binds the compatibility route to the canonical web, Apple, and Android paths and scopes untyped 404 route diagnosis to collection-page loading; web-static parses the shipped JavaScript."

version = 1

[[coverage]]
commits = ["ad1e2b6b"]
points = ["cluster.auth", "cluster.membership", "server.api"]
checks = ["cluster-auth", "rust-gate", "security-boundaries"]
reason = "The retained three-voter lifecycle recreates the old exploit by using a removed node's shared API secret to sign a fresh request that claims a surviving activity sender; only distinct per-node authority rejects it. Migration coverage preserves path-prefixed join coordinators and keeps peer URLs origin-only. Daemon regressions enforce the exact escaped-JSON byte budget, reject an oversized chunked body, prove redirect refusal, and require a ninth peer to expire immediately when the first concurrency window consumes the common deadline. Reverting the correction restores shared-secret activity impersonation, join-URL upgrade refusal, unsafe response handling, or reset peer deadlines."

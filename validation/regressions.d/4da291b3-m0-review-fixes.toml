version = 1

[[coverage]]
commits = ["4da291b3"]
points = ["playback.pipeline"]
checks = ["rust-gate"]
reason = "Four corrections to M0's own pins, each with the check that would have caught it. The two `plan_derivation::snapshot()` deltas were exact against process-global `AtomicU64` counters that nineteen other tests in the same binary increment on other threads, so `cargo test -p plurxd` under default parallelism could fail `a_create_that_sends_no_caps_document_still_gets_a_review` and `every_review_is_counted_and_the_parts_never_exceed_the_total` on an unrelated change; both are lower bounds now, which stay red under every mutation of the arms they pin and green under everyone else's counting, and `rust-gate` runs them. The argv capture in `a_copy_that_cannot_convert_strips_the_argv_it_spawns` held a 256-entry `LogBuffer` ring whose oldest entry is the argv line, while `start_copy` spawns a real ffmpeg logging its stderr a line at a time — 256 events after the spawn and the test fails on its `expect` rather than on its assertions; the ring is 8192 and one assertion was added so `-tag:v hvc1`, which `tag_for` also returns for a preserved Profile 7, is no longer the only discriminator. `STATUS.md` described merged #849 as open, which `tests/operations/test_status_pr_claims.py` fails on under `operations-contract`. And the two `rolling-producer-owners.toml` sentinels were one behind after M0's `write_real_hevc_video` fixture added a `Command::new` and its `.status()`, which `test_rolling_producer_ownership_inventory.py` fails on under the same check."

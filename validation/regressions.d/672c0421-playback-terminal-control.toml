version = 1

[[coverage]]
commits = ["672c0421", "619765ab", "2f7953c5", "3f34493e"]
points = ["playback.pipeline", "server.api"]
checks = ["rust-gate"]
reason = "The Rust gate retains the corrected M3c3 terminal-control contracts: accepted rolling and VOD demand=end requests atomically enter terminal state; one post-linearization continuation preserves the exact reply after HTTP cancellation while pre-admission and nonterminal work remain caller-bounded; reply insertion and exact-owner route settlement are one Store transaction that cannot leave takeover eligible; the rolling reaper joins the pending reply fence; exact retries share one receipt and retain original platform telemetry; ended responses validate through the cluster relay; terminal mailbox commands claim an already-due monotonic expiry before End or AuthorityFence can relabel it; and manager, VOD, HTTP, relay, backend-neutral Store, cancellation/reaper, crash/takeover, and 5,040-order model tests retain those invariants."

version = 1

[[coverage]]
commits = ["672c0421", "619765ab", "2f7953c5", "3f34493e", "ca2a333c", "049bd43a", "7975a8ca", "8406a873", "cd6fa076"]
points = ["playback.pipeline", "server.api"]
checks = ["rust-gate"]
reason = "The Rust gate retains the corrected M3c3 terminal-control contracts: accepted rolling and VOD demand=end requests atomically enter terminal state; the rolling actor installs one session-owned operation before its fallible reply and queued controls cannot mutate after cancellation or their inherited deadline; exact retries share that operation while the reaper joins its pending fence; every Store write, read, and backoff is capped by one absolute attempt deadline and the immutable acknowledgement expiry; starts, success completions, and delayed waiters fail at or after that boundary; fail-before and commit-then-error outcomes reuse one immutable acknowledgement; attempt exhaustion remains recoverable from the rolling-manager or VOD tombstone without a new timestamp only inside that exact window; rolling and VOD retention are checked immediately before, at, and after the prepared receipt deadline; VOD releases the response/Store graph after expiry while retaining its ownership tombstone and completed idempotent cleanup marker; simultaneous retry waiters share one paused in-flight attempt and project Replay truthfully; every VOD retry rendezvouses at and waits through session-owned reader detach before durable visibility; expiry plus repeated end emits exactly one lifecycle event; removing the VOD tombstone releases the deferred operation graph; reply insertion and exact-owner route settlement are one Store transaction that cannot leave takeover eligible; replay retains its original platform; ended responses validate through the cluster relay; terminal mailbox commands claim an already-due monotonic expiry before End or AuthorityFence can relabel it; and manager, VOD, HTTP, relay, backend-neutral Store, cancellation/reaper, crash/takeover, and 5,040-order model tests retain those invariants."

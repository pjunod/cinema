version = 1

[[coverage]]
commits = ["672c0421", "619765ab", "2f7953c5", "3f34493e", "ca2a333c", "049bd43a", "7975a8ca", "8406a873"]
points = ["playback.pipeline", "server.api"]
checks = ["rust-gate"]
reason = "The Rust gate retains the corrected M3c3 terminal-control contracts: accepted rolling and VOD demand=end requests atomically enter terminal state; the rolling actor installs one session-owned operation before its fallible reply and queued controls cannot mutate after cancellation or their inherited deadline; exact retries share that operation while the reaper joins its pending fence; every Store write, read, and backoff is capped by one absolute attempt deadline; fail-before and commit-then-error outcomes reuse one immutable acknowledgement; attempt exhaustion remains recoverable from the bounded rolling-manager or VOD tombstone without a new timestamp; concurrent waiters project Replay truthfully; VOD reader cleanup precedes durable visibility; reply insertion and exact-owner route settlement are one Store transaction that cannot leave takeover eligible; replay retains its original platform; ended responses validate through the cluster relay; terminal mailbox commands claim an already-due monotonic expiry before End or AuthorityFence can relabel it; and manager, VOD, HTTP, relay, backend-neutral Store, cancellation/reaper, crash/takeover, and 5,040-order model tests retain those invariants."

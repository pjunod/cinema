version = 1

[[coverage]]
commits = ["ba397026"]
points = [
  "cluster.auth",
  "cluster.membership",
  "identity.access",
  "server.api",
]
checks = [
  "rust-gate",
  "rust-gate-ci",
  "hiqlite-vendor-clippy",
  "cluster-auth",
  "security-boundaries",
]
reason = "The persistent cluster/WAL lane constructs an exact three-voter rolling upgrade and proves that legacy login and password mutations remain usable while only part of the roster advertises cache-admin revocation v3. It then activates the permanent guard under the replicated membership exclusion only after all three current heartbeat proofs exist, rolls one voter back, and proves legacy password, login-token, and logout-token mutations fail closed."

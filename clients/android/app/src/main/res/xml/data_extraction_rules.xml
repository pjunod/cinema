<?xml version="1.0" encoding="utf-8"?>
<!--
  API >= 31 backup and transfer rules. Both sections carry the same exclusions:
  `cloud-backup` alone would leave device-to-device transfer free to copy the
  same bytes to a new handset. See backup_rules.xml for why `datastore/` goes
  as a whole directory.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="offline/" />
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="offline/" />
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
